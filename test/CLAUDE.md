# test/ — Minitest + Capybara 테스트 스위트 (1182 runs)

'책갈피'(Rails 8.1)의 전체 자동화 테스트 모음이다. 모델·정책·서비스 단위 테스트부터 역할별 화면·플로우를 검증하는 integration 테스트까지 포함한다. 테스트는 **외부 API를 절대 호출하지 않으며**(`test_helper.rb`가 credentials 키를 공란으로 강제 → 도서검색·정보나루·Gemini가 오프라인 폴백 경로를 탄다), 원격 성공 경로는 스텁 커넥션을 DI로 주입해 검증한다.

## 하위 카테고리
- `test_helper.rb` — 공통 설정. 외부 키 공란 강제(오프라인 폴백), 병렬 실행, `fixtures :all`, ISBN 필수 Book 픽스처용 유효·유일 `TestBookIsbn` 기본값과 제약 도입 전 중복 재현용 `create_legacy_book!`, `seed_monster_species!`·`seed_badges!`·`login_as(user)` 및 최소 XLSX 생성기를 포함한다.
- `models/` — 모델 단위: 뱃지·도서(**`book` — isbn 정규화·유일성 2중방어 + `cover_url` http→https 승격 콜백[혼합 콘텐츠 가드]·https/blank 불변 + `promote_from_search!`[searched→recommended 승격·비-searched no-op]**)·학급·학교·리포트(**`report_book_title_normalization` — `before_validation :normalize_book_title` squish·빈값→nil·정상 제목 불변**)·토픽·유저·몬스터종·유저몬스터, 게임화/퀴즈 데이터 모델(`quiz_question`의 1-based `answer_number` 가상 접근자 포함), **`game_play`**(부분 유니크 인덱스 2종의 일일 dedup — book 있는/없는 플레이 각각; sequel:5 additive enum 포함), **`book_sequel`**(뒷이야기 body 검증[10..2000]·ai_status enum·ranked/for_classroom scope·공감 1인 1표), **`quiz_contribution`**(학생 출제 기여 — 축별 페이로드 검증[mcq 보기4·정답인덱스 범위·프롬프트, hint_reveal 정답+힌트≥2]·status enum[pending/approved/rejected]·금칙어 QUIZ 리스트), 몬스터 시드 무결성, 마이그레이션 FK 존재 검증. **Phase 6 보강(#5)**: `library_loan`·`library_event`·`user_badge`·`forum_post`·`forum_post_like`(좋아요 uniqueness·likes_count counter_cache 증감) 모델 검증, `app_setting`(API 키 저장 차단 보안검증 추가), `monster_seed_integrity`(스타터 3종 정합 + `image_key` WebP 72종의 누락·잔존 파일 검증 + **게임 재구성 Phase 5 골든 불변식**: unlock/evolve 어떤 `distinct_games` 조건도 활성 게임 종류 수 4 이하), **`fk_on_delete_roundtrip`**(reports→books·monster_species 자기참조 on_delete nullify 동작 + SQLite up/down 왕복 무손실; 이 파일만 `use_transactional_tests = false` — DDL 커밋 후 teardown 에서 up 복원·행 정리). **`monster_species`**는 `unlock_condition` JSON 검증(evolve_condition과 별개 컬럼·화이트리스트 공유)도 함께 커버한다. **`backfill_squished_book_titles`**(독후감 열람 백필 마이그레이션 전용 — 콜백 우회로 심은 레거시 이중공백 `book_title`을 `up` 직접 실행으로 squish 정규화·멱등·`down` no-op 검증, quiz_backfill 관례). **`promote_reviewed_searched_books`**(검색 도서 승격 백필 마이그레이션 전용 — 승인 독후감이 붙은 `searched` 도서만 `up` 직접 실행으로 `recommended` 소급 승격·미승인/독후감 없는 searched·비-searched 불변·멱등·`down` no-op 검증). **`hard_delete_vocab_game`**(게임 재구성 Phase 1 — vocab hard-delete 마이그레이션 `up` 이 game_type=2 game_plays[raw SQL 삽입]과 content_axis=1 matching 퀴즈+자식[question/attempt/report]을 삭제하고 mcq 퀴즈·quiz game_play 는 보존, `down`=IrreversibleMigration; 정수 리터럴이라 enum 비의존).
- `models/concerns/` — 공유 모듈: `Badgeable`·`Evolvable`·`Leveling`·`Pointable`(**랭킹 시즌제 훅 포함** — award/credit 시즌 experience_earned 축적·스냅샷, spend 시즌 불변, revoke floor 0 하향, 비학생·학급없음 무축적, 임포트 초기 experience 시즌 미시딩)·`RubricScorable`. `models/season_score_test.rb`(랭킹 시즌제 점수 — 검증·`[academic_year,user_id]` 유니크·음수 거부·학년도별 1행·`for_year` 스코프).
- `controllers/` — 컨트롤러 공용 로직 단위: `axis_averages_parity`(전교 5축 SQL 집계 == 인메모리 집계 parity, #3).
- `tasks/` — rake 시드·운영 태스크: `quizzes_seed`(샘플 퀴즈 멱등 재현)·`schools_seed`(전량 CSV 헤더/필수값/전국 검증, 배치 upsert, 멱등 갱신, 누락 NEIS·구 합성 학교 비활성화, manual 보존, 파일 미존재 no-op)·`books_seed`(`books:enrich` 큐레이션 보강)·`books_seed_full`(TSV 전량 적재 검증)·`books_deduplicate`(기본 dry-run/명시적 apply 중복 병합)·**`books_seed_summaries`**(Gemini 줄거리 YAML 시드 — `seed_summaries` 매칭·멱등·미매칭 skip·빈/무 YAML 크래시 0·무네트워크 + `export_summaries` Gemini 생성분[checked_at present]만 export·네이버 blurb 제외, `ENV["BOOK_SUMMARIES_YML"]` 임시 파일 주입)·**`books_generate_summaries`**(스텁 GeminiClient/BookSummaryService 로 무키→skip·known→YAML+DB·모름→checked_at만·YAML 이미 포함분 skip·limit 준수, 실제 Gemini 호출 0).
- `policies/` — Pundit 인가. `authorization_matrix_test.rb`가 역할×액션 경계 회귀 매트릭스, 개별 정책(cheer·classroom·report·sticker·topic·**book_sequel**[뒷이야기 학급 경계]·**quiz_contribution**[학생 출제 기여 — 작성=학급 학생 본인만, 교사·비학급·비로그인 차단])이 스코프/권한 세부.
- `services/ai/` — Gemini 클라이언트·OCR·퀴즈 초안·첨삭(review)·규칙기반 첨삭·검증(verify) + **뒷이야기 격려 코멘트(`sequel_feedback_service` — LLM/무키·실패·스키마이탈 폴백·학생 글 body 프롬프트 주입 검증, `rule_based_sequel_feedback` — 항상 긍정·결정적·점수 미부여)** + **줄거리 생성(`book_summary_service` — 무키/모름/저확신(<0.7)/blank/ApiError/스키마이탈→nil, known+고확신→줄거리, 프롬프트에 서지 정보만·기존 summary 미포함 검증, 게임 재구성 Phase 4 §1b)**. 무키 시 규칙기반 폴백 + 스텁 성공 경로.
- `services/books/`·`services/library/` — 도서 검색(네이버) 및 정보나루(data4library) 서비스. `services/books/`는 `isbn`(체크디지트·ISBN-10→13), `search_service`, **`catalog_enricher`**(네이버 ISBN 정확 매칭 표지·출판사 보강 + **네이버 0건 시 정보나루 표지 폴백**·양쪽 무키 no-op), **`deduplicator`**(레거시 중복 dry-run·참조보존 병합), **`genre_inference`** 5파일을 검증한다. `services/library/`의 `data4library_service`는 인기대출(`popular_loans`)과 **표지 상세조회(`cover_url_for`/`srchDtlList` — 빈 표지·미존재·비200·파싱실패 nil, last_error 불오염)** 및 **인근 도서관(`libraries_holding` — `libs[].lib` 정규화·빈결과 []·비200/파싱실패 nil·truncation 경고, `loan_status` — Y/N/에러 status+fetched_at·짧은 read timeout·last_error 불오염)**을 스텁 커넥션으로 검증한다. **`region_codes`**(17교육청 → 정보나루 시도코드 매핑 + 특별자치 별칭·미지값 nil·`for_school` region 우선/address 폴백), **`nearby_availability`**(시도코드 도출·**gu 토큰 완전일치 필터**[서구/달서구·서구/강서구·동구/남동구 제외]·세종 gu nil 유지·N컷 ≤5·시간예산 초과 :unknown degrade·각 state 분기·`cache:` MemoryStore 주입 holdings 캐시 히트 외부콜 0·**as_of=가장 오래된 fetched_at**[15분 캐시→"15분 기준"])도 함께 검증한다.
- `services/schools/` — NEIS 학교기본정보 연동: `gu_parser`(도농복합시·단층제·시도 토큰 생략)·`neis_fetcher`(전체 건수 페이지네이션, 후속 페이지 실패/HTTP 200 API 오류/국내 필터)·`snapshot_validator`(필수값·중복·전국 최소 건수·17개 교육청).
- `services/recommendations/` — 실제 번들 XLSX의 어린이 분과 203권 파싱 계약, ISBN upsert, 청소년 제외, digest 멱등, 활성 목록 교체, 실패 롤백 + **`BookSummaryJob` 신규 유입 예약**(게임 재구성 Phase 4 code-review 후속 — summary blank·checked_at nil 인 신규 도서만 예약, 이미 summary/checked_at 있는 도서는 스킵, `BookEnrichmentJob` 장르 보강과 동형)을 검증한다.
- `services/games/`·기타 서비스 — 퀴즈 플레이 멱등 적립(포인트 파밍 차단), 몬스터 획득, 랭킹 집계, 독서 통계(`reading_stats` — `max_daily_reports`·`game_plays`/`distinct_games`/`game_books` 신규 지표 + **게임 재구성 Phase 5 몬스터 도달성**: 활성 4종[quiz·whoami·book·sequel] 플레이→`distinct_games==4`·dex7/21/23 도달, book+sequel 바닥[2종]→dex23만 도달·dex7/21 미도달[game_plays 충분·distinct_games 게이트] 포함). **`content_provider`는 Phase 3 문제은행 세트 단위 랜덤 출제(sampler seam 으로 결정적 검증 — 풀 여러 세트면 랜덤 후보 중 하나, 1세트면 그거, MISS면 오프라인 플로어) + 기여 세트가 같은 전국 풀 후보가 됨(물질화)을 커버**하고, 기존 회귀(HIT·워밍·재시도·신고·재생성)는 결정적 sampler 로 안정 유지. **게임 재구성 Phase 4**: `game_content_available?`(고전·summary 책·기여 있는 책→available, offline 만 있는 무명 책→unavailable) + 줄거리 미확인 **classic**(가용·실제 도달 경로) 책 resolve 시 `BookSummaryJob` 함께 큐잉(summary 있음·checked_at 있음·무키면 미큐잉)도 커버한다. **`monster_unlock`**(fixpoint 캐스케이드 — dex_count 20 도달 시 같은 `evaluate!` 호출에서 dex 24 동시 해금, AND 다중조건, 멱등 재실행, 라인 지급 실패 rescue).
- `jobs/` — 백그라운드 잡: `AiReviewJob`(AI 첨삭)·`OcrJob`(사진 OCR)·**`SequelFeedbackJob`(뒷이야기 격려 AI 코멘트 — 무키 폴백 done+ai_comment 확보·네트워크 0·LLM 스텁 경로·방송·삭제 no-op·실패 전이)**·**`BookSummaryJob`(Gemini 줄거리 생성 — 무키 no-op[checked_at 미세팅]·키+known=summary+checked_at·키+모름=checked_at만·이미 summary/checked_at 멱등 skip·삭제 no-op, 게임 재구성 Phase 4 §1c)**·`BookEnrichmentJob`(무API genre 보강 — 공란만 채움·멱등)·`RecommendationCoverEnrichmentJob`(추천도서 업로드 뒤 표지 공란 범위만 네이버 비동기 보강).
- `helpers/` — 뷰 헬퍼(`TeacherHelper`, `MonstersHelper`의 WebP 렌더·이모지 폴백).
- `integration/` — 역할별 화면·플로우 E2E(대표 그룹은 아래).
- `system/` — Capybara+Selenium 브라우저 시스템 테스트(`application_system_test_case.rb` = `driven_by :selenium, using: :headless_chrome`). 현재 `reports_guided_compose`(직접쓰기 안내 질문 조립 E2E) 1종. **chromedriver 미가용 환경에서는 각 테스트가 스스로 skip**(무JS·무드라이버 CI 안전) — `bin/rails test`(비-system)와 분리되어 `bin/rails test:system`으로 실행.
- `fixtures/files/` — 테스트 자산. `handwriting.png`(OCR 입력 이미지)·`schools_sample.csv`(NEIS 전량 시드 파싱용 샘플). YAML 픽스처는 사용하지 않고, 데이터는 각 테스트의 `setup`/시더로 생성.

## integration/ 대표 그룹
- **인증·역할 진입**: `registrations`(교사 회원가입+**이메일 필수**+가입 즉시 로그인)·`sessions`(**로그인 표면 2분화** — 안내 인덱스 선택 화면 + 학생 튜플 로그인 + 교직원 이메일 로그인 + 표면 교차 차단 + 스로틀)·`passwords`(본인 비밀번호 변경 — 현재 비번 확인 성공/실패·fail-closed·역할무관)·`dashboard_access`·`dashboard_role`·`board_posts`·`schools_search`·`school_picker`(학교 목록형 검색→선택→학급 캐스케이딩)·`topics`·`forum_post_likes`(게시판 글 좋아요 토글·1인 1좋아요·학급 경계 차단·로그인 게이트)·**`reading_discussion`**(독서 토론 e2e — 토론 글→dex 03 도달성, 기능 플래그 하드 kill 컨트롤러 강제, classroom_id nil 교사의 개설·경계·모더레이션, 금칙어 오탐 회귀·길이·1인1신고·자기글 금지·자동숨김 없음·저자 학급 라우팅, 진입점 노출)·`books_catalog`·`books_autocomplete`(로컬 비-searched 자동완성 스코프·응답 id·**시리즈 접기**[같은 제목·저자 별권을 대표 1행으로 접고 series_count·대표 권차 반환, JSON 계약에 publisher/volume/series_count 추가]·**volumes 엔드포인트**[시리즈 전 권 권차 순 반환·searched 제외·빈 제목 []·로그인 게이트]). 정책 단위 `topic_policy`(학생·**교사 classroom_id nil 담당학급 경계**)와 서비스 단위 `services/moderation/text_denylist`(QUIZ 동작 보존 vs FORUM 오탐 제외)도 참조.
- **독후감 파이프라인**: `report_review_flow`(Phase 3 완료 게이트)·`reports`·`reports_book_link`(자동완성 선택 시 `book_id` 연결·표지 표시·미선택 book_title 폴백)·`ocr`(사진→텍스트, 모드선택 후 redirect·거부 가드)·`report_input_mode`(새 글 모드 선택 chooser/keyboard/photo 3분기 + 위저드 프리필 보존 + 직접쓰기 링크 `guided: 1`)·`report_first_review`(OCR 초안 첫 제출 첨삭 예약·revise 스킵·빈본문 레이스 가드)·`ocr_book_reference`(도서 참조 없으면 draft 미생성·500 아님)·`learn_wizard`(단계 학습 위저드)·**`reports_guided_write`**(직접쓰기 안내 질문 단계 — 밴드별 문항 수 g56 8/g34 7/g12 5, 학년 미상→g12 5, ratio_hint·example 렌더, 조립 본문 create 경로 불변)·**`report_photo_guide`**(사진쓰기 가이드 모달 렌더 + OCR 제출 회귀)·**`reports_index_filters`**(index book_id/reviewed/book_title 필터·policy_scope 위조 차단·이중공백 레거시 정규화 조회·필터+2페이지 이월)·**`reports_self_view`**(내 서재·마이페이지 열람 링크 href + 카드 상세 링크·삭제 버튼 공존 + **책 필터 문맥의 '새 독후감 쓰기' href가 그 책을 `report:{book_id,book_title}`/레거시 `report:{book_title}`로 실어 넘김·무필터는 맨몸**). **시스템 테스트 `test/system/reports_guided_compose`**(직접쓰기 조립 E2E — 질문 입력→"초안 만들기"→본문 조립→제출; chromedriver 부재 시 skip)도 참조.
- **랭킹 시즌제(account_linking_seasons_plan §Phase 1)**: `services/ranking_board`(플래그 on 시즌 정렬[평생 최고참이 시즌 0 이면 비독점·신규 0 출발]·grade_ranking·school/nation 시즌 합·플래그 off 폴백; nation N+1 상수 쿼리 바운드는 플래그 조회 1회 포함해 ≤3)·`integration/rankings`(grade 탭 렌더·시즌 XP 표시 행[`_ranking_row`=브로드캐스트 공유 파셜]).
- **게임화·반려 몬스터**: `games`(독서게임 4종 — 퀴즈 2종 실동작)·`games_catalog`(카탈로그 재구성 — 도서 검색 + 4종 게임 칩 렌더)·**`games_roster`**(게임 재구성 Phase 1·2 — classic·vocab play 라우트/카탈로그 부재, game_type enum 정수 보존[quiz0·classic1·whoami3·book4·sequel5, vocab 제거])·`games_ondemand`(온디맨드 e2e)·**`games_content_gate`**(게임 재구성 Phase 4 §2 가용성 게이트 — 비활성[AI-부적격·콘텐츠 없는] 책은 독서활동에서 퀴즈·나는 누구게? 칩 은닉·책 소개/뒷이야기 칩 유지, 직접 URL 진입 시 오프라인 세트 미생성 리다이렉트[BookSummaryJob 도 미큐잉 — resolve 미도달 상호작용 고정], **다시 뽑기(regenerate) 게이트**[비활성 책 조작 POST → 리다이렉트·Quiz 미생성, 가용 책은 기존대로 새 content_version 회귀 없음], **reading_activities 자가치유**[미확인 책 진입 시 키 있으면 BookSummaryJob 큐잉·무키면 미큐잉·summary 보유/이미 확인된 책은 미큐잉], AI-적격[summary] 책은 칩 표시+오프라인 즉시 서빙 무회귀)·`games_authorization`(경계 클램프 — @book 에 summary 부여로 가용성 게이트 통과 후 밴드 클램프만 검증)·`games_book_intro`(책 소개 대결 — 소셜·1인 1표·크로스학급 차단·Gemini/Quiz 미생성 assert)·**`games_sequel`**(뒷이야기 이어쓰기 — 창작 소셜·game_plays(sequel) 원장·SequelFeedbackJob 큐잉·1인 1공감·크로스학급 차단·**AI 코멘트 작성자만 노출**·distinct_games==4 도달성·라우트)·`game_points_flow`·`rankings`·`starter_selection`·`monster_evolution`·`monster_feed`·`discovery_modal`(발견 연출 드레인 — `pending_celebration` 노출·acknowledge 멱등 마킹·교사 승인→학생 노출)·`mission_participation`·`shop_purchase`·**`monster_unlock_flow`**(독후감 승인·게임 완료 트리거 배선 e2e — 승인 시 해금+flash, `game_plays` 원장 기록[표면·책·일자], game_type allowlist 밖 값 미기록, 동일 퀴즈 재제출 dedup, 게임 완료로 해금 임계 도달 시 발견)·**`reading_activities_nearby`**(인근 도서관 §7 — 활동 카드 아래 Turbo Frame lazy-load 액션: ok 도서관·배지 렌더/none 안내/no_key·error 섹션 숨김/no_location 안내/비학생 리다이렉트/**첫 show 는 외부콜 0**[스텁 호출 카운트]·단수 라우트 헬퍼)·`student_nav_persistence`(학생 상단 navbar가 7개 메뉴 페이지[내 서재·독후감·게임·도감·상점·미션·랭킹] 전체에서 모바일 disclosure/데스크톱 필 목록·동일 경로·활성 탭 강조를 유지하고, 메뉴 페이지의 중복 제목 제거와 독후감 작성 액션·상점 포인트의 navbar 직후 배치를 검증).
- **전국 공유 문제은행 UGC(Phase 3 §4)**: `quiz_contributions`(학생 출제→담임 검토·수정→승인·편입 e2e — 학생 create=pending·서버 확정 학급/밴드·타 유형 무효 거부·교사 create 금지; 담임만 자기 학급 pending 열람·크로스학급 approve 403; 승인=system·global·band 풀 퀴즈+contributed 문항 물질화·resolve 풀 등장·교사 수정 반영, 반려=미편입; **전국성**=타 학교 같은 밴드 학생이 승인 기여를 플레이 풀에서 만남[QuizPolicy within_band]; **총괄 에스컬레이션**=신고 전국 풀 퀴즈 숨김/복원/삭제·비총괄 403).
- **교사(P6)**: `teacher_dashboard`·`teacher_missions`(목표·발행·잠금 + **특정 도서 지정 목표** — 폼 도서 피커 렌더·프리필, create book_id 연결, 빈값=아무책, searched·위조 id 서버검증 nil)·`teacher_quizzes`·`teacher_reviews`·`teacher_students`(**학생 추가·비밀번호 초기화에 교사가 직접 입력한 6자 이상 비밀번호 사용, 빈 값·공백·5자 거부와 실패 시 기존 비밀번호 유지, 타 학급 초기화 403**)·`teacher_rubric_config`·`teacher_exports`(CSV)·`teacher_prints`(인쇄 문서). 미션 진행·보상은 서비스 `missions/progress_calculator`(**특정 도서 목표=그 책 활동만 집계·아무책 목표=전량·batch book_title**)와 통합 `mission_participation`(**지정도서 독후감 승인 때만 완료·보상** e2e)이 커버.
- **사서(P6.5)**: `librarian_dashboard`·`librarian_events`·`librarian_loans`(정보나루 동기화).
- **교무관리자**: `school_admin_neis`(생기부 요약)·`school_admin_stats`(전교 통계·학교 경계).
- **총괄관리자(P7)**: `admin_isolation`(superadmin 전용 격리)·`admin_users`(**사용자 비밀번호를 총괄이 직접 6자 이상으로 초기화, 빈 값·공백·5자 거부와 실패 시 기존 비밀번호 유지**)·`admin_content`·`admin_settings`(API 키 저장 금지 가드)·`admin_moderation`·`admin_analytics`·`admin_monster_species`.
- **인가 안전망·보안**: `authorization_safety_net`(fail-closed, authorize 누락 감지)·`content_security_policy`.
- **Phase 6 하드닝(#2·#4·#7·#9·misc)**: `books_catalog`(카탈로그 페이지네이션 + `searched` 캐시 제외)·`admin_moderation`(3섹션 페이지네이션)·`admin_users`(포인트 조정 award_points 델타 경유 — 랭킹 후크 발화·하향 원자 차감)·`sessions`(계정 단위 스로틀+락아웃, RateLimiter 원자 increment 주입 시임)·`reports`(고쳐쓰기 동일 본문 재첨삭 스킵·본문 수정 시 재예약·작성자 전용 목록 삭제).

## 패턴·규칙
- 실행: `bin/rails test` (전체, 1104 runs). 시스템 테스트는 `bin/rails test:system`(chromedriver 없으면 skip). 단일 파일은 `bin/rails test test/경로/파일_test.rb`.
- 품질 게이트: `bin/ci`가 순서대로 `bin/rubocop`(스타일) → `bin/bundler-audit`·`bin/importmap audit`·`bin/brakeman`(보안) → `bin/rails test`(테스트) → `db:seed:replant`(시드 재적재)를 실행. PR은 이 전 단계가 통과해야 한다.
- 테스트는 병렬(`parallelize`) 실행되므로 전역 상태에 의존하지 말 것.
- 몬스터/뱃지가 필요한 테스트는 `setup`에서 `seed_monster_species!`·`seed_badges!`를 호출(트랜잭션 롤백되며 멱등).

---
> ⚠️ **유지보수 규칙**: 이 폴더의 파일이 추가·삭제되거나 역할이 바뀌면 이 CLAUDE.md도 함께 갱신하세요. 하위 폴더 구조가 바뀌면 관련 상·하위 CLAUDE.md 링크도 확인하세요.
