# test/ — Minitest + Capybara 테스트 스위트 (1938 runs)

'책갈피'(Rails 8.1)의 전체 자동화 테스트 모음이다. 모델·정책·서비스 단위 테스트부터 역할별 화면·플로우를 검증하는 integration 테스트까지 포함한다. 테스트는 **외부 API를 절대 호출하지 않으며**(`test_helper.rb`가 credentials 키를 공란으로 강제 → 도서검색·정보나루·Claude가 오프라인 폴백 경로를 탄다), 원격 성공 경로는 스텁 커넥션을 DI로 주입해 검증한다.

## 하위 카테고리
- `test_helper.rb` — 공통 설정. 외부 키 공란 강제(오프라인 폴백 — **`resend` 포함**이라 테스트는 기본적으로 "메일 발송 불가" 상태이고 이메일 인증 게이트도 꺼져 있다), **`with_mail_delivery_available` 헬퍼**(블록 안에서만 `ENV["RESEND_API_KEY"]` 를 주입해 발송 가능 환경을 흉내낸다 — 게이트가 **작동하는** 경로를 검증하는 테스트만 감싼다. 모듈 함수 스텁 대신 ENV 를 쓰는 이유는 minitest 6 에서 `minitest/mock`(`Object#stub`)이 빠졌고, `ResendGateway.api_key` 가 ENV 우선이라 ENV 주입이 곧 실제 키 소스 규약 검증이 되기 때문. 병렬 테스트는 프로세스 단위로 갈리고 프로세스 안에서는 순차 실행이라 ensure 복원으로 격리가 충분), 병렬 실행, `fixtures :all`, ISBN 필수 Book 픽스처용 유효·유일 `TestBookIsbn` 기본값과 제약 도입 전 중복 재현용 `create_legacy_book!`, `seed_monster_species!`·`seed_badges!`·`login_as(user, onboarded: true)` 및 최소 XLSX 생성기를 포함한다. 학생 `login_as`는 기본적으로 테스트용 유일 닉네임+랭킹 참여 상태를 주입해 기존 플로우가 온보딩 리다이렉트에 가려지지 않게 하고, 설정 게이트 자체를 검증할 때만 `onboarded: false`를 사용한다.
- `models/` — 모델 단위: 뱃지·도서(**`book` — isbn 정규화·유일성 2중방어 + `cover_url` http→https 승격 콜백[혼합 콘텐츠 가드]·https/blank 불변 + `promote_from_search!`[searched→recommended 승격·비-searched no-op]**)·학급·학교·리포트(`report_test` — enum·검증·첨부 매직바이트·방송 rescue에 더해 **`display_photo`/`display_photo?`**[자기 사진·다세대 고쳐쓰기 root 승계·무사진 nil·**depth cap(10) 초과 장쇄 nil**·memoize 후 추가 쿼리 0·`ocr?` AND 사진 게이트]·**`autosave_eligible?`/`first_submission?`/`draft_version`**[사진 첫 제출 화면만 제외·원본을 지운 고쳐쓰기 초안 포함·버전은 저장마다 바뀌고 저장 직후 값 == 다시 읽은 값] 커버, **`report_book_title_normalization` — `before_validation :normalize_book_title` squish·빈값→nil·정상 제목 불변**)·토픽·**`learn_wizard_progress`**(학생당 한 행·raw delete 에도 CASCADE)·유저(**닉네임 정규화/검증·랭킹 설정 기본값·실명 비폴백**)·**`audit_log`(작업 라벨·최근순·append-only)**·몬스터종·유저몬스터, 게임화/퀴즈 데이터 모델(`quiz_question`의 1-based `answer_number` 가상 접근자 포함), **`game_play`**(부분 유니크 인덱스 2종의 일일 dedup — book 있는/없는 플레이 각각; sequel:5 additive enum 포함), **`book_sequel`**(뒷이야기 body 검증[10..2000]·ai_status enum·ranked/for_classroom scope·공감 1인 1표), **`quiz_contribution`**(학생 출제 기여 — 축별 페이로드 검증[mcq 보기4·정답인덱스 범위·프롬프트, hint_reveal 정답+힌트≥2]·status enum[pending/approved/rejected]·금칙어 QUIZ 리스트), 몬스터 시드 무결성, 마이그레이션 FK 존재 검증. **Phase 6 보강(#5)**: `library_loan`·`library_event`·`user_badge`·`forum_post`·`forum_post_like`(좋아요 uniqueness·likes_count counter_cache 증감) 모델 검증, `app_setting`(API 키 저장 차단 보안검증 추가), `monster_seed_integrity`(스타터 3종 정합 + `image_key` WebP 72종의 누락·잔존 파일 검증 + **게임 재구성 Phase 5 골든 불변식**: unlock/evolve 어떤 `distinct_games` 조건도 활성 게임 종류 수 4 이하), **`fk_on_delete_roundtrip`**(reports→books·monster_species 자기참조 on_delete nullify 동작 + SQLite up/down 왕복 무손실; 이 파일만 `use_transactional_tests = false` — DDL 커밋 후 teardown 에서 up 복원·행 정리). **`monster_species`**는 `unlock_condition` JSON 검증(evolve_condition과 별개 컬럼·화이트리스트 공유)도 함께 커버한다. **`backfill_squished_book_titles`**(독후감 열람 백필 마이그레이션 전용 — 콜백 우회로 심은 레거시 이중공백 `book_title`을 `up` 직접 실행으로 squish 정규화·멱등·`down` no-op 검증, quiz_backfill 관례). **`promote_reviewed_searched_books`**(검색 도서 승격 백필 마이그레이션 전용 — 승인 독후감이 붙은 `searched` 도서만 `up` 직접 실행으로 `recommended` 소급 승격·미승인/독후감 없는 searched·비-searched 불변·멱등·`down` no-op 검증). **`hard_delete_vocab_game`**(게임 재구성 Phase 1 — vocab hard-delete 마이그레이션 `up` 이 game_type=2 game_plays[raw SQL 삽입]과 content_axis=1 matching 퀴즈+자식[question/attempt/report]을 삭제하고 mcq 퀴즈·quiz game_play 는 보존, `down`=IrreversibleMigration; 정수 리터럴이라 enum 비의존).
- `models/concerns/` — 공유 모듈: `Badgeable`·`Evolvable`·`Leveling`·`Pointable`(**랭킹 시즌제 훅 포함** — award/credit 시즌 experience_earned 축적·스냅샷, spend 시즌 불변, revoke floor 0 하향, 비학생·학급없음 무축적, 임포트 초기 experience 시즌 미시딩)·`RubricScorable`. `models/season_score_test.rb`(랭킹 시즌제 점수 — 검증·`[academic_year,user_id]` 유니크·음수 거부·학년도별 1행·`for_year` 스코프).
- `controllers/` — 컨트롤러 공용 로직 단위: `axis_averages_parity`(전교 5축 SQL 집계 == 인메모리 집계 parity, #3).
- `tasks/` — rake 시드·운영 태스크: `quizzes_seed`(샘플 퀴즈 멱등 재현)·`schools_seed`(전량 CSV 헤더/필수값/전국 검증, 배치 upsert, 멱등 갱신, 누락 NEIS·구 합성 학교 비활성화, manual 보존, 파일 미존재 no-op)·`books_seed`(`books:enrich` 큐레이션 보강)·`books_seed_full`(TSV 전량 적재 검증)·`books_deduplicate`(기본 dry-run/명시적 apply 중복 병합)·**`books_seed_summaries`**(Claude 줄거리 YAML 시드 — `seed_summaries` 매칭·멱등·미매칭 skip·빈/무 YAML 크래시 0·무네트워크 + `export_summaries` Claude 생성분[checked_at present]만 export·네이버 blurb 제외, `ENV["BOOK_SUMMARIES_YML"]` 임시 파일 주입)·**`books_generate_summaries`**(스텁 ClaudeClient/BookSummaryService 로 무키→skip·known→YAML+DB·모름→checked_at만·YAML 이미 포함분 skip·limit 준수, 실제 Claude 호출 0).
- `policies/` — Pundit 인가. `authorization_matrix_test.rb`가 역할×액션 경계 회귀 매트릭스, 개별 정책(cheer·classroom·report·sticker·topic·**book_sequel**[뒷이야기 학급 경계]·**quiz_contribution**[학생 출제 기여 — 작성=학급 학생 본인만, 교사·비학급·비로그인 차단])이 스코프/권한 세부.
- `services/ai/` — Claude 클라이언트·**Gemini 클라이언트(OCR 전용 — JSON 강제·모델 엔드포인트·타임아웃·재시도 계약)**·OCR·퀴즈 초안·첨삭(review)·규칙기반 첨삭·검증(verify) + **뒷이야기 격려 코멘트(`sequel_feedback_service` — LLM/무키·실패·스키마이탈 폴백·학생 글 body 프롬프트 주입 검증, `rule_based_sequel_feedback` — 항상 긍정·결정적·점수 미부여)** + **줄거리 생성(`book_summary_service` — 무키/모름/저확신(<0.7)/blank/ApiError/스키마이탈→nil, known+고확신→줄거리, 프롬프트에 서지 정보만·기존 summary 미포함 검증, 게임 재구성 Phase 4 §1b)**. 무키 시 규칙기반 폴백 + 스텁 성공 경로.
- `services/books/`·`services/library/` — 도서 검색(네이버) 및 정보나루(data4library) 서비스. `services/books/`는 `isbn`(체크디지트·ISBN-10→13), `search_service`, **`catalog_enricher`**(네이버 ISBN 정확 매칭 표지·출판사 보강 + **네이버 0건 시 정보나루 표지 폴백**·양쪽 무키 no-op), **`deduplicator`**(레거시 중복 dry-run·참조보존 병합), **`genre_inference`** 5파일을 검증한다. `services/library/`의 `data4library_service`는 인기대출(`popular_loans`)과 **표지 상세조회(`cover_url_for`/`srchDtlList` — 빈 표지·미존재·비200·파싱실패 nil, last_error 불오염)** 및 **인근 도서관(`libraries_holding` — `libs[].lib` 정규화·빈결과 []·비200/파싱실패 nil·truncation 경고, `loan_status` — Y/N/에러 status+fetched_at·짧은 read timeout·last_error 불오염)**을 스텁 커넥션으로 검증한다. **`region_codes`**(17교육청 → 정보나루 시도코드 매핑 + 특별자치 별칭·미지값 nil·`for_school` region 우선/address 폴백), **`nearby_availability`**(시도코드 도출·**gu 토큰 완전일치 필터**[서구/달서구·서구/강서구·동구/남동구 제외]·세종 gu nil 유지·N컷 ≤5·**렌더 경로(call)의 외부 HTTP 0**[캐시 미스 시 워밍 잡 큐잉 후 :warming 즉시 반환·한 반이 동시에 열어도 스탬피드 마커로 잡 1건·캐시가 다 차면 외부콜 0 으로 :ok·소장목록만 캐시되고 대출여부가 비면 반쯤 채운 화면 대신 :warming]·**병렬 팬아웃(warm!)**[5곳 0.3초 지연이 1초 안에 완료 = 순차 1.5초를 넘기지 않음·느린 1곳만 :unknown 강등되고 이웃은 실제 상태 유지·예산 0 이면 원격 미호출·캐시 히트는 예산과 무관]·각 state 분기·**대출여부 TTL 2단**[일시 `:unknown` 은 2분만 기억 — 직후 재워밍은 캐시를 읽어 무한 루프를 막고, TTL 이 지나면(`travel`) 다시 시도해 회복을 막지 않는다. 확정값은 그 시점에도 캐시에 남아 두 TTL 이 실제로 다름을 고정]·`cache:` MemoryStore 주입 holdings 캐시 히트 외부콜 0·**as_of=가장 오래된 fetched_at**[15분 캐시→"15분 기준"])도 함께 검증한다.
- `services/schools/` — NEIS 학교기본정보 연동: `gu_parser`(도농복합시·단층제·시도 토큰 생략)·`neis_fetcher`(전체 건수 페이지네이션, 후속 페이지 실패/HTTP 200 API 오류/국내 필터)·`snapshot_validator`(필수값·중복·전국 최소 건수·17개 교육청).
- `services/recommendations/` — 실제 번들 XLSX의 어린이 분과 203권 파싱 계약, ISBN upsert, 청소년 제외, digest 멱등, 활성 목록 교체, 실패 롤백 + **`BookSummaryJob` 신규 유입 예약**(게임 재구성 Phase 4 code-review 후속 — summary blank·checked_at nil 인 신규 도서만 예약, 이미 summary/checked_at 있는 도서는 스킵, `BookEnrichmentJob` 장르 보강과 동형)을 검증한다.
- `services/exports/` — **`xlsx_writer`(최소 XLSX 생성기 — 필수 파트 6종 존재, 머리글·값 왕복, 빈 칸 미생성, `=`/`+`/`-`/`@` 로 시작하는 값도 문자열 셀[수식 주입 방어], XML 특수·제어문자 처리, 숫자/문자열 셀 분기, 시트명 금지문자·31자, 머리글 굵게+틀 고정, 행 0개)**. `read_xlsx_sheet`/`read_xlsx_entry`/`xlsx_entry_names`(test/xlsx_test_helper.rb)로 되읽어 검증한다.
- `services/teacher/` — 교사 화면 집계: **`student_stats_query`**(학생별 통계 — 원본/고쳐쓰기/승인/검토대기 분리 카운트, **5축 SQL 집계가 `Report#final_rubric_scores`(축별 교사 조정 우선)와 일치**, 미승인·무루브릭 글 제외, 게임/퀴즈/미션/챌린지/토론 집계, 최근 활동일=독후감·게임 중 늦은 쪽(독후감 쪽은 **제출한 글의 마지막 submitted_at** — 5일 전에 쓰기 시작해 2일 전에 낸 글=2일 전, 미제출 초안만 있는 학생은 최근 활동 없음·active? false), **학생 수가 늘어도 쿼리 수 불변**[GROUP BY 상수 쿼리]·빈 명단은 쿼리 0).
- `services/games/`·기타 서비스 — 퀴즈 플레이 멱등 적립(포인트 파밍 차단), 몬스터 획득, 랭킹 집계, 독서 통계(`reading_stats` — `max_daily_reports`·`game_plays`/`distinct_games`/`game_books` 신규 지표 + **게임 재구성 Phase 5 몬스터 도달성**: 활성 4종[quiz·whoami·book·sequel] 플레이→`distinct_games==4`·dex7/21/23 도달, book+sequel 바닥[2종]→dex23만 도달·dex7/21 미도달[game_plays 충분·distinct_games 게이트] 포함 + **제출 시각 규칙**[streak_days 는 제출한 글의 제출일만 — 미제출 초안 제외·쓰기 시작한 날이 아니라 낸 날 / max_daily_reports 는 제출일로 묶고 같은 책은 먼저 낸 한 편만]). **student_library_query**(내 서재 최근 활동 — 낸 글=제출 시각[KST 날짜, SQLite MAX 집계의 UTC 문자열 캐스팅]·초안=처음 저장 시각·:in_progress, 책 순서=마지막 제출 순, 레거시 제목 그룹 동일) · **student_home_query** 인기 도서(최근 30일=제출 시각 — 40일 전에 쓰기 시작해 5일 전에 낸 글 포함, 35일 전에 낸 글 제외). **`content_provider`는 Phase 3 문제은행 세트 단위 랜덤 출제(sampler seam 으로 결정적 검증 — 풀 여러 세트면 랜덤 후보 중 하나, 1세트면 그거, MISS면 오프라인 플로어) + 기여 세트가 같은 전국 풀 후보가 됨(물질화)을 커버**하고, 기존 회귀(HIT·워밍·재시도·신고·재생성)는 결정적 sampler 로 안정 유지. **게임 재구성 Phase 4**: `game_content_available?`(고전·summary 책·기여 있는 책→available, offline 만 있는 무명 책→unavailable) + 줄거리 미확인 **classic**(가용·실제 도달 경로) 책 resolve 시 `BookSummaryJob` 함께 큐잉(summary 있음·checked_at 있음·무키면 미큐잉)도 커버한다. **`monster_unlock`**(fixpoint 캐스케이드 — dex_count 20 도달 시 같은 `evaluate!` 호출에서 dex 24 동시 해금, AND 다중조건, 멱등 재실행, 라인 지급 실패 rescue).
- `jobs/` — 백그라운드 잡: `AiReviewJob`(AI 첨삭)·`OcrJob`(사진 OCR — 판독 결과·실패 안내는 **그 글의 채널로만** 방송[학생의 다른 초안·사용자 단위 채널로는 가지 않음, 리뷰 #9])·**`SequelFeedbackJob`(뒷이야기 격려 AI 코멘트 — 무키 폴백 done+ai_comment 확보·네트워크 0·LLM 스텁 경로·방송·삭제 no-op·실패 전이)**·**`BookSummaryJob`(Claude 줄거리 생성 — 무키 no-op[checked_at 미세팅]·키+known=summary+checked_at·키+모름=checked_at만·이미 summary/checked_at 멱등 skip·삭제 no-op, 게임 재구성 Phase 4 §1c)**·`BookEnrichmentJob`(무API genre 보강 — 공란만 채움·멱등)·**`Library::NearbyLibrariesWarmJob`(인근 도서관 워밍 — 잡이 캐시를 채워 이후 렌더가 외부콜 0 으로 :ok, 완성본 파셜을 `target: "nearby_libraries"` 로 replace 방송[프레임 제자리 교체], 방송 실패가 채운 캐시를 되돌리지 않음, 삭제된 book/school no-op, 무키는 원격 0 이되 대기 문구에서는 벗어남. **잡이 안 돌면 학생은 영영 "찾고 있어요"만 본다** — 캐시 채움과 방송 둘 다 고정한다)**·`RecommendationCoverEnrichmentJob`(추천도서 업로드 뒤 표지 공란 범위만 네이버 비동기 보강).
- `mailers/` — **`account_mailer_test`(인증 메일 봉투·본문 계약)**. 발신(`admin@chaekgalpi.net`)·수신·제목, **HTML·텍스트 두 파트가 모두 존재하고 각각 토큰 링크를 포함**(한쪽만 렌더되면 실패), 만료 문구가 `User::*_EXPIRY` 상수에서 파생된 값과 일치(문서-코드 드리프트 차단), 링크 호스트가 환경 `default_url_options` 를 따름, **평문 비밀번호·다이제스트·salt 미포함**(PII 회귀 가드 — 어서션이 URL 경로의 `password_resets` 에 걸리지 않도록 실제 비밀값으로 검사한다).
- `helpers/` — 뷰 헬퍼(`TeacherHelper`, `MonstersHelper`의 WebP 렌더·이모지 폴백).
- `integration/` — 역할별 화면·플로우 E2E(대표 그룹은 아래).
- `system/` — Capybara+Selenium 브라우저 시스템 테스트(`application_system_test_case.rb` = `driven_by :selenium, using: :headless_chrome`, **`CHROME_BIN` 환경 변수가 있으면 그 크롬 실행 파일을 쓴다** — 크롬이 기본 경로에 없는 개발기에서 Selenium Manager 가 받아 둔 Chrome for Testing[`~/.cache/selenium/chrome/linux64/<버전>/chrome`]을 가리키면 된다). 현재 `reports_guided_compose`(직접쓰기 안내 질문 조립 E2E)·`report_review_gate`(제출→"선생님 확인 중"→교사 승인→첨삭 노출, 한 브라우저 세션에서 학생↔교사 전환)·**`report_autosave`(자동 저장 E2E 33종 — **skip 은 크롬을 쓸 수 없을 때만**(`skip_without_chrome` — 예상 밖의 확인창 `UnexpectedAlertOpenError` 도 WebDriverError 라, 그대로 건너뛰면 "확인창이 한 번 더 뜬다" 같은 회귀가 실패가 아니라 skip 으로 가려진다. 5차 리뷰 후속 변이 실험에서 세 가지가 실제로 가려졌다) ① 새 글: 저장 버튼 없이 입력→"저장했어요"→주소가 편집 화면으로 바뀜→새로고침해도 본문 유지→제출하면 **한 편만**[첫 저장이 만든 초안을 PATCH 로 냄] ② 고쳐쓰기: 자동 저장 뒤 새로고침해도 '수정하기'가 열려 있고 누르면 제출됨 ③ 질문형 작성: 답을 쓰는 중에 저장·질문 영역에 상태 표시·초안 만들기→제출이 한 편 ④ 질문형: 첫 저장 전에 "질문 없이 바로 쓰기"를 눌러도 쓴 답이 그 자리 폼에 남고 한 편 ⑤ **첫 저장(create) 응답이 늦는 사이 제출** — 버튼 잠금·"저장하는 중이에요" 뒤 그 초안을 PATCH 로 한 편만 ⑥ 오래된 탭 — 화면을 연 뒤 DB 를 더 고치면 자동 저장이 멈추고 DB 본문 불변·dirty 유지 ⑦ errors 없는 422(보안 토큰 바뀜) — '책 제목 확인' 오안내 없이 멈춤·dirty 유지. ⑧·⑨ **keepalive 한도(리뷰 #12)** — 한글 약 2만 자 글은 창을 닫는 순간(`beforeunload` 흉내) 붙잡고 keepalive 없이 보내 저장이 끝남, 보통 길이는 keepalive 로 조용히 저장하고 붙잡지 않음. ㉓~㉝ **5차 리뷰 후속** — '이미 제출했어요' 화면에서 책 제목에 Enter 를 쳐도 폼 제출 요청 0·승인 유지(F1), 입력 오류 상태·422 로 다시 그린 화면·다른 기기의 글로 거절될 저장이 날아가는 중에 링크를 누르면 거절된 뒤 묻고 머물 수 있음(F2·F2b·R2 — 나가기를 고르면 한 번만 묻고 떠남), 링크로 떠나면 저장이 끝난 뒤 이동하고 끊긴 저장은 보통 요청으로 다시 보내 저장 뒤 이동([false, false]), 마지막 문장을 쓰고 곧바로 로그아웃해도 저장(L1 — 로그아웃 요청을 멈췄다가 저장 뒤 보냄), 저장할 수 없는 글이면 로그아웃 전에 묻고 머물면 로그아웃하지 않음, 이미 제출한 글로 멈추면 주소를 그 글 화면으로 바꾸고 '낸 글 보러 가기' 링크(LOW-b), **뒤로 가기(Turbo 복원 방문)로 떠난 새 글 화면에 늦게 온 201·409 응답이 지금 주소를 바꾸지 않음**(`visit_via_turbo` 로 들어가 `page.go_back` — 되돌려도 통과하던 ADOPTLOC·MUT-G), 입력 오류 뒤 고쳐 저장되면 떠날 때 붙잡지 않음(MUT-AJ), 제출이 연결 문제로 실패하면 자동 저장이 다시 돎(SUBMITEND — Turbo 의 폼 제출 fetch 만 실패시킨다). 떠난 뒤 재전송(keepalive 한 번)·재시도 타이머 금지 두 테스트는 **뒤로 가기**로 떠난다(링크 이동은 이제 저장을 기다린다). ⑳~㉒ **4차 리뷰 후속** — 자동 저장·임시 저장이 입력 횟수를 요청 순번으로 실음(요청 FormData 의 `autosave_seq` 기록 + 임시 저장 뒤 DB 순번), 첫 저장 응답을 잃은 사이 다른 곳에서 더 쓴 초안은 재시도가 덮지 않고 주소를 그 초안으로 바꿈, 입력 오류로 다시 그린 화면은 떠날 때 붙잡음(이 테스트가 `connect` 초기화 순서 결함을 실제로 잡았다). ⑰~⑲ **3차 리뷰 후속** — 떠난 뒤에는 실패해도 재시도 타이머를 걸지 않음(긴 타이머를 직접 기록한다 — 요청 수만 세면 실패마다 벌어지는 간격 탓에 수십 초를 기다려야 한다), 실패 뒤 더 입력해도 재시도 간격을 지키고 재시도가 최신 글 저장, 제출이 나가는 중에 쓴 글도 저장 안 된 글로 셈. 떠난 뒤 재전송은 keepalive 한 번([false, true]), 충돌 화면은 떠날 때 붙잡음. ⑩~⑯ **2차 리뷰 후속 브라우저 계약** — 자동 저장 뒤 처음 새 글 주소로 다시 오면 초안이 열림(`autosave_origin` 이름·값), 첫 저장 중 책 제목을 고치면 늦은 응답이 옛 책을 되살리지 않음, 요청에 시간 제한 신호(AbortSignal), 응답만 잃은 저장의 재시도가 거짓 충돌 없이 저장, 첫 저장 응답을 잃어도 초안 한 편, 저장 중 떠나고 그 저장이 끊겨도 떠난 뒤 한 번 더 보내 저장, 충돌 안내 뒤 임시 저장은 쓴 글을 보여 주며 한 번 더 물음. ④~⑦은 `window.fetch` 대역으로 **자동 저장(Accept JSON) 응답만** 늦추거나 흉내 낸다 — 요청을 늦추면 제출이 먼저 끝나 위험을 못 본다(음성 대조로 확인). 컨트롤러 상태는 `window.Stimulus.getControllerForElementAndIdentifier` 로 읽는다)** 3파일 25종. **chromedriver 미가용 환경에서는 각 테스트가 스스로 skip**(무JS·무드라이버 CI 안전) — `bin/rails test`(비-system)와 분리되어 `bin/rails test:system`으로 실행.
  - **브라우저 로그인 3계약**(integration 의 `login_as` 가 대신해 주던 일을 여기선 직접 지켜야 한다): ① 학생은 `nickname`·`ranking_opted_in`을 **미리 채워** 만든다 — 비우면 `require_student_ranking_profile` 게이트가 로그인 직후 모든 화면을 `ranking_preferences#edit`로 되돌린다. ② `click_button "로그인"`(로그아웃도 동일) 뒤에는 **도착지를 `assert_current_path`로 기다린다** — Turbo 폼 전송은 즉시 반환하므로 바로 `visit` 하면 세션 쿠키가 심기기 전에 다음 요청이 나가 로그인 인덱스로 튕긴다(뒤늦게 쿠키가 붙어 화면은 로그인한 것처럼 보여 진단이 어렵다). ③ 교사 화면의 로그아웃 버튼은 **대시보드에만** 있으므로(학생 헤더와 달리 전 화면 공통이 아님) 로그아웃 전 `visit root_path`. 잡 실행도 마찬가지로 `perform_enqueued_jobs do click ... end` 는 무효다 — 클릭은 즉시 반환하고 잡은 서버 스레드가 나중에 큐잉하므로, 리다이렉트 도착을 기다린 뒤 블록 없는 `perform_enqueued_jobs`로 큐를 비운다.
- `fixtures/files/` — 테스트 자산. `handwriting.png`(OCR 입력 이미지)·`schools_sample.csv`(NEIS 전량 시드 파싱용 샘플). YAML 픽스처는 사용하지 않고, 데이터는 각 테스트의 `setup`/시더로 생성.

## integration/ 대표 그룹

- **`password_reset_test` / `email_verification_test`(계정 인증, resend)** — 세 계약을 지킨다. ① **학생 제외 fail-closed**(이메일이 있는 학생 주소로 요청해도 발송 0). ② **계정 열거 방지** — 존재하는 교직원·미존재 주소·학생·정지 계정·레이트리밋 초과가 **모두 같은 `sent` 화면**을 받는지 각각 검증. ③ **1회용 성질** — 재설정 성공 후 같은 토큰 재사용이 거부되고, 발급 후 정지된 계정의 토큰도 사용 시점에 막힌다. 인증 쪽은 **가입 즉시 로그인 흐름 보존 회귀**(인증 도입이 기존 UX 를 바꾸지 않음)와 게이트 4상태(유예 내 허용 / 유예 후 차단 / 무키 허용 / 인증 후 허용 / 읽기는 항상 허용)를 커버한다. **차단 테스트는 `flash[:alert]` 문구까지 단언**한다 — `assert_no_difference` 만 두면 파라미터 오류로 생성이 실패해도 통과하는 거짓 음성이 생기기 때문(실제로 초기 구현에서 이 구멍이 드러나 보강).
- **인증·역할 진입**: `registrations`(교사 회원가입+**이메일 필수**+가입 즉시 로그인)·`sessions`(**로그인 표면 2분화** — 안내 인덱스 선택 화면 + 학생 튜플 로그인 + 교직원 이메일 로그인 + 표면 교차 차단 + 스로틀 + **체험 계정 원클릭**[계정 부재 시 섹션 은닉·존재 시 공개 버튼 2종(학생·담임) 렌더·교무/사서 버튼 미노출 및 마크업에 비밀번호 미노출, 일부 공개 역할 부재 시 그 버튼만 은닉, student/teacher/school_admin/librarian role 직접 로그인 호환, 알 수 없는·누락 role·계정 부재 시 무세션 alert, 정지 계정 403] + **출처 표기 회귀**[로그인 3화면 모두 `shared/_credits_footer` 를 렌더하고 폰트 OFL·네이버 오픈API·정보나루·국립어린이청소년도서관·나이스·Claude·Gemini 항목이 빠지지 않는지 — 표기 누락은 화면에서 티가 나지 않아 테스트로 고정])·`passwords`(본인 비밀번호 변경 — 현재 비번 확인 성공/실패·fail-closed·역할무관)·`dashboard_access`·`dashboard_role`·`board_posts`·`schools_search`·`school_picker`(학교 목록형 검색→선택→학급 캐스케이딩)·`topics`(없는 범위 값은 500 없이 목록으로·토론방 미생성)·`forum_post_likes`(게시판 글 좋아요 토글·1인 1좋아요·학급 경계 차단·로그인 게이트)·**`reading_discussion`**(독서 토론 e2e — 토론 글→dex 03 도달성, 기능 플래그 하드 kill 컨트롤러 강제, classroom_id nil 교사의 개설·경계·모더레이션, 금칙어 오탐 회귀·길이·1인1신고·자기글 금지·자동숨김 없음·저자 학급 라우팅, 진입점 노출)·`books_catalog`·`books_autocomplete`(로컬 비-searched 자동완성 스코프·응답 id·**시리즈 접기**[같은 제목·저자 별권을 대표 1행으로 접고 series_count·대표 권차 반환, JSON 계약에 publisher/volume/series_count 추가]·**volumes 엔드포인트**[시리즈 전 권 권차 순 반환·searched 제외·빈 제목 []·로그인 게이트]). 정책 단위 `topic_policy`(학생·**교사 classroom_id nil 담당학급 경계**)와 서비스 단위 `services/moderation/text_denylist`(QUIZ 동작 보존 vs FORUM 오탐 제외)도 참조.
- **독후감 파이프라인**: `report_review_flow`(Phase 3 완료 게이트)·`reports`·`reports_book_link`(자동완성 선택 시 `book_id` 연결·표지 표시·미선택 book_title 폴백)·`ocr`(사진→텍스트, 모드선택 후 redirect·거부 가드)·`report_input_mode`(새 글 모드 선택 chooser/keyboard/photo 3분기 + 위저드 프리필 보존 + 직접쓰기 링크 `guided: 1` + **없는 입력 방식 값**[`bogus` — new 는 500 없이 첫 단계·create·update·자동 저장은 422 에 글 불변·AI 첨삭 미예약])·`report_first_review`(OCR 초안 첫 제출 첨삭 예약·revise 스킵·빈본문 레이스 가드)·**`report_draft_submission`(미제출 초안 경계 `submitted_at` 회귀 — 판독 끝난 OCR 초안이 교사 검토 큐·카운트·대시보드 통계에서 빠짐, URL 직접 요청·`batch_approve` 승인 fail-closed[초안 승인 = rubric 없는 독후감 확정], 학생 화면 '작성 중' 배지+'아직 제출하지 않았어요' 배너+이어서 쓰기 CTA, compose 화면의 제출 안내, **제출하면 실제로 5축 rubric·등급·avg 가 저장되고 교사 큐에 등장**, 재제출이 최초 제출 시각을 덮어쓰지 않음, 고쳐쓰기 초안도 편집 전에는 큐 미노출)**·**`report_autosave`(자동 저장 서버 계약 — JSON 첫 저장=201·초안 생성·update_url/edit_url/book_id 반환·AI 첨삭 미예약, 다음 저장=같은 초안 본문만 갱신, 빈 본문 422, **제출된 글은 자동 저장이 못 덮음**[JSON 409·HTML 409 — 쓴 글을 보여 주는 화면], 담임의 초안 저장 403, 자동 저장된 새 초안 제출 시 "제출했어요" 안내, **고쳐쓰기: 자동 저장으로 본문이 이미 저장돼 있어도 '수정하기'로 재제출**[요청에 본문 변경 없음]·원본과 같은 본문(CRLF·앞뒤 공백 차이)은 재첨삭 안 함, 편집 화면의 컨트롤러·상태 문구·스냅샷 금지 meta·원본 본문 baseline, 사진 첫 제출은 경고 전용, 질문형 작성의 sync·수신 액션 배선 + **2026-09-13 리뷰 후속**: 다른 탭·기기가 더 고친 초안은 옛 `draft_version` 의 자동 저장이 못 덮음[409 stale, 최신 버전이면 저장·새 버전 반환], 저장 응답 버전 == 다시 연 편집 화면 버전[마이크로초 왕복], 버전 없는 요청은 받음, **원본을 지운 고쳐쓰기 초안**이 '제출하기'로 제출·첨삭 예약, 새 글 주소 기억[같은 `/reports/new?…` 로 다시 오면 초안 편집 화면으로 redirect·다른 책은 새 글·제출 뒤·TTL 경과·새 글 주소 아닌 origin 은 새 글], 첫 저장 뒤 원격 검색 책도 자동 저장·제출(update)이 등록, 담임이 연 학생 초안 화면엔 자동 저장·임시 저장·화면 표 없음(버전 칸은 있음 — 담임 저장도 학생의 더 새 글을 덮지 않게), **확인과 갱신 사이의 틈(리뷰 #11)** — 파일 머리의 테스트 전용 시임 `ReportLockRaceHook`(`Report#lock!` 직전에 한 번 다른 요청이 먼저 끝난 것처럼 행을 바꾼다)으로 자동 저장이 읽은 뒤 잠그기 전에 제출이 끝났으면 409 already_submitted·본문 불변, 두 저장이 겹치면 늦게 잠근 쪽 409 stale, 제출도 같은 잠금을 잡고 제출 본문으로 제출, **2차 리뷰(e411848) 후속** — 본문이 실린 4KB 넘는 새 글 주소도 첫 저장 201·지문으로 기억·재방문 시 초안으로(쿠키 넘침 회귀), 같은 화면이 응답만 잃고 다시 보낸 저장은 받고 다른 화면이면 409, 다른 곳에서 더 고친 초안에 임시 저장·제출(HTML)은 409 충돌 화면[쓴 글 유지·최신 글 보기 링크·자동 저장 꺼짐·최신 버전 실음]·한 번 더 누르면 저장·제출, 같은 화면 표로 다시 온 첫 저장·제출은 초안을 새로 만들지 않음, 다른 글이 쓰는 표가 와도 500 없이 표만 비움, 초안의 원격 책은 searched 로 남고 제출하면 recommended, **3차 리뷰(b67c678) 후속** — 초안일 때 연 화면의 제출이 그사이 다른 곳에서 낸 글·승인된 글을 바꾸지 않음[409·쓴 글 유지·제출 버튼 없음·떠날 때 붙잡음·AI 첨삭 미예약], 같은 표로 늦게 온 새 글 제출이 이미 낸 글을 다시 내지 않음, 같은 화면이라도 앞선 순번의 늦은 저장은 409·같은 순번 재전송과 뒤 순번은 저장, 담임이 고친 초안은 학생 옛 탭이 못 덮음[마지막으로 쓴 화면 비움], 첫 저장 응답을 잃은 사이 다른 탭이 저장해도 재시도가 초안을 새로 안 만듦[만든 화면으로 찾음], 같은 표의 동시 첫 저장도 한 편 — 파일 머리의 두 번째 테스트 전용 시임 `ReportInsertRaceHook`(새 글 저장 직전에 같은 화면의 다른 요청이 만든 초안[표·순번]을 끼워 넣는다)으로 유일 위반 경로를 따로 지킨다. 표 형식은 8~64자라 테스트 값도 그 길이로 쓴다. **4차 리뷰(bce37c8) 후속** — 첫 저장 응답을 잃은 사이 다른 탭이 저장하면 첫 화면의 재시도는 초안을 새로 만들지도 덮지도 않음[409 stale·edit_url], 같은 화면의 동시 첫 저장이라도 앞선 순번이 뒤 순번을 되돌리지 않음, 첫 저장 응답을 잃은 새 글 화면의 제출(create)은 그사이 쓴 글을 바로 덮지 않음[409 충돌 화면], 담임이 고친 초안을 학생 첫 화면의 재시도가 못 덮음, 사진 첫 제출 화면에 `opened_as_draft` 표시(버전 칸은 없음)·그 화면의 제출이 그사이 낸·승인된 글을 바꾸지 않음, 입력 오류 422 화면은 unsaved, 담임 충돌 안내는 학생 기준 문구가 아님. **5차 리뷰(ee11907) 후속** — 이미 제출 화면이 보내는 칸을 그대로 다시 보내도(Enter 암묵 제출 흉내 — 응답 HTML 의 폼 칸을 Nokogiri 로 읽는 `form_fields`) 409·본문·승인·교사 코멘트 유지[학생·사진 첫 제출·담임 세 변형], 빈 본문 임시 저장의 422 화면이 보낸 제목을 보여 줌(저장은 안 함), 이미 제출한 글에 온 자동 저장 409 JSON 에 `report_url`)**·`ocr_book_reference`(도서 참조 없으면 draft 미생성·500 아님, 원격 검색 책은 사진 초안 동안 searched·제출하면 recommended)·**`report_photo_display`**(학생 상세 OCR 512 사진 + 확대 링크 렌더, 비-OCR·무사진 미렌더, 다세대 고쳐쓰기 root 사진, 타 학생 403)·**`teacher_review_photo`**(교사 검토 화면 사진 렌더·비-OCR 미렌더·타 학급 담임 403 회귀)·**`report_photo_serving`**(아동 PII 바이트 인가 매트릭스 — 본인·담임·동일학교 사서/교무 200, 타 학생·타 학급 담임·타 학교 403, 비로그인 리다이렉트; 512/1600 유계 variant 요청과 `RecordNotFound` 404·무사진 404·조건부 GET 304. **`ActiveStorage::Attachment#variant` 시임**으로 libvips 유무와 무관하게 결정적 검증 — 요청된 `resize_to_limit` 값을 포착하고, `StandardError`·**`LoadError`**(libvips 부재) 양쪽에서 원본 폴백 200 을, 비인가 요청에서는 variant 가 실패할 상황에서도 **403 유지 + variant 호출 0**[폴백이 인가를 우회하지 않음]을 단언)·`learn_wizard`(단계 학습 위저드 — 단계 진행·이탈 뒤 복원·**로그아웃 뒤 재로그인해도 이어짐**[진행은 `LearnWizardProgress` 행]·**마치면 미제출 초안 생성 + 그 편집 화면으로**[redirect 주소에 본문 없음·keyboard·학급·1단계 첫 줄=책 제목·AI 첨삭 미예약·진행 초기화]·**단계마다 한글 약 800자여도 500 없음**[쿠키 넘침 회귀]·1단계 첫 줄이 비면 1단계로·답 보존·챌린지 참여 표로 초안에 `challenge_id`·**학생 전용**[담임 index·advance 403, 진행 행 없음]·**학급 없는 학생**은 한국어 안내(영어 검증 문구 아님)·답 보존·**마치기 두 번째 요청**은 목록으로 "이미 마쳤어요"·빈 진행 행 미생성 + **학년군별 성취기준**[2학년=[2국…]·3학년=[4국…]·학급 없음=g12, 1~4학년 화면에 [6국 코드 없음, 단계마다 세 학년군 코드가 모두 그 학년군 첨삭 목록 안에 있음])·**`reports_guided_write`**(직접쓰기 안내 질문 단계 — 밴드별 문항 수 g56 8/g34 7/g12 5, 학년 미상→g12 5, ratio_hint·example 렌더, 조립 본문 create 경로 불변)·**`report_photo_guide`**(사진쓰기 가이드 모달 렌더 + OCR 제출 회귀)·**`reports_index_filters`**(index book_id/reviewed/book_title 필터·policy_scope 위조 차단·이중공백 레거시 정규화 조회·필터+2페이지 이월)·**`reports_self_view`**(내 서재·마이페이지 열람 링크 href + 카드 상세 링크·삭제 버튼 공존 + **책 필터 문맥의 '새 독후감 쓰기' href가 그 책을 `report:{book_id,book_title}`/레거시 `report:{book_title}`로 실어 넘김·무필터는 맨몸**). **시스템 테스트 `test/system/reports_guided_compose`**(직접쓰기 조립 E2E — 질문 입력→"초안 만들기"→본문 조립→제출; chromedriver 부재 시 skip)도 참조.
- **랭킹·자기성장(P1-5/8 + 시즌제)**: `services/ranking_board`(챌린지 순위는 제출한 참여 독후감만 — 자동 저장 초안 제외; 모든 학생 class/grade/school/nation/challenge/hall 집계·비공개 학생의 닉네임/몬스터 마스킹·플래그 on 시즌 정렬[평생 최고참이 시즌 0 이면 비독점·신규 0 출발]·grade_ranking·school/nation 시즌 합·플래그 off 폴백; nation N+1 상수 쿼리 바운드는 플래그 조회 1회 포함해 ≤3)·`integration/rankings`(닉네임 행·비공개 물음표 행·grade 탭·시즌 XP 표시)·`integration/ranking_preferences`(NULL 닉네임 강제 이동·명시 선택·비공개 설정·실명 미노출)·`integration/growths`(**제출 순 정렬** — 먼저 쓰기 시작해 나중에 낸 글이 '최근 글'·성장 배너·날짜도 낸 날; 승인된 본인 루브릭만 시간순, 교사 조정 우선·빈 상태 + **시각화 회귀 가드**: 방사형 차트 SVG 렌더·글이 2편 이상이면 지난 글 점선 폴리곤 1개·1편이면 0개, 축 막대 5 + 시간 변화 막대 N 개수).
- **게임화·반려 몬스터**: `games`(독서게임 4종 — 퀴즈 2종 실동작)·`games_catalog`(카탈로그 재구성 — 도서 검색 + 4종 게임 칩 렌더)·**`games_roster`**(게임 재구성 Phase 1·2 — classic·vocab play 라우트/카탈로그 부재, game_type enum 정수 보존[quiz0·classic1·whoami3·book4·sequel5, vocab 제거])·`games_ondemand`(온디맨드 e2e)·**`game_content_broadcast`**(짝 없는 방송 회수 — `GenerateGameContentJob#broadcast_ready` 는 오랫동안 **구독자도 대상 요소도 없는 스트림**에 쏘고 있었다[크래시도 로그도 없어 눈으로는 못 잡는 결함]. "구독이 있다"로 끝내지 않고 **잡이 실제로 쏘는 것과 화면이 실제로 받는 것을 맞댄다** — 서명된 스트림 이름 동일성 + 방송 payload 의 `target` id 를 렌더된 화면에서 직접 찾기 + 교사 발행 퀴즈에는 구독 부재[음성 케이스])·**`games_content_gate`**(게임 재구성 Phase 4 §2 가용성 게이트 — 비활성[AI-부적격·콘텐츠 없는] 책은 독서활동에서 퀴즈·나는 누구게? 칩 은닉·책 소개/뒷이야기 칩 유지, 직접 URL 진입 시 오프라인 세트 미생성 리다이렉트[BookSummaryJob 도 미큐잉 — resolve 미도달 상호작용 고정], **다시 뽑기(regenerate) 게이트**[비활성 책 조작 POST → 리다이렉트·Quiz 미생성, 가용 책은 기존대로 새 content_version 회귀 없음], **reading_activities 자가치유**[미확인 책 진입 시 키 있으면 BookSummaryJob 큐잉·무키면 미큐잉·summary 보유/이미 확인된 책은 미큐잉], AI-적격[summary] 책은 칩 표시+오프라인 즉시 서빙 무회귀)·`games_authorization`(경계 클램프 — @book 에 summary 부여로 가용성 게이트 통과 후 밴드 클램프만 검증)·`games_book_intro`(책 소개 대결 — 소셜·1인 1표·크로스학급 차단·Claude/Quiz 미생성 assert)·**`games_sequel`**(뒷이야기 이어쓰기 — 창작 소셜·game_plays(sequel) 원장·SequelFeedbackJob 큐잉·1인 1공감·크로스학급 차단·**AI 코멘트 작성자만 노출**·distinct_games==4 도달성·라우트)·`game_points_flow`·`rankings`·`starter_selection`·`monster_evolution`·`monster_feed`·`discovery_modal`(발견 연출 드레인 — `pending_celebration` 노출·acknowledge 멱등 마킹·교사 승인→학생 노출)·`mission_participation`·`shop_purchase`·**`monster_unlock_flow`**(독후감 승인·게임 완료 트리거 배선 e2e — 승인 시 해금+flash, `game_plays` 원장 기록[표면·책·일자], game_type allowlist 밖 값 미기록, 동일 퀴즈 재제출 dedup, 게임 완료로 해금 임계 도달 시 발견)·**`reading_activities_nearby`**(인근 도서관 §7 — 활동 카드 아래 Turbo Frame lazy-load 액션: **캐시 미스=워밍 잡 큐잉+"찾고 있어요"+구독 1개·정보나루 미호출**, 워밍 후 ok 도서관·배지 렌더/none 안내/no_key·error 섹션 숨김[**실패도 무한 대기가 아니라 섹션 숨김으로 끝난다** — 실패를 짧게 기억하지 않으면 학생이 대기 문구에 갇힌다]/no_location 안내/비학생 리다이렉트/**첫 show 는 외부콜 0**[스텁 호출 카운트]·단수 라우트 헬퍼. ⚠️ 렌더가 캐시만 읽으므로 상태 분기를 보려면 `with_warmed_cache` 로 **`Rails.cache` 를 MemoryStore 로 바꿔야** 한다 — test 기본은 `:null_store` 라 워밍이 아무것도 남기지 못한다)·`student_nav_persistence`(학생 상단 navbar가 7개 메뉴 페이지[내 서재·독후감·게임·도감·상점·미션·랭킹] 전체에서 모바일 disclosure/데스크톱 필 목록·동일 경로·활성 탭 강조를 유지하고, 메뉴 페이지의 중복 제목 제거와 독후감 작성 액션·상점 포인트의 navbar 직후 배치를 검증. **`MENU_LABELS`·`menu_paths` 가 `_student_nav` 의 6메뉴 단일 진실과 순서·개수까지 맞물려** 메뉴가 늘거나 순서가 바뀌면 실패한다).
- **사용방법 안내(학생 도움말)**: `guide`(상단 메뉴 "사용방법" 노출·`guide_path` 링크, 안내 화면의 진입점 링크[독서활동·내 서재·도감·성장·마이페이지] 렌더, **수치·명칭이 도메인 상수에서 파생**됨[5축 라벨+학년군 눈높이 설명·등급 포인트·퀴즈 정답당 포인트·게임 4종 이름 — 상수를 고치면 안내도 따라 바뀌는지의 드리프트 가드], **학생 대면 문구가 "AI 선생님"이 아니라 "선생님"**, 비학생 루트 리다이렉트+메뉴 미노출).
- **전국 공유 문제은행 UGC(Phase 3 §4)**: `quiz_contributions`(학생 출제→담임 검토·수정→승인·편입 e2e — 학생 create=pending·서버 확정 학급/밴드·타 유형 무효 거부·교사 create 금지; 담임만 자기 학급 pending 열람·크로스학급 approve 403; 승인=system·global·band 풀 퀴즈+contributed 문항 물질화·resolve 풀 등장·교사 수정 반영, 반려=미편입; **전국성**=타 학교 같은 밴드 학생이 승인 기여를 플레이 풀에서 만남[QuizPolicy within_band]; **총괄 에스컬레이션**=신고 전국 풀 퀴즈 숨김/복원/삭제·비총괄 403).
- **교사(P6)**: `teacher_dashboard`·`teacher_missions`(목표·발행·잠금 + **특정 도서 지정 목표** — 폼 도서 피커 렌더·프리필, create book_id 연결, 빈값=아무책, searched·위조 id 서버검증 nil)·`teacher_quizzes`·**`teacher_reviews`(검토 목록 — 상태 필터[기본 미검토·`status=reviewed|all`·위조값 pending 폴백]·검토완료 행의 승인 컨트롤 부재·필터 링크 3종·`batch_approve`가 이미 승인된 글의 `reviewed_at`을 갱신하지 않음·페이지네이션 필터 이월·검토완료 `show`의 승인 버튼 부재 포함)**·`teacher_students`(**학생 추가·비밀번호 초기화에 교사가 직접 입력한 6자 이상 비밀번호 사용, 빈 값·공백·5자 거부와 실패 시 기존 비밀번호 유지, 타 학급 초기화 403**)·`teacher_rubric_config`·**`teacher_exports`(원자료 XLSX — 워크북 MIME·`.xlsx` 파일명·ZIP 매직바이트, 머리글/사전·사후 값 왕복, **점수=숫자 셀·이름/제목=문자열 셀**, **수식처럼 생긴 제목도 문자열 셀이고 `<f>` 0개**[수식 주입 표면 제거], 쉼표·따옴표 제목 보존, 학생 403)**·**`teacher_prints`(인쇄 문서 4종 + `index` 진입 화면 — 문서 4종 링크 전량·교사 네비 진입점·print 레이아웃 미적용·무학급 교사 빈 상태[403 아님]·타 학급 id 403·학생 403·웹 `target=_blank` vs 앱 target 부재 + `#raw-export` 섹션[학급 유무 무관 노출·앱 다운로드 훅용 마크업·**버튼 아이콘의 `icon-inverse`**·**AI 예시가 내려받기 버튼 옆 배경색 토글 버튼 + `disclosure` 패널**(details 부재)·네비에 별도 내려받기 항목 부재])**·**`teacher_student_stats`(학생별 통계 — 담임 학급 학생만 표에 노출[타 학급 미노출], `?sort=` 정렬과 위조 정렬 값의 이름순 폴백[표 tbody 행 순서로 단언], 타 학급 `classroom_id`·타 학급 학생 show 403, 학생 역할 진입 403, 상세의 독후감·게임 종류·미션·챌린지 렌더와 무활동 학생 빈 상태, 대시보드·학생관리 화면의 진입 링크)**. 미션 진행·보상은 서비스 `missions/progress_calculator`(**특정 도서 목표=그 책 활동만 집계·아무책 목표=전량·batch book_title** + **기간=제출 시각**[시작 전에 쓰기 시작해 기간 안에 낸 글 인정, 기간 안에 시작해 종료 뒤 낸 글 제외, 단건·batch 동일])와 통합 `mission_participation`(**지정도서 독후감 승인 때만 완료·보상** e2e + **시작 전에 자동 저장한 초안을 기간 안에 제출·승인하면 완료·보상**[승인 트리거의 후보 날짜도 제출 시각])이 커버.
- **챌린지**: `challenges`(조회·참여·관리 CRUD·학교 경계·소개글 + **목록 카드**[stretched-link, 참여 시 `ChallengeParticipation`+`joined_at` 기록, **미참여 카드는 진행률 막대·수치 없이 참여 안내만**])·`challenge_goals`(목표·진행·보상 e2e — **참여 후 활동만 집계**[참여 전 승인 독후감·플레이한 게임은 완료·보상 불가, 참여 후 활동은 정상 집계, **참여 전에 자동 저장으로 쓰기 시작해 참여 후에 제출·승인한 글은 집계** — 집계 기준=제출 시각, 참여 전에 '낸' 글만 제외]·미참여 학생은 진행/보상 대상 아님·재참여해도 `joined_at` 불변·정확히-1회 보상 멱등·지정도서 목표·스코프 경계[타 학교 join 403]·전국 챌린지·상세 화면 진행률/완료 배지).
- **사서(P6.5)**: `librarian_dashboard`·`librarian_events`·`librarian_loans`(정보나루 동기화).
- **교무관리자**: `school_admin_neis`(생기부 요약)·`school_admin_stats`(전교 통계·학교 경계).
- **총괄관리자(P7)**: `admin_isolation`(superadmin 전용 격리)·`admin_users`(**사용자 비밀번호를 총괄이 직접 6자 이상으로 초기화, 빈 값·공백·5자 거부와 실패 시 기존 비밀번호 유지**)·`admin_content`(없는 도서 분류 값 422·도서 불변)·`admin_settings`(API 키 저장 금지 가드)·`admin_moderation`·`admin_analytics`·`admin_monster_species`·**`audit_logs`(총괄 전용 격리·필터·삭제/포인트/비밀번호/CSV 성공 기록)**.
- **인가 안전망·보안**: **`authorization_safety_net`**(fail-closed — ① `ApplicationController` 의 `verify_authorized` after_action 존재 ② **`app/controllers` 파일 목록을 직접 훑어** ApplicationController 를 상속하지 않는 컨트롤러가 허용목록(`NativeConfigurationsController`)과 정확히 일치하는지 [인가 체인 밖 컨트롤러가 새로 생겨도 화면도 테스트도 아무 말을 하지 않는다] ③ 허용목록 컨트롤러가 쿠키·CSRF·flash·뷰 없는 `ActionController::API` 인지 ④ **소스에 session·cookies·Current·current_user 가 없는지** — `session` 은 `ActionController::Metal` 이 위임해 API 컨트롤러에서도 **읽히므로** 구조가 아니라 소스로 막는다)·`content_security_policy`.
- **Phase 6 하드닝(#2·#4·#7·#9·misc)**: `books_catalog`(카탈로그 페이지네이션 + `searched` 캐시 제외)·`admin_moderation`(3섹션 페이지네이션)·`admin_users`(포인트 조정 award_points 델타 경유 — 랭킹 후크 발화·하향 원자 차감)·`sessions`(계정 단위 스로틀+락아웃, RateLimiter 원자 increment 주입 시임)·`reports`(고쳐쓰기 동일 본문 재첨삭 스킵·본문 수정 시 재예약·작성자 전용 목록 삭제).

## 패턴·규칙
- 실행: `bin/rails test` (전체, 1938 runs). 시스템 테스트는 `bin/rails test:system`(chromedriver 없으면 skip). 단일 파일은 `bin/rails test test/경로/파일_test.rb`.
- 품질 게이트: `bin/ci`가 순서대로 `bin/rubocop`(스타일) → `bin/bundler-audit`·`bin/importmap audit`·`bin/brakeman`(보안) → `bin/rails test`(테스트) → `db:seed:replant`(시드 재적재)를 실행. PR은 이 전 단계가 통과해야 한다.
- 테스트는 병렬(`parallelize`) 실행되므로 전역 상태에 의존하지 말 것.
- 몬스터/뱃지가 필요한 테스트는 `setup`에서 `seed_monster_species!`·`seed_badges!`를 호출(트랜잭션 롤백되며 멱등).

---
> ⚠️ **유지보수 규칙**: 이 폴더의 파일이 추가·삭제되거나 역할이 바뀌면 이 CLAUDE.md도 함께 갱신하세요. 하위 폴더 구조가 바뀌면 관련 상·하위 CLAUDE.md 링크도 확인하세요.
