# Android 빌드 툴체인 고정(계획 P4 재현 가능한 빌드).
# 이 디렉터리에서 mise 가 활성화되면 아래 버전이 선택된다.
#   AGP 8.13.2 ↔ Gradle 8.13+ ↔ Kotlin 2.3.0(= dev.hotwire:core 1.3.1 의 stdlib) 조합이다.
[tools]
java = "temurin-17.0.20+8"
gradle = "8.14.5"
