# 게임 보상·AI 첨삭 오류 4건 수정 계획

- 작성일: 2026-09-20
- 상태: **구현·검증 완료(2026-09-20) · 운영 배포 전** — 구현 결과와 남은 일은 [§10](#10-구현-결과2026-09-20)
- 검토: 2026-09-20 현재 코드와 대조해 원인(1.1)과 누락 항목(실패 복구·시드/테스트 영향·배포 구성)을 보강
- 대상: Rails 서버, 공용 웹 화면, 관련 데이터와 회귀 테스트
- 목표: 같은 활동의 중복 보상을 막고, 최신 제출물에 대한 첨삭만 저장·승인·공개한다.

## 1. 확인한 오류와 수정 우선순위

아래 결과는 테스트 전용 DB에서 재현했다. F2는 외부 AI 서비스를 대역으로 바꿔 작업 완료 순서를 제어했다. 운영 데이터의 실제 피해 규모를 측정한 결과는 아니다.

| ID | 우선순위 | 오류 | 확인한 결과 | 핵심 수정 위치 |
| --- | --- | --- | --- | --- |
| F1 | P1 | 같은 게임 결과의 중복 보상 | 같은 `attempt_id`를 두 번 제출하자 기록은 1개인데 포인트가 5→10으로 증가 | [QuizPlay](app/services/games/quiz_play.rb), [PointAward](app/services/games/point_award.rb) |
| F2 | P1 | 이전 AI 작업이 최신 첨삭을 덮어씀 | 새 본문에 이전 첨삭이 저장됨. 해당 재현에서 글의 지급 기록은 10점, 사용자 적립은 40점 | [AiReviewJob](app/jobs/ai_review_job.rb), [ReportsController](app/controllers/reports_controller.rb) |
| F3 | P1 | AI 처리 중 승인으로 미검토 첨삭 공개 | 첨삭 생성 전에 승인되고, 생성 후 추가 승인 없이 학생에게 공개 | [ReportPolicy](app/policies/report_policy.rb), [교사 검토 컨트롤러](app/controllers/teacher/reviews_controller.rb) |
| F4 | P2 | 요청값으로 게임 완료 종류 조작 | 객관식 퀴즈에 `game` 값만 바꿔 5종 완료 기록 생성. 책 소개·뒷이야기 글은 0개 | [AttemptsController](app/controllers/games/attempts_controller.rb), [GamePlay](app/models/game_play.rb) |

구현은 **재현 테스트 정착 → F1·F4 → F2·F3 → 통합 검증** 순서로 진행한다. F1·F4는 같은 게임 제출 경로를 공유하고, F2·F3는 같은 제출 버전과 상태 전이 규칙이 필요하므로 각각 묶어서 완성한다.

### 1.1 코드에서 확인한 원인

- **F1** — [QuizPlay#persist_attempt](app/services/games/quiz_play.rb#L66)가 이미 확정된(`played_at`이 있는) 선생성 attempt도 다시 `update!`한다. [PointAward#prior_max](app/services/games/point_award.rb#L27)는 `excluding:`으로 바로 그 행을 상한 계산에서 빼므로 상한이 0으로 돌아가 같은 보상을 전액 다시 지급한다.
- **F2** — [AiReviewJob#perform](app/jobs/ai_review_job.rb#L9)에 제출 버전이 없어 늦게 끝난 작업이 결과를 덮어쓴다. 차액의 기준인 `points_awarded`도 작업이 시작할 때 읽어 둔 값이라, 겹쳐 실행된 작업이 저마다 전액을 적립하고 글에는 마지막 값만 남는다.
- **F3** — [ReportPolicy#approve?](app/policies/report_policy.rb#L76)는 `review? && submitted?`만 보고 `ai_status`를 보지 않는다. [Report#feedback_visible?](app/models/report.rb#L94)는 `reviewed? && rubric.present?`여서, 먼저 승인된 글은 첨삭이 저장되는 순간 그대로 공개된다.
- **F4** — [AttemptsController#create](app/controllers/games/attempts_controller.rb#L22)가 `params[:game]`을 그대로 `record_game_play!`에 넘기고, 이 메서드는 `GamePlay.game_types` 허용 목록만 확인한다. 그래서 객관식 제출이 `book`·`sequel`·`classic`·`whoami` 완료로도 기록된다.

## 2. 수정 후 지켜야 할 규칙

1. 완료된 게임 attempt는 답안·점수·힌트 사용량·완료 시각이 다시 바뀌지 않는다. 같은 요청을 다시 받아도 보상과 완료 기록은 추가되지 않는다.
2. 새 게임 플레이에서 기록을 개선하면 기존 최고점 대비 차액만 지급한다. **추가 포인트가 0인 정상 새 플레이**도 활동 완료로 기록할 수 있다.
3. 게임 종류는 서버가 검증한 퀴즈 유형에서 정한다. 책 소개·뒷이야기 완료는 해당 글을 저장하는 전용 경로에서만 발생한다.
4. AI 결과는 작업이 시작한 제출 버전과 현재 제출 버전이 같을 때만 반영한다. 같은 버전의 결과와 포인트는 한 번만 확정한다.
5. 교사는 자신이 화면에서 확인한 버전의 완성된 첨삭만 승인한다. 재제출하면 이전 승인은 해제된다.
6. 결과 기록·포인트·경험치·시즌 점수는 같은 트랜잭션에서 반영한다. 방송 실패가 이미 확정한 결과를 실패 상태로 바꾸거나 보상을 다시 지급하게 해서는 안 된다.

현재 [Pointable](app/models/concerns/pointable.rb)의 `credit_points!`, `revoke_points!`, `run_point_side_effects!`를 우선 재사용한다. `award_points`는 DB 변경과 방송·후크를 함께 실행하므로 트랜잭션 안에서 그대로 호출하지 않는다.

SQLite는 `FOR UPDATE`를 무시하므로 `lock!`·`with_lock`으로 행 잠금이 생기지 않는다. 이 앱에서 쓰기가 직렬화되는 근거는 Rails SQLite 어댑터가 트랜잭션을 `BEGIN IMMEDIATE`로 시작해(Rails 8.1 기본값, `config/database.yml`에 재정의 없음) DB 단위 쓰기 잠금을 먼저 잡는다는 점이다. [ReportsController#update](app/controllers/reports_controller.rb#L118)의 `with_lock`도 이 직렬화와 잠금 안의 재조회에 기대고 있다. 새 코드도 같은 전제를 따른다.

- 판단에 쓰는 값(최고 보상액, 현재 버전, 승인 상태)은 트랜잭션 **안에서** 다시 읽는다.
- 처리 권한은 조건부 UPDATE의 영향 행 수로 확인한다. [미션 Rewarder](app/services/missions/rewarder.rb)의 조건부 선점·적립 패턴을 참고한다.
- 쓰기 잠금이 DB 전체에 걸리므로 트랜잭션은 짧게 유지하고, 외부 호출·방송을 안에 넣지 않는다.
- 별도 DB 연결을 사용하는 동시성 테스트로 보장한다.

## 3. F1·F4 — 게임 제출과 보상 처리

### 3.1 서버에서 게임 종류 확정

퀴즈의 완료 종류를 결정하는 함수를 한 곳에 두고, 컨트롤러와 서비스가 같은 결과를 사용한다.

| 검증한 퀴즈 유형 | 서버가 정하는 완료 종류 | 처리 조건 |
| --- | --- | --- |
| `content_axis=mcq` | `quiz` | 현행 객관식 퀴즈 경로 |
| `content_axis=hint_reveal` | `whoami` | 본인·같은 퀴즈의 선생성 attempt 필수 |
| `origin=teacher`, `content_axis=nil` | `quiz` | 교사 퀴즈는 축을 저장하지 않는다(현행 전체가 nil). 문항이 객관식인지 확인 |
| `matching`, 알 수 없는 유형, 유형이 서로 모순되는 문항 구성 | 없음 | 저장·채점·보상 전에 거부 |

- `params[:game]`은 완료 원장과 이동 경로를 결정하는 근거에서 제거한다. 예전 폼이 값을 보내더라도 서버가 정한 종류를 사용한다.
- `attempt_id`가 명시됐는데 본인·같은 퀴즈의 기록을 찾지 못하면 새 attempt로 대체하지 않고 거부한다.
- `book`, `sequel`, `classic`은 퀴즈 제출 경로에서 새로 기록할 수 없다. 기존 `classic` 기록과 enum 정수값은 보존한다.
- [GamePlay](app/models/game_play.rb)에 있는 “클라이언트 선언을 허용하는 의도적 편차” 주석은 새 규칙으로 갱신한다. 현재 `quiz`·`whoami`는 콘텐츠 유형으로 구분 가능하다.

### 3.2 attempt 확정과 적립을 원자적으로 처리

`QuizPlay#record!`가 채점 결과, 보상 차액, 새로 만들어진 `GamePlay`를 함께 반환하도록 정리한다. 컨트롤러는 이 결과로 안내·리다이렉트·후속 평가를 수행한다.

처리 순서는 다음과 같다.

1. 기존 정책으로 사용자·퀴즈 접근 권한을 확인하고 서버 게임 종류를 결정한다.
2. 짧은 DB 트랜잭션 안에서 최신 attempt와 이전 최고 보상액을 읽는다. 같은 사용자의 같은 보상 범위에 속하는 서로 다른 attempt도 경쟁을 제어해야 한다.
3. 선생성 attempt의 `played_at`이 이미 있으면 저장된 결과와 `awarded_delta=0`, `newly_finalized=false`를 반환한다. 답안이 바뀐 재전송도 기존 기록을 덮어쓰지 않는다.
4. 미완료 attempt는 `WHERE played_at IS NULL` 조건으로 확정한다. 기존 attempt가 없는 객관식 플레이는 새 행을 생성한다.
5. 같은 트랜잭션에서 `max(이번 보상액 - 이전 최고 보상액, 0)`만 `credit_points!`로 적립한다. attempt의 `points_awarded`에는 현행 규약대로 **이번 점수에 대응하는 보상액**을 저장한다.
6. 실제 새로 확정한 플레이만 서버 게임 종류로 `GamePlay`를 기록한다. 확정과 **같은 트랜잭션**에서 기록한다 — 따로 커밋하면 그 사이에 중단됐을 때 확정은 됐는데 완료 원장이 빠지고, 이후 재전송은 `newly_finalized=false`라 복구되지 않는다. 기존 일일 유니크 제약을 유지하며, 같은 날의 기존 완료 행과 충돌해도 attempt 확정·적립을 롤백하지 않는다(충돌 무시 삽입 또는 savepoint).
7. 커밋 후 포인트 후크·방송을 실행한다. 미션·챌린지·몬스터 해금 평가는 실제 새 `GamePlay`가 생긴 경우에 실행한다.

상한 비교는 현재의 두 규칙을 유지한다: 교사 퀴즈는 퀴즈별, 시스템 퀴즈는 `(book, band, content_axis)`별 최고 보상이다. **새 attempt 두 개를 동시에 저장한 뒤 서로를 제외해 조회하는 방식**은 양쪽 모두 미지급될 수 있으므로, 상한 조회부터 확정·적립까지 같은 직렬화 범위 안에 둔다.

SQLite 잠금 경합 재시도는 롤백된 DB 트랜잭션 전체에 한정하고 횟수를 제한한다. 지급 후 방송 실패를 지급 트랜잭션 재시도로 연결하지 않는다.

### 3.3 힌트·중복 요청·화면 동작

- [WhoamiController](app/controllers/games/whoami_controller.rb)의 힌트 공개도 미완료 attempt에만 허용한다. 제출과 힌트 공개가 겹쳐도 확정 점수와 힌트 기록이 어긋나지 않도록 같은 완료 상태 조건을 사용한다.
- 중복 제출에는 “이미 제출한 결과예요. 추가 포인트는 없어요.”라고 안내하고 저장된 결과를 사용한다.
- 중복 요청이 날짜를 넘어 도착해도 다음 날의 새 게임 완료로 기록하지 않는다.
- 새 플레이의 차액이 0인 경우와 같은 attempt의 재전송을 구분한다. 단순히 `awarded_delta > 0`으로 활동 완료를 판단하지 않는다.
- 객관식은 선생성 attempt가 없어 재전송을 같은 요청으로 식별할 수 없다. 객관식의 연타·재전송은 새 플레이로 처리하되 보상은 차액 상한으로, 완료 원장은 일일 유니크로 제한한다. 자정을 넘긴 객관식 재전송은 정상 새 플레이와 구분되지 않으며, 이 한계는 이번 계획에서 수용한다(요청 식별 토큰은 도입하지 않는다).

**주요 변경 파일:** `app/services/games/{quiz_play,point_award}.rb`, `app/controllers/games/{attempts,whoami,base}_controller.rb`, 필요 시 `app/models/{quiz,quiz_attempt,game_play}.rb`. 별도의 지급 테이블 신설은 현재 계획에 포함하지 않는다.

## 4. F2 — AI 첨삭에 제출 버전 연결

### 4.1 데이터 모델

`reports`에 다음 두 컬럼을 추가하는 안을 기준으로 구현한다.

| 컬럼 | 형식 | 의미 |
| --- | --- | --- |
| `review_version` | integer, NOT NULL, 기본값 0 | 현재 첨삭 요청의 버전. 제출·재제출 시 1 증가 |
| `completed_review_version` | integer, NULL 허용 | 결과 저장과 포인트 반영을 완료한 버전 |

- `updated_at`은 승인·자동 저장 등에도 바뀌므로 첨삭 버전으로 사용하지 않는다.
- `submitted_at`은 최초 제출 시각이라는 기존 의미를 유지한다.
- 아직 제출하지 않은 초안은 버전 0이다. OCR 처리 완료나 고쳐쓰기 초안에 복사된 루브릭만으로 첨삭 완료 버전을 부여하지 않는다.
- 새 제출에는 `reviewed=false`, `reviewed_at=nil`, 교사 편집본 초기화와 공유 해제를 기존 규칙대로 적용한다. 완료 버전은 새 결과를 적용하기 전까지 현재 버전과 일치하지 않아야 한다.

### 4.2 제출 경로 통합

`record_submission!`을 중심으로 새 글 제출·초안 첫 제출·OCR 후 제출·고쳐쓰기·재제출이 모두 같은 버전 발급을 거치도록 한다.

1. 본문 저장, 제출 버전 증가, 검토 상태 초기화를 한 트랜잭션에 넣는다. 현재 분리되어 있는 새 글 생성 경로도 이 규칙에 맞춘다.
2. 그 트랜잭션에서 확정한 버전을 지역 변수/결과 객체로 반환한다.
3. 커밋 후 `AiReviewJob.perform_later(report, expected_review_version: version)`으로 예약한다. 예약 직전에 다시 읽은 버전으로 대체하지 않는다.
4. 실제 AI 입력인 본문·도서 연결/제목을 바꾸는 저장 경로를 점검한다. 제출된 글의 입력을 바꾸면서 이전 버전·승인을 유지하는 경로가 남지 않게 한다. 자동 저장만으로 AI 작업을 예약해서는 안 된다. 현재 확인된 경로는 두 가지이며, 구현 전에 각각을 "새 버전 발급"과 "제출된 글에서는 해당 입력 변경 거부" 중 하나로 정한다.
   - 작성자가 제출된 글에서 본문은 그대로 두고 책·제목만 바꾸는 저장(`update`의 `:saved` 분기). → **결정: 새 버전 발급**(재첨삭·이전 승인 해제). 학생 본인의 제출이 바뀐 것이고, 다른 책에 대한 첨삭이 남지 않게 한다.
   - 담임이 학생의 제출된 글 본문을 고치는 저장. `ReportPolicy#update?`는 허용하지만 `resubmit?`는 작성자 본인일 때만 참이라, 본문이 바뀌어도 기존 첨삭과 승인이 그대로 남는다. → **결정: 변경 거부**(422). 담임의 저장은 재제출이 아니며, 담임은 검토 화면에서 첨삭 내용을 고친다. 제출된 글을 담임이 편집 화면으로 여는 링크는 앱에 없다(주소 직접 입력만). 초안은 버전·승인이 없어 예전처럼 담임도 고칠 수 있다.
5. primary DB와 queue DB는 분리되어 있으므로 제출 커밋과 큐 적재를 하나의 원자적 작업으로 간주하지 않는다. 큐 적재 실패를 기록하고, 누락된 현재 버전의 `pending` 작업을 재예약할 수 있는 복구 절차를 마련한다.

### 4.3 AI 작업을 세 구간으로 분리

**① 시작 확인 — 짧은 DB 트랜잭션**

- 현재 버전이 인자의 버전과 같고, 실제 제출된 글이며, 해당 버전이 아직 완료되지 않았는지 확인한다.
- 이전 버전·삭제된 글·완료된 같은 버전의 작업은 결과를 바꾸지 않고 종료한다.
- 해당 버전의 본문·도서 정보·학년군·채점 가중치를 스냅샷으로 확보하고 `processing` 상태를 기록한다.

**② 첨삭 생성 — DB 트랜잭션 밖**

- 스냅샷을 대상으로 외부 AI 또는 기존 규칙 기반 폴백을 실행한다.
- 외부 응답을 기다리는 동안 DB 쓰기 잠금을 유지하지 않는다.
- 동의 확인과 외부 전송 최소화 규칙을 유지한다.

**③ 결과 확정 — 짧은 DB 트랜잭션**

- 다시 읽은 현재 버전과 작업 버전을 비교하고, 같은 버전이 이미 완료됐다면 종료한다.
- 조건부 UPDATE로 현재 버전에 대한 결과 확정 권한을 확인한다. 중복 작업이 같은 결과를 두 번 적립할 수 없어야 한다. 조건은 아래와 같고, 영향 행이 1일 때만 이후 단계를 진행한다.

  ```text
  WHERE id = :id
    AND review_version = :작업_버전
    AND (completed_review_version IS NULL OR completed_review_version <> :작업_버전)
  ```

- 루브릭·평균·등급·개선도, `ai_status=done`, `completed_review_version`을 저장한다. 채점에는 ①에서 확보한 가중치를 쓴다(현재 `apply_rubric!`는 호출 시점의 학급 가중치를 다시 읽는다).
- 이때 DB에서 읽은 최신 `points_awarded`를 기준으로 차액을 계산하고, 지급 기록·포인트·경험치·시즌 점수를 함께 갱신한다. 양수는 `credit_points!`, 음수는 `revoke_points!`를 사용한다.
- DB 처리 중 예외가 나면 결과와 지급을 함께 롤백한다. 커밋 후 후크·방송을 실행하며, 음수 정정 시 기존의 역진화 금지 규칙을 유지한다.

같은 버전의 중복 작업은 최종 확정 지점에서 한 번만 반영한다. 워커가 `processing` 상태에서 중단되어도 재시도가 영구 차단되지 않게 한다. 외부 호출 중복 최소화보다 **결과와 보상의 한 번 반영**을 먼저 보장한다.

### 4.4 실패·방송 처리

- 이전 버전 작업의 실패는 최신 버전을 `failed`로 바꾸지 않는다.
- 같은 버전의 다른 작업이 이미 성공했다면 뒤늦은 실패도 무시한다.
- 현재 버전이 `failed`로 끝난 글의 복구 경로를 둔다. 지금 학생 화면은 "잠시 후 다시 시도해 주세요"라고 안내하지만, 제출된 글은 본문을 바꿔야만 재첨삭이 예약된다. 여기에 F3 수정으로 담임 승인까지 막히면 글이 그 상태로 멈춘다. 같은 버전으로 다시 예약하는 절차를 4.2의 복구 절차와 함께 마련하고, 이때 버전은 올리지 않는다(③의 조건이 중복 확정을 막는다).
- 채점·DB 저장 예외와 커밋 이후 후크·방송 예외를 별도로 처리한다. 방송 예외로 `done`을 `failed`로 바꾸지 않는다.
- 방송 시 최신 DB 상태를 다시 확인하고, 이미 승인·재제출된 글에 오래된 검토 행을 덧붙이지 않는다. 상세 파셜의 승인·버전 검증도 함께 적용한다.
- 로그에는 글 ID·작업 버전·현재 버전·처리 결과를 남긴다. 학생 본문과 AI 응답 전문은 남기지 않는다.

**주요 변경 파일:** `db/migrate/`의 신규 마이그레이션, `db/schema.rb`, `app/models/report.rb`, `app/controllers/reports_controller.rb`, `app/jobs/ai_review_job.rb`. 필요하면 제출/결과 확정 로직을 서비스로 추출하되 책임은 위 경계를 유지한다.

## 5. F3 — 완성된 같은 버전만 승인·공개

### 5.1 공통 상태 판정

`Report#review_ready?`를 추가하고 다음 조건을 모두 만족할 때 참으로 한다.

```text
submitted?
AND ai_status == done
AND rubric.present?
AND review_version > 0
AND completed_review_version == review_version
```

- `ReportPolicy#approve?`는 기존 교사·학급 권한에 `review_ready?`를 결합한다.
- 학생 공개 조건인 `feedback_visible?`는 `reviewed? && review_ready?`로 강화한다.
- 우수작 공유의 신규 허용 조건도 현재 버전의 유효한 승인을 확인한다. 기존 공유를 취소하는 경로는 유지한다.
- `pending`, `processing`, `failed`, 초안은 승인할 수 없다. 현재 규칙 기반 폴백도 정상 완료 시 `done`이므로 계속 승인 가능하다.
- 승인된 글을 **직접 만드는 코드**도 새 조건을 만족해야 한다. 버전 컬럼 기본값(0·NULL)으로는 `review_ready?`가 거짓이라 첨삭이 숨는다.
  - [DemoSeeder](db/seeds/demo_seeder.rb)는 `ai_status: :done`·`reviewed: true`인 글을 직접 만들고, [PublicClassroomRefresh](app/services/demo_data/public_classroom_refresh.rb)는 `feedback_visible?`로 재적재 결과를 검증한다. 시드의 제출된 글에는 `review_version=1`, 첨삭이 있는 글에는 `completed_review_version=1`을 함께 기록한다(7.1의 백필과 같은 규칙). 빠뜨리면 공개 체험 학급의 첨삭이 모두 숨고 재적재 검증이 실패한다.
  - `reviewed: true`인 글을 직접 만드는 테스트가 42개 파일에 있다. 공용 생성 도우미를 두어 버전 컬럼을 함께 채우고, 개별 테스트가 컬럼 기본값에 기대지 않게 한다.

### 5.2 교사가 확인한 버전 전달

- 상세 화면의 승인·교사 첨삭 저장 폼에 확인한 `review_version`을 싣는다.
- 일괄 승인 폼에는 선택한 report ID별 버전을 함께 보낸다. ID 목록만으로 현재 버전을 대신 승인하지 않는다.
- 버전 누락·불일치는 최신 화면 확인 안내로 처리한다. 누락값을 서버의 현재 버전으로 채우지 않는다.
- 승인 전 상태 검증은 트랜잭션 안에서 다시 수행한다. 조건부 UPDATE로 버전·미승인 상태를 확인하고 성공한 요청만 후속 처리를 실행한다.
- 같은 버전의 재승인은 성공한 기존 결과로 안내하되 승인 시각·보상·방송을 다시 발생시키지 않는다.
- 교사가 열어 둔 편집 화면도 학생 재제출 이후에는 이전 교사 코멘트를 최신 글에 덮어쓰지 못하도록 같은 버전 검사를 거친다.

### 5.3 화면과 일괄 승인

| 상태 | 교사 화면 | 학생 화면 |
| --- | --- | --- |
| AI 대기·처리 중 | 상태 안내, 승인 버튼·선택 체크박스 비활성 | 첨삭 준비/처리 안내 |
| AI 실패 | 실패 안내, 승인 불가(복구는 4.4의 같은 버전 재예약) | 실패 안내, 미검토 결과 비공개 |
| 현재 버전 첨삭 완료·미승인 | 검토·승인 가능 | 선생님 확인 중 |
| 현재 버전 승인 완료 | 승인 내용 확인·정정 가능 | 승인된 첨삭 공개 |
| 화면을 연 뒤 학생이 재제출 | 이전 폼 저장·승인 거부, 다시 확인 안내 | 새 첨삭 확인 중 |

일괄 승인은 각 행을 검증하고 승인 건수와 제외 건수를 안내한다. 준비 중·버전 변경·이미 승인된 글을 모두 승인했다고 표시하지 않는다. 권한 밖 ID에 대해서는 기존 정책대로 정보를 노출하지 않는다.

승인 시 미션·챌린지·몬스터 후크는 실제 상태 전이가 성공한 경우에 실행한다. 커밋 이후 실패한 후크는 재평가할 수 있어야 하며, 이를 위해 승인을 되돌리거나 포인트를 재지급하지 않는다.

**주요 변경 파일:** `app/models/report.rb`, `app/policies/report_policy.rb`, `app/controllers/teacher/reviews_controller.rb`, `app/views/teacher/reviews/{index,show,_report_row}.html.erb`, `db/seeds/demo_seeder.rb`, 테스트 공용 도우미(`test/test_helper.rb`), 필요 시 학생 상세·공유 관련 파셜.

## 6. 회귀 테스트 계획

최초 점검의 임시 재현 테스트 4건을 해당 저장소 테스트 파일에 옮겨 먼저 실패를 확인한다. `/tmp` 파일을 이후 검증의 필수 의존성으로 남기지 않는다.

| 대상 | 반드시 검증할 경우 | 기대 결과 |
| --- | --- | --- |
| F1 | 같은 whoami attempt 동일 답안·다른 답안 재전송 | 기록 불변, 추가 포인트·경험치·시즌 점수 0 |
| F1 | 동일 attempt 동시 제출 | 확정·보상 한 번 |
| F1 | 같은 보상 범위의 다른 attempt 동시 제출 | 최고점에 해당하는 총 보상, 중복 지급·미지급 없음 |
| F1 | 첫 정답, 더 높은 점수, 낮은 점수, 새 판 재플레이 | 기존 차액 규칙 유지 |
| F1 | 다음 날 같은 attempt 재전송 | 새 완료 기록·미션 진행 없음 |
| F1 | 힌트 공개와 제출 경합, 완료 뒤 힌트 공개 | 확정 점수·힌트 기록 일치 |
| F1 | 확정 트랜잭션 안에서 완료 원장 기록 중 예외, 같은 날 완료 행과 유니크 충돌 | 전자는 확정·적립·원장이 함께 롤백되고 재전송으로 정상 확정, 후자는 확정·적립 유지 |
| F4 | `game=book/sequel/classic/whoami`로 객관식 제출, `game` 생략 | 실제 퀴즈 종류로만 기록·이동 |
| F4 | 잘못된 attempt ID, 다른 학생·다른 퀴즈, 지원하지 않는 유형 | 채점·기록·보상 부작용 없음 |
| F4 | 정상 책 소개·뒷이야기 제출, 과거 classic 기록 조회 | 기존 전용 경로·과거 기록 보존 |
| F2 | 이전 작업 시작 → 재제출 → 새 작업 완료 → 이전 작업 완료 | 최신 첨삭만 유지, 정확한 보상 차액 |
| F2 | 같은 버전 재실행·동시 완료 | 결과 확정·적립 한 번 |
| F2 | 이전 작업 또는 중복 작업의 뒤늦은 실패 | 최신 성공 상태·승인 유지 |
| F2 | 결과 저장/시즌 점수 갱신 중 예외, 커밋 후 방송 예외 | 전자는 함께 롤백, 후자는 완료 상태 유지 |
| F2 | 새 버전에서 등급 상승·하락 | 지급 기록·포인트·경험치·시즌 점수 정합 |
| F2 | 현재 버전이 실패한 뒤 같은 버전으로 재예약 | 버전 증가 없이 결과·보상 한 번 확정 |
| F2 | 제출된 글에서 작성자의 책·제목만 변경, 담임의 본문 저장 | 4.2에서 정한 규칙대로 새 버전 발급 또는 변경 거부. 이전 첨삭·승인이 바뀐 입력에 남지 않음 |
| F3 | 준비 중·처리 중·실패·초안의 단건/일괄 승인 직접 요청 | 승인·공개·보상 없음 |
| F3 | 오래된 승인·교사 편집 폼, 버전 누락 | 최신 글의 승인·교사 첨삭 불변 |
| F3 | 같은 버전 중복 승인, 재제출과 승인 경합 | 승인 전이 한 번, 새 제출은 미승인 |
| F3 | 학생 HTML·Turbo 방송·공유 경로 | 현재 버전의 승인된 첨삭만 공개 |
| F3 | 데모 시드·공개 체험 학급 재적재로 만든 승인 글 | `review_ready?` 참, 첨삭 공개와 재적재 검증 통과 |

기존 테스트를 확장할 우선 위치:

- `test/services/games/{quiz_play,point_award}_test.rb`
- `test/integration/{games_ondemand,games_authorization,game_points_flow,games_roster}_test.rb`
- `test/jobs/ai_review_job_test.rb`
- `test/models/report_test.rb`, `test/policies/report_policy_test.rb`
- `test/integration/{report_draft_submission,report_first_review,report_revise_editor,report_autosave,teacher_reviews,report_feedback_gate}_test.rb`
- `test/system/{report_review_gate,games_whoami_answers,report_autosave}_test.rb`

AI 작업 순서 검증에는 서비스 대역과 명시적인 동기화 지점을 사용한다. 무작위 `sleep`으로 실행 순서를 기대하지 않는다. 실제 DB 경합 검사는 파일 기반 테스트 DB와 별도 연결에서 실행하고, 트랜잭션 픽스처 때문에 상대 연결이 데이터를 못 보는 문제를 분리한다.

기존 AI 잡 테스트 중 “같은 인자로 다시 실행하면 재채점된다”는 전제는 변경한다. 중복 실행 테스트는 **같은 버전**, 정당한 재채점 테스트는 **새 제출 버전**을 사용해야 한다.

## 7. 데이터 전환과 배포 계획

이 절은 구현 이후 사용할 절차이며, 계획 작성 단계에서 운영 DB·큐·배포는 변경하지 않는다.

### 7.1 기존 데이터의 버전 채우기

- 미제출 초안: `review_version=0`, `completed_review_version=NULL`.
- 제출된 글: 초기 `review_version=1`.
- 제출된 글 중 `done`이고 루브릭이 있는 행: `completed_review_version=1`.
- 제출된 글 중 대기·처리 중·실패한 행: 완료 버전은 NULL. 예전 루브릭이 남아 있어도 현재 결과가 완성됐다고 추정하지 않는다.
- 백필 과정에서 기존 본문·첨삭·포인트·경험치를 재계산하지 않는다.

### 7.2 이전 형식의 큐 작업 처리

운영은 서버 한 대에서 `SOLID_QUEUE_IN_PUMA: true`([config/deploy.yml](config/deploy.yml))로 돌아, AI 워커가 웹 프로세스 안에 있다. 워커만 따로 멈출 수 없고, Kamal의 기본 교체 배포는 구·신 컨테이너가 잠시 함께 떠 있어 구버전 워커가 새 형식의 작업을 집을 수 있다(인자 불일치로 실패하고 글은 `pending`으로 남는다). 그래서 이번 배포는 무중단 교체에 기대지 않는다.

1. 짧은 점검 시간에 앱 컨테이너를 내려 제출 쓰기와 AI 워커를 함께 멈추고, 진행 중인 구버전 작업이 남지 않았는지 확인한다. primary와 queue DB를 일관된 시점으로 백업한다.
2. 스키마·백필·새 코드를 함께 반영한다. 구버전 컨테이너가 내려간 것을 확인한 뒤 신버전을 올려, 구버전 워커와 신버전 워커가 같은 글에 결과를 쓰지 않게 한다.
3. `expected_review_version`이 없는 기존 작업은 호환 분기에서 기록 후 종료한다. 현재 버전을 임의로 붙여 실행하지 않는다.
4. 제출된 `pending/processing` 행의 최신 버전만 새 형식으로 재예약한다. 이미 완료한 행은 다시 채점하지 않으며, 실패한 행은 원인을 확인한 뒤 선택적으로 재시도한다.
5. 입력·승인·큐 처리·포인트를 점검한 뒤 쓰기를 재개한다. 큐 전체 삭제는 하지 않는다.

### 7.3 과거 오류 데이터 점검

- `reviewed=true`인데 새 `review_ready?`를 만족하지 않는 글을 읽기 전용으로 목록화한다. 새 공개 조건으로 노출을 막고, 필요한 재첨삭·재승인 대상으로 정리한다.
- 과거 포인트 불일치는 교사 수동 지급·소비·다른 보상도 있으므로 단순 합계 차이로 확정하지 않는다.
- 책 소개·뒷이야기 원장만 있고 글이 없는 경우도 곧바로 위조로 판정하지 않는다. 삭제·시드·이관 이력을 함께 확인한다.
- 과거 포인트 차감·완료 기록 삭제·몬스터 회수는 이번 예방 수정에 자동으로 포함하지 않는다. 근거를 확보한 보정 목록과 별도 실행 절차가 필요하다.

되돌리기가 필요하면 새 컬럼을 즉시 삭제하기보다 전진 수정 또는 검증된 호환 버전을 우선한다. 구버전으로 롤백하면 버전 검사 없는 AI 작업과 승인 문제가 다시 발생하므로 쓰기·워커를 중지한 상태에서 판단한다.

## 8. 작업 단위와 완료 기준

| 순서 | 작업 단위 | 완료 기준 |
| --- | --- | --- |
| 1 | 재현 테스트 정착 | 최초 오류 4건이 저장소 테스트에서 동일하게 실패 |
| 2 | F1·F4 게임 제출 수정 | 중복·동시 제출·게임 종류 조작 테스트 통과, 정상 차액·활동 기록 유지 |
| 3 | F2·F3 제출 버전·승인 수정 | 새 컬럼/백필, 버전 있는 잡, 결과 원자 반영, 오래된 승인 차단까지 함께 동작 |
| 4 | 통합 검증·전환 리허설 | 전체 검사 결과 정리, 구형 큐 작업·기존 데이터 전환 검증 |
| 5 | 구현 문서 정리 | 변경 폴더의 `CLAUDE.md` 갱신, 실제 검증 결과와 남은 제약 기록 |

F2의 버전 컬럼·작업 인자·결과 저장과 F3의 승인·공개 검사는 하나의 일관된 배포 단위로 준비한다. 브라우저와 Android는 같은 Rails 화면을 사용하므로 공용 화면 수정 후 Android에서도 제출·중복 탭·승인 반영을 확인한다.

수정 후 검증 명령의 기본값은 다음과 같다. 브라우저 테스트에는 설치된 Chrome/ChromeDriver가 필요하다.

```sh
PARALLEL_WORKERS=1 bin/rails test
PARALLEL_WORKERS=1 bin/rails test:system
bin/rubocop --no-parallel --cache false
bundle exec brakeman --no-pager
```

최초 점검 기준선은 Rails **2,052건 / 실패 0 / 건너뜀 1**, 브라우저 **52건 / 실패 0**, Android 디버그 단위 테스트 **80건 / 실패 0**이다. Brakeman 경고는 0건이었다. RuboCop에는 기존 `script/book_grounding/export_yaml.rb:101` 공백 위반 2건, Android 린트에는 경고 31건이 있었다. 이 기존 경고와 이번 수정으로 새로 생긴 실패를 구분해서 기록한다.

최종 완료 체크리스트:

- [x] F1~F4 재현 테스트와 확장 회귀 테스트 통과
- [x] 포인트·경험치·시즌 점수와 완료 원장의 정상 처리·롤백·동시성 검증
- [x] 이전 AI 작업의 성공·실패·재시도가 최신 결과를 바꾸지 않음
- [x] 준비 중 승인 및 오래된 승인·교사 편집 폼 차단
- [x] 학생 상세·Turbo 방송·공유에서 승인되지 않은 현재 첨삭 비공개
- [x] 실패한 현재 버전의 재예약 경로 동작, 승인 불가 상태로 멈춘 글 없음
- [x] 데모 시드·공개 체험 학급 재적재 후 승인 첨삭 공개 유지
- [x] 기존 테스트 대비 새 실패·불필요한 건너뛰기 없음
- [x] 기존 데이터 백필과 구형 큐 작업 처리 리허설 완료(개발 DB — 운영은 §7.2 절차로 배포할 때)
- [x] 구현 범위·검증 결과·후속 데이터 보정 필요 여부를 이 문서에 갱신(§10)

## 9. 범위 밖

AI 모델·평가 기준·포인트 배점 변경, 몬스터 해금 조건 재설계, 유지 중단된 `frontend/` 체험판 수정, Android 네이티브 기능 추가, 전체 포인트 원장 신설은 포함하지 않는다. 이번 문서는 확인한 4건의 재발 방지에 필요한 제출·완료·승인 경계를 다룬다.

## 10. 구현 결과(2026-09-20)

계획대로 **재현 테스트 정착 → F1·F4 → F2·F3 → 통합 검증** 순서로 구현했다. 운영 DB·큐·배포는 건드리지 않았다(배포 동결 — 배포는 §7.2 절차와 승인을 따른다).

### 10.1 재현 테스트(수정 전 실패 확인)

| ID | 테스트 | 수정 전 결과 |
| --- | --- | --- |
| F1 | `games_ondemand_test` "같은 whoami attempt 를 다시 제출해도 보상은 한 번만 지급된다" | 포인트 15 → 30 |
| F4 | `monster_unlock_flow_test` "객관식 퀴즈에 game 값을 바꿔 보내도 실제 퀴즈 종류(quiz)로만 기록된다" | 원장에 book·sequel·classic·whoami 4종 |
| F2 | `ai_review_job_test` "이전 작업이 늦게 끝나도 최신 제출의 첨삭과 보상을 덮어쓰지 않는다" | 새 본문에 이전 첨삭(내용 5 → 1) |
| F3 | `teacher_reviews_test` "AI 첨삭이 끝나기 전의 글은 승인되지 않고…" | 첨삭 전에 승인됨 |

### 10.2 구현한 것

- **F1** — `Games::QuizPlay#record!` 가 상한 조회 → attempt 확정(`WHERE played_at IS NULL`) → 차액 적립(`credit_points!`) → 완료 원장을 **한 트랜잭션**으로 처리하고 `Result(attempt, awarded_delta, newly_finalized, game_play)` 를 돌려준다. 확정된 attempt 의 재전송은 저장된 결과·보상 0. `PointAward` 는 계산만 한다. 힌트 공개는 `QuizAttempt#reveal_hint!`(미완료만, 같은 완료 조건). 후크·방송은 커밋 뒤.
- **F4** — `Quiz#play_game_type` 한 곳에서 완료 종류를 정하고 컨트롤러·서비스가 함께 쓴다. `params[:game]` 은 읽지 않으며 폼에서도 뺐다. 명시한 `attempt_id` 가 본인·같은 퀴즈의 기록이 아니면 거부, 받을 수 없는 유형은 저장·채점·보상 전에 거부. 원장 기록은 `GamePlay.record_daily!`(savepoint — 같은 날 충돌이 확정·적립을 되돌리지 않는다).
- **F2** — 마이그레이션 `20260920000002`(`review_version`·`completed_review_version` + §7.1 백필). 모든 제출이 `Report#record_submission!` 을 거쳐 버전을 발급받고 `AiReviewJob.enqueue_for(report, version)` 으로 예약한다. 잡은 ① 시작 확인 ② 트랜잭션 밖 첨삭 생성 ③ 조건부 UPDATE 로 확정 권한을 잡은 뒤 결과·지급 기록·포인트·경험치·시즌 점수를 한 트랜잭션에 반영한다. 채점은 ①에서 확보한 가중치로. 실패 기록·방송도 버전을 확인한다. 버전 없는 구형 작업은 기록만 남기고 종료.
- **복구(§4.2-5·§4.4)** — 큐 적재 실패는 기록만 남기고 제출은 성공시킨다. 실패했거나 10분 넘게 멈춘 현재 버전은 `POST /reports/:id/retry_review`(글쓴이·담임)와 `reports:requeue_reviews`(운영, 기본 dry-run)로 **같은 버전** 재예약. `reports:audit_review_versions` 는 §7.3 의 읽기 전용 목록.
- **F3** — `Report#review_ready?`(§5.1 조건 그대로)를 승인(`ReportPolicy#approve?`)·학생 공개(`feedback_visible?`)·공유·화면이 함께 쓴다. 승인은 `Report#approve!(seen_version:)`(조건부 UPDATE, `:approved/:already/:stale/:not_ready`). 상세·목록·일괄 승인 폼이 확인한 버전을 싣고, 누락·불일치는 거부한다. 교사 첨삭 저장도 같은 버전 검사 + `review_ready?`. 일괄 승인은 승인·제외 건수를 따로 안내한다. 데모 시더는 제출된 글에 버전 1/1 을 적는다.
- **§4.2-4 결정** — ① 글쓴이가 책·제목만 바꿔도 새 버전 발급 ② 담임이 제출된 글의 본문·책·제목을 바꾸는 저장은 거부(위 §4.2 에 기록).

### 10.3 구현 중에 발견해 고친 것

- **`record_submission!` 의 메모리 의존 UPDATE** — `update!` 는 바뀐 칸만 UPDATE 에 싣는다. 객체를 읽은 뒤 다른 요청이 승인했다면 메모리의 `reviewed` 가 여전히 false 라 "false 로 되돌리기"가 빠지고, **새 제출이 승인된 채** 남았다. 승인·재제출 경합 테스트(별도 DB 연결)가 1회차에 잡았다. `update_all` + SQL `review_version + 1` 로 고쳤다.
- **재첨삭 뒤 곧바로 공개되는 옛 글** — 이 규칙 이전에 "첨삭 없이 승인"된 글(개발 DB 1건)을 같은 버전으로 다시 첨삭하면 `reviewed` 가 남아 있어 교사 확인 없이 공개될 수 있었다. 결과를 확정하는 순간 `reviewed` 를 false 로 못박는다(정상 흐름에서는 이미 false).

### 10.4 독립 리뷰(작성과 분리한 검토 패스)와 반영

별도 리뷰어가 워킹트리 변경을 계획서와 대조했다 — CRITICAL 0 · HIGH 2 · MEDIUM 6 · LOW 7, 결론 "수정 후 승인". 정확히-1회 보장·F4 우회·보안·마이그레이션은 "이상 없음"이었고, 아래를 반영했다.

| 지적 | 내용 | 반영 |
| --- | --- | --- |
| H1 | 승인 후속 방송이 메모리의 낡은 객체로 그려져, 승인 직후 재제출되면 이전 제출의 첨삭이 학생 화면에 실릴 수 있다(§4.4) | `Report#broadcast_detail_refresh` 가 방송 직전에 DB 에서 다시 읽은 글로 그린다. 승인 후속 처리도 `reload` 한 글로 한다 |
| H2 | '나의 성장'·인쇄 문서가 `reviewed` 만 보고 5축·평균·등급을 보인다 | `scope :review_ready`(SQL 판) + `scope :approved = review_ready.where(reviewed: true)`. 성장 화면이 `Report.approved` 를 쓴다 |
| M1·M6 | 첨삭 입력 변경을 열(`book_title`)로 판정해, 연결한 책 제목과 열이 다른 글은 아무것도 안 고친 저장이 재제출이 됐다(개발 DB 실측 2편). 고쳐쓰기 초안은 책만 바꾸면 제출되지 않았다 | **AI 가 실제로 받는 값**(정규화 본문 + 연결한 책의 제목 또는 제목 열)으로 대입 전·후를 비교한다. 고쳐쓰기 초안도 같은 입력으로 원본과 비교 |
| M2 | `approve?` 정책이 승인 경로에서 불리지 않았다 | 일괄 승인은 `approve?` 로 거른 뒤 `approve!`. 단건은 안내를 위해 `review?` + `approve!`(주석·문서 정렬) |
| M3 | 첨삭이 안 보이는 옛 글에 "확인 완료" 배지 | 배지 첫 분기를 `feedback_visible?` 로 |
| M4 | "제출됐는데 버전 0"인 행이 생기면 영구 고착(잠재) | 잡·예약·`requeue_reviews` 에 버전 > 0 가드, audit 에 목록 추가 |
| M5 | 담임이 제출된 글의 첨부를 바꿀 수 있었다 | 담임 잠금에 첨부 변경 포함 |
| L2·L6·L7 | 힌트 공개 반환값·`Integer("010")`·백필의 `'[]'` | 각각 수정 |

2차 검토(반영분 재확인): 1차 지적 11건 모두 닫힘·남은 HIGH 없음. 추가 지적 N1(방송 실패 rescue 테스트가 다른 인스턴스를 스텁해 공허하게 통과 → 클래스 단위 스텁으로)·N4(`review_ready` SQL↔Ruby 패리티 테스트)·N5(`enqueue_for` 가드 명료화)·N6(버전 0 글에는 다시 요청 버튼을 보이지 않음)·N7(목록 행 방송도 다시 읽은 글로)·N8(주석)을 반영했고, N2('승인'의 두 뜻 — 활동 사실 `reviewed` vs 공개 경계 `Report.approved`)는 세 지점 주석과 문서로, N3(옛 글만 가진 학생의 인쇄 문서가 조용히 빈다)는 문서 출력 화면 안내 + 아래 배포 전 절차로 갈음했다.

**바꾸지 않기로 한 것(결정)**: `ReadingStats`(몬스터 해금·뱃지 지표)와 미션·챌린지 진행도는 계속 `reviewed` 만 센다. 그쪽은 "승인된 글 수"라는 활동 사실이고, 기준을 바꾸면 옛 데이터의 몬스터 조건이 퇴행한다(§7.3 — 과거 데이터 보정은 별도 절차). L3(교사가 만든 hint_reveal 퀴즈의 복귀 경로)·L4(행마다 버전 hidden 전송)·L5(일괄 승인 id 상한)는 기존 동작·무해라 두었다. L1 은 개발 DB 의 게시 퀴즈 45개(교사 mcq 7·system mcq 21·system hint_reveal 17)가 모두 플레이 가능함을 확인했다 — **운영 배포 전에 같은 쿼리로 확인한다**.

### 10.5 검증 결과

| 검사 | 결과 | 기준선 |
| --- | --- | --- |
| `PARALLEL_WORKERS=1 bin/rails test` | **2,125건 / 실패 0 / 오류 0 / 건너뜀 1**(리뷰 1·2차 반영 뒤 재실행) | 2,052 / 0 / 1 |
| `PARALLEL_WORKERS=1 bin/rails test:system` | **52건 / 실패 0 / 건너뜀 0**(리뷰 1·2차 반영 뒤 재실행) | 52 / 0 |
| `bin/rubocop`(변경·신규 Ruby 51개 파일) | 위반 0 | 기존 `script/book_grounding/export_yaml.rb:101` 2건은 이번 변경과 무관 |
| `bundle exec brakeman` | 경고 0 | 0 |
| 동시성(별도 DB 연결, `use_transactional_tests = false`) | `game_reward_concurrency_test` 5건 · `ai_review_job_concurrency_test` 4건 반복 실행 통과 | — |
| 개발 DB 전환 리허설 | 백필 2,028편 중 2,027편 1/1 · 1편 1/NULL, `reports:audit_review_versions` 1건(report 1977 — 첨삭 없이 승인된 옛 글), `reports:requeue_reviews` 대상 0건, 구형 직렬화 작업은 역직렬화 후 건너뜀(테스트) | — |

Android 셸은 같은 Rails 화면을 쓰며 네이티브 코드는 바꾸지 않았다. **Android 기기·에뮬레이터에서의 제출·중복 탭·승인 반영 확인은 하지 않았다**(§8 의 확인 항목 — 배포 전 점검 때 함께 본다).

### 10.6 남은 일·제약

- **운영 배포는 하지 않았다.** §7.2 대로 짧은 점검 시간에 구버전 컨테이너를 내린 뒤 스키마·백필·새 코드를 함께 올리고, `reports:requeue_reviews` 로 pending/processing 을 재예약한다. 배포 전에 게시 퀴즈의 유형 점검(§10.4 L1)과 `reports:audit_review_versions` 를 돌린다(운영 사전 점검은 §10.7 에서 이미 한 번 돌렸고 잔여 0건이다) — 목록에 오른 글(승인됐지만 완성된 첨삭이 없는 옛 글)은 배포 뒤 학생 화면·'나의 성장'·인쇄 문서에서 첨삭·점수가 빠지므로, 담임에게 알리고 "AI 첨삭 다시 요청 → 확인 → 승인"으로 정리한다(잔여 0건이 목표. 마이페이지·교사 통계의 '승인 편수'는 `reviewed` 기준이라 그동안 성장 화면의 편수와 다를 수 있다).
- **과거 오류 데이터 보정은 포함하지 않았다**(§7.3). 운영에서 `reports:audit_review_versions` 로 "승인됐지만 완성된 첨삭이 없는 글"을 목록화한 뒤, 과거 포인트 차감·완료 기록 삭제·몬스터 회수는 근거를 확보한 별도 절차로 다룬다.
- **객관식 재전송은 같은 요청으로 식별하지 못한다**(§3.3 에서 수용한 한계 — 보상은 차액 상한, 완료 원장은 일일 유니크가 묶는다).
- 같은 버전의 AI 작업이 겹쳐 돌면 외부 AI 호출은 중복될 수 있다(결과·보상의 한 번 반영을 먼저 보장 — §4.3).

### 10.7 운영 사전 점검(2026-09-20 23:30 KST, 읽기 전용)

§7.3 의 과거 데이터 점검을 **배포 전에** 운영(chaekgalpi.net, 커밋 `96074a7` 이미지)에서 한 번 돌렸다. 운영에는 아직 새 컬럼도 rake 태스크도 없으므로, 마이그레이션 `20260920000002` 의 백필 규칙을 그대로 옮긴 등가 판정(`제출 + done + 루브릭 있음` = 백필 뒤 `review_ready?`)을 `docker exec … bin/rails runner` 로 실행했다. **SELECT 만 했고 스키마·데이터·큐·배포는 건드리지 않았다.** 같은 스크립트를 개발 DB 에서 먼저 돌려 `reports:audit_review_versions` 와 같은 결과(1건, report 1977)를 내는지 확인한 뒤 운영에 붙였다.

| 항목 | 운영 | 개발 |
| --- | --- | --- |
| 독후감(전부 제출, 초안 0) | 2,053편 | 2,028편 |
| 승인(`reviewed`) | 1,755편 | 1,731편 |
| 백필 뒤 `review_ready` | 2,053편(전부) | 2,027편 |
| **승인됐지만 `review_ready?` 아님** | **0건** | 1건(report 1977) |
| 제출됐는데 버전 0 | 0건(백필 구조상) | 0건 |
| `requeue_reviews` 대상(대기·처리 중) | 0건 | 0건 |
| 실패(`failed`) | 0건 | 0건 |
| 완료 표시인데 루브릭이 빈 글 | 0건 | 1건(= report 1977) |

운영의 제출 글은 2,053편 모두 `done` + 루브릭이 있어 백필 뒤 전부 `review_ready` 가 된다. 즉 **새 공개 조건 때문에 첨삭·등급이 가려지는 글은 운영에 없고, 승인된 1,755편은 배포 후에도 그대로 학생에게 보인다.** 재첨삭·재승인 대상도, 버전을 손볼 행도 없다. 개발 DB 의 1건(report 1977 — 2026-07-27 에 제출 54초 뒤 승인됐고 루브릭·등급·점수가 비어 있음)은 운영에 없는 로컬 데이터다.

이 수치는 점검 시점의 스냅샷이다. 그사이 학생 제출이 쌓이므로 **§7.2 의 점검 시간에 쓰기를 멈춘 뒤 한 번 더 돌려 0건을 확인**하고(배포 후에는 태스크 이름 그대로 `bin/rails reports:audit_review_versions`), 그때 대기·처리 중 행이 있으면 `reports:requeue_reviews` 로 재예약한다. 한편 `requeue_reviews` 는 상태가 `done` 인 행을 대상에 넣지 않으므로, "완료 표시인데 루브릭이 빈 글"(위 표의 마지막 줄)은 rake 가 아니라 화면의 'AI 첨삭 다시 요청'으로 처리한다 — 이런 행은 `review_retryable?` 가 참이라 버튼이 보인다.

