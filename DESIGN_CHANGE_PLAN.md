# 「책갈피」 전면 디자인 변경 계획서

> 상태: **1차 묶음 구현 완료** (Phase 1·2 + Phase 3 대표 화면)  
> 작성일: 2026-07-15  
> 대상: `app/views/` 전체와 공통 레이아웃·Tailwind 스타일  
> 디자인 기준: [`DESIGN.md`](DESIGN.md) · 구현 매핑: [`DESIGN.md` §Implementation](DESIGN.md)

> **진행 상태 (2026-07-15)**
> - ✅ **완료**: Phase 1(디자인 토큰 `@theme` + 전역 base + Pretendard 자체호스팅), Phase 2(유동 페이지 셸 레이아웃·통합 flash·학생 반응형 내비·admin 반응형 사이드바), Phase 3 대표 화면(학생 대시보드, 로그인·가입 4화면, 독후감 index/show/new/edit + partial). 공통 컴포넌트 클래스(`.btn`/`.card`/`.form-*`/`.badge`/`.page-shell*`/`.state-banner` 등)와 토큰 체계 구축. `bin/rails test` 746 runs 그린 유지.
> - ⏭ **이월(후속 묶음)**: Phase 4(게임·도감·상점·미션·랭킹), Phase 5(커뮤니티), Phase 6(교사·사서·교무 심화), Phase 7(관리자 CRUD 전면), Phase 8(인쇄·메일·PWA·Turbo Stream 정리). 구축된 공통 시스템을 화면군 단위로 확장 적용한다(계획서 §8·§10 순서·회귀 방지 준수).

---

## 1. 목적

이 문서는 「책갈피」의 학생·교사·교무관리자·사서·총괄관리자 화면을 일관된 디자인 체계로 개편하기 위한 실행 계획이다.

현재 `DESIGN.md`에는 브랜드 색상과 컴포넌트 원칙이 정의되어 있지만 실제 화면은 개별 Tailwind 유틸리티 조합에 크게 의존한다. 그 결과 페이지마다 카드, 버튼, 입력창, 여백, 콘텐츠 너비가 달라지고 넓은 데스크톱 화면도 충분히 활용하지 못한다.

이번 변경은 다음 세 가지를 함께 달성한다.

1. 초등학생에게 친근한 밝고 단정한 시각 언어를 만든다.
2. 데스크톱의 불필요한 상하좌우 여백을 줄이고 넓은 화면을 적극 활용한다.
3. 모바일에서는 단순 축소가 아닌 정보 구조 재배치로 완성도 높은 반응형 경험을 제공한다.

---

## 2. 목표와 비목표

### 목표

- 학생 화면에 몬스터·독서·성장 경험을 살리는 파스텔 색과 둥근 형태를 적용한다.
- 교직원 화면은 같은 브랜드 체계를 사용하되 업무 집중도가 높은 차분한 콘솔로 만든다.
- 모든 화면에 명확한 배경, 카드 경계, 섹션 구분, 활성 상태를 제공한다.
- 반복되는 Tailwind 클래스와 ERB 구조를 토큰·공통 클래스·partial로 통합한다.
- 데스크톱, 태블릿, 모바일에서 기능이 끊김 없이 이어지게 한다.
- 키보드, 스크린 리더, 터치 조작을 고려한 접근성을 확보한다.
- 기존 Rails, Turbo, Stimulus 동작과 인가 경계를 유지한다.

### 비목표

- 컨트롤러 비즈니스 로직, 모델 규칙, 권한 정책을 재설계하지 않는다.
- 디자인 변경만을 이유로 외부 UI 프레임워크를 추가하지 않는다.
- 모든 역할 화면을 학생용처럼 장식적으로 만들지 않는다.
- 본문과 입력 폼을 와이드 화면 끝까지 무작정 늘리지 않는다.
- 무거운 그림자, 과도한 애니메이션, 장식용 이미지를 남발하지 않는다.

---

## 3. 현행 문제

### 공통 기반

- `app/assets/tailwind/application.css`가 Tailwind import 외에 공통 토큰과 컴포넌트를 제공하지 않는다.
- `app/assets/stylesheets/application.css`도 실질적인 공통 스타일이 없다.
- 버튼, 카드, 폼, 상태 배지가 각 뷰에서 반복 구현된다.
- Pretendard 자체 호스팅 원칙이 문서에는 있으나 실제 전역 적용은 완료되지 않았다.

### 레이아웃

- 기본 레이아웃의 고정 `mt-28` 때문에 모든 화면에서 상단 공간이 과도하게 비어 있다.
- 다수 페이지가 `max-w-3xl` 또는 `max-w-4xl`에 제한되어 데스크톱 공간을 사용하지 못한다.
- 페이지 유형과 관계없이 비슷한 컨테이너 패턴을 사용한다.
- 관리자 사이드바와 본문이 작은 화면에서 자연스럽게 접히지 않는다.

### 학생 경험

- 학생 내비게이션이 텍스트 탭 위주이며 모바일에서 항목이 넘친다.
- 학생 대시보드, 게임, 도감, 상점, 미션이 서로 다른 경험처럼 보인다.
- 몬스터 속성 색과 성장 상태가 화면 구조에 충분히 반영되지 않는다.
- 터치 대상과 핵심 행동의 시각적 우선순위가 약하다.

### 교직원 경험

- 통계 카드, 검토 큐, 표, CRUD 폼이 대부분 비슷한 회색 보더 박스로 표현된다.
- 교사·사서·교무·총괄관리자의 역할 구분이 화면에서 충분히 드러나지 않는다.
- 넓은 화면에서 표와 대시보드가 불필요하게 좁고 세로로 길어진다.

---

## 4. 디자인 방향

### 전체 콘셉트: 깔끔한 독서 놀이터

- 흰색과 아주 옅은 크림색을 기본 캔버스로 사용한다.
- 책갈피 옐로는 워드마크, 포인트, 레벨, 핵심 강조에 제한적으로 사용한다.
- 라벤더, 로즈, 코랄, 민트, 틸 파스텔은 학생 피처 카드와 몬스터 속성 표현에 사용한다.
- 기본 카드에 1px 보더를 적용해 배경과 내용을 명확히 구분한다.
- 큰 카드와 학생 피처는 20~28px, 폼과 업무 카드는 12~16px 라운드를 기본으로 한다.
- 핵심 CTA는 검은색 필 버튼, 보조 행동은 흰색 아웃라인 버튼을 사용한다.
- 장식은 몬스터 스프라이트, 진행 상태, 실제 앱 데이터가 담당한다.

### 역할별 톤

| 화면군 | 기본 톤 | 색 사용 | 밀도 |
|---|---|---|---|
| 학생 | 밝고 활기찬 파스텔 | 속성색과 옐로 강조 | 넉넉함 |
| 교사 | 차분한 흰 카드와 라벤더 포인트 | 검토·성장 상태 | 중간 |
| 사서 | 흰 카드와 민트/틸 포인트 | 도서·행사 상태 | 중간 |
| 교무 | 흰 카드와 블루/틸 포인트 | 통계와 비교 | 높음 |
| 총괄관리자 | 중립 표면과 명확한 상태색 | 경고·승인·관리 상태 | 높음 |

---

## 5. 반응형 레이아웃 기준

### 데스크톱 공간 활용

데스크톱에서는 화면 전체를 활용하되 읽기 폭과 업무 폭을 구분한다.

- 전역 상단 고정 여백 `mt-28`을 제거한다.
- 기본 페이지 상하 패딩은 24~32px로 줄인다.
- 좌우 거터는 화면 폭에 따라 16~32px 범위에서 유동적으로 적용한다.
- 일반 콘텐츠 셸은 최대 1440~1536px를 사용한다.
- 관리자·통계·목록 화면은 사이드바를 제외한 가용 너비를 거의 모두 사용한다.
- 긴 글과 단일 폼은 720~960px의 읽기 폭을 유지한다.
- 긴 글 주변의 남는 공간에는 책 정보, 작성 단계, 점수, 도움말 등 보조 패널을 배치한다.
- 대시보드와 목록은 데스크톱에서 2~4열 카드, 분할 패널, 넓은 표를 사용한다.

### 페이지 셸 유형

| 셸 | 용도 | 데스크톱 | 모바일 |
|---|---|---|---|
| `wide` | 대시보드, 목록, 통계, 관리자 | 최대 1536px 또는 가용 폭 전체 | 1열 |
| `content` | 도서 상세, 게임, 커뮤니티 | 최대 1280px | 1열 |
| `reading` | 독후감 상세, 긴 안내 | 본문 720~840px + 보조 패널 | 본문 단일 열 |
| `form` | 가입, 작성, CRUD 폼 | 폼 720~960px | 전체 폭 |
| `split` | 목록+상세, 차트+요약 | 2:1 또는 3:2 분할 | 세로 스택 |

### 브레이크포인트

| 구간 | 너비 | 주요 변화 |
|---|---:|---|
| 소형 모바일 | 360~479px | 1열, 16px 거터, 전체 폭 주요 버튼 |
| 대형 모바일 | 480~767px | 일부 2열 타일, 학생 내비 가로 스크롤 또는 하단 배치 |
| 태블릿 | 768~1023px | 2열 카드, 접이식 사이드바, 표 스크롤 |
| 데스크톱 | 1024~1439px | 다열 대시보드, 사이드바+본문, 분할 상세 |
| 와이드 | 1440px 이상 | 최대 1536px 셸, 3~4열 카드, 넓은 데이터 표 |

### 모바일 재배치

- 헤더의 제목, 설명, 행동 버튼을 세로로 배치한다.
- 주요 CTA는 필요한 화면에서 전체 너비로 확장한다.
- 학생 메뉴는 가로 스크롤 탭 또는 하단형 내비게이션으로 전환한다.
- 교직원 사이드바는 메뉴 버튼으로 열고 닫는 패널로 전환한다.
- 통계 카드는 2열 또는 1열로 축소한다.
- 데이터 표는 의미에 따라 가로 스크롤 또는 카드형 행으로 바꾼다.
- 필터와 검색은 줄바꿈 또는 세로 스택을 허용한다.
- 모달은 작은 화면에서 바텀 시트 또는 화면 폭에 가까운 패널로 표시한다.
- 긴 제목, 학교명, 책 제목은 줄바꿈과 말줄임을 화면별로 구분한다.

### 터치와 접근성

- 버튼, 링크형 탭, 입력창은 최소 44px 높이를 확보한다.
- 포커스 링을 제거하지 않고 브랜드 블루 기반으로 통일한다.
- 한글 본문은 최소 14px, 학생 핵심 안내는 16px 이상을 사용한다.
- 색만으로 상태를 전달하지 않고 텍스트·아이콘·보더를 함께 사용한다.
- 장식용 이모지는 스크린 리더에서 불필요하게 읽히지 않게 처리한다.
- `prefers-reduced-motion` 환경에서는 장식 애니메이션을 줄인다.

---

## 6. 디자인 시스템 구현

### 디자인 토큰

`DESIGN.md`를 기준으로 Tailwind v4 테마에 다음 토큰을 연결한다.

- 색상: 브랜드 옐로, 잉크, 표면, 헤어라인, 블루, 코랄, 로즈, 틸, 민트, 라벤더
- 타이포그래피: Pretendard 폰트 스택, 제목·본문·캡션·스탯 단계
- 간격: 4px 기준의 페이지·섹션·카드 간격
- 라운드: 입력 8px, 업무 카드 12~16px, 학생 피처 20~28px, 필 버튼 9999px
- 그림자: 기본 카드 최소, 떠 있는 패널과 모달만 제한적으로 사용
- 레이아웃: 거터, 최대 폭, 사이드바 폭, 스티키 헤더 높이

### 공통 UI 패턴

반복 빈도가 높은 패턴만 공통 클래스 또는 partial로 만든다. 모든 Tailwind 조합을 별도 클래스로 추상화하지 않는다.

- 페이지: `page-shell`, `page-header`, `page-actions`
- 카드: 기본 카드, 피처 카드, 통계 카드, 선택 카드
- 버튼: primary, secondary, subtle, danger, icon
- 폼: label, input, select, textarea, hint, error
- 상태: badge, alert, empty state, progress bar
- 데이터: table shell, toolbar, pagination
- 내비게이션: student nav, staff sidebar, mobile menu

### 공통 partial 후보

- `shared/_page_header.html.erb`
- `shared/_stat_card.html.erb`
- `shared/_status_badge.html.erb`
- `shared/_flash_messages.html.erb`
- 기존 `shared/_empty_state.html.erb` 개선
- 기존 `shared/_student_nav.html.erb` 반응형 개편

partial은 전달 인자가 과도하게 늘어나면 만들지 않고 단순한 HTML 패턴은 공통 클래스만 사용한다.

---

## 7. 단계별 변경 계획

### Phase 1. 문서와 공통 기반

**대상**: `DESIGN.md`, `app/assets/tailwind/application.css`, 글꼴 에셋

**작업**

- 디자인 토큰을 실제 Tailwind 값과 1:1로 맞춘다.
- 학생/교직원 톤, 셸 유형, 반응형 규칙을 `DESIGN.md`에 추가한다.
- Pretendard를 자체 호스팅하고 전역 기본 폰트로 지정한다.
- 공통 포커스, 본문 배경, 텍스트 선택, 모션 감소 스타일을 정의한다.

**완료 조건**

- 토큰 이름과 실제 렌더 색상이 일치한다.
- 임의의 새 악센트색 없이 기존 팔레트만 사용한다.
- 360px와 1536px 화면에서 공통 셸이 정상 동작한다.

### Phase 2. 공통 레이아웃과 내비게이션

**대상**: `layouts/application`, `layouts/admin`, 학생 내비게이션, 교사 내비게이션, flash와 배너

**작업**

- 고정 `mt-28`과 획일적인 컨테이너를 제거한다.
- 역할과 페이지 유형에 맞는 유동형 셸을 적용한다.
- 학생 내비게이션을 아이콘·라벨·활성 상태가 명확한 형태로 바꾼다.
- 교사와 관리자 사이드바를 모바일에서 접을 수 있게 한다.
- 알림을 공통 배너 또는 토스트 형태로 통일한다.

**완료 조건**

- 모든 일반 화면의 상단이 과도하게 비지 않는다.
- 데스크톱에서 가용 너비를 사용하고 모바일에서 가로 페이지 스크롤이 생기지 않는다.
- 현재 메뉴가 시각적·의미적으로 구분된다.

### Phase 3. 로그인·가입과 학생 핵심 여정

**대상**: `sessions/`, `registrations/`, 학교 선택, 학생 대시보드, `books/`, `reports/`

**작업**

- 로그인 선택 화면을 학생/교직원 선택 카드로 구성한다.
- 학교 선택 폼과 오류 메시지를 모바일 친화적으로 정리한다.
- 학생 홈을 인사·오늘의 행동·몬스터·포인트·미션·최근 독서 영역으로 재배치한다.
- 도서 목록은 반응형 카드 그리드와 검색 도구 모음을 사용한다.
- 독후감 작성은 단계와 도움말이 명확한 집중형 폼으로 만든다.
- 독후감 상세는 본문과 AI 피드백/루브릭을 데스크톱 분할, 모바일 스택으로 표시한다.

**완료 조건**

- 학생이 로그인 후 핵심 행동을 한 화면 안에서 찾을 수 있다.
- 폼 오류, OCR, AI 진행 상태가 모바일에서도 잘리지 않는다.
- Turbo Stream으로 교체되는 독후감 상세가 동일한 디자인을 유지한다.

### Phase 4. 학생 성장·게임 화면

**대상**: `games/`, `monsters/`, `shops/`, `missions/`, `challenges/`, `rankings/`

**작업**

- 게임 종류를 색과 아이콘으로 구분하고 큰 터치 카드로 만든다.
- 몬스터 도감은 모바일 2열, 데스크톱 4~6열로 구성한다.
- 미발견 몬스터는 점선 보더와 명확한 잠금 상태를 사용한다.
- 진화 로드맵과 진행률은 단계형 시각 구조로 바꾼다.
- 상점은 아이템 정보, 보유 여부, 가격, 구매 행동의 우선순위를 정리한다.
- 랭킹은 Top 3 포디움과 나머지 목록을 구분한다.

**완료 조건**

- 6속성 색상 매핑이 화면 간 일관된다.
- 현재 포인트, 진행률, 잠금 조건을 색에만 의존하지 않고 이해할 수 있다.
- 구매·진화·게임 제출 후 Turbo 갱신에서도 레이아웃 이동이 과도하지 않다.

### Phase 5. 학생 커뮤니티 화면

**대상**: `board_posts/`, `topics/`, 응원·좋아요·스티커 Turbo partial

**작업**

- 게시글 목록을 작성자·본문·반응이 구분되는 카드로 정리한다.
- 토론 상세는 주제와 답변 흐름이 한눈에 보이게 한다.
- 반응 버튼의 터치 크기와 선택 상태를 명확히 한다.
- 빈 상태와 모더레이션 상태를 공통 상태 컴포넌트로 표현한다.

**완료 조건**

- 긴 글과 반응 버튼이 모바일에서 겹치지 않는다.
- Turbo Stream 갱신 전후 버튼 크기와 배치가 유지된다.

### Phase 6. 교사·사서·교무 화면

**대상**: `teacher/`, `librarian/`, `school_admin/`, 역할별 대시보드

**작업**

- 교사 콘솔에 그룹형 사이드바와 넓은 검토 작업 영역을 제공한다.
- 통계 카드는 데스크톱 3~4열, 모바일 2열 또는 1열로 배치한다.
- 검토 큐는 필터·학생·상태·행동이 명확한 목록으로 구성한다.
- 사서 화면은 도서·대출·행사를 민트/틸 포인트로 구분한다.
- 교무 통계는 넓은 표와 차트 영역을 사용하고 모바일에서는 요약 우선으로 재배치한다.
- CRUD 폼은 공통 필드, 오류, 저장 행동 패턴을 사용한다.

**완료 조건**

- 1280px 이상에서 업무 데이터가 좁은 중앙 열에 갇히지 않는다.
- 사이드바가 태블릿과 모바일에서 콘텐츠를 밀어내지 않는다.
- 표 헤더, 행 행동, 필터를 키보드와 터치로 사용할 수 있다.

### Phase 7. 총괄관리자 화면

**대상**: `admin/` 전체와 `layouts/admin`

**작업**

- 관리자 사이드바를 메뉴 그룹과 활성 상태 중심으로 재구성한다.
- 통계, 목록, 상세, 폼, 페이지네이션을 공통 관리자 패턴으로 통일한다.
- 넓은 데스크톱에서는 전체 데이터 열을 최대한 노출한다.
- 모바일에서는 낮은 우선순위 열을 숨기거나 상세 카드에서 확인하게 한다.
- 위험한 행동은 색, 위치, 확인 흐름으로 일반 행동과 분리한다.

**완료 조건**

- 모든 관리자 CRUD가 같은 헤더·버튼·폼·표 규칙을 사용한다.
- 사이드바와 데이터 표 때문에 페이지 전체 가로 스크롤이 발생하지 않는다.
- 성공·경고·오류 상태가 역할 화면 전체에서 일관된다.

### Phase 8. 특수 출력과 최종 정리

**대상**: Turbo Stream 뷰, print 레이아웃과 출력물, mailer, PWA 회귀 확인, `app/views/CLAUDE.md`

**작업**

- 비동기 교체 영역의 공통 상태와 애니메이션을 정리한다.
- 인쇄 화면에서는 앱 배경, 내비게이션, 그림자를 제거한다.
- 메일은 앱과 동일한 색을 사용하되 이메일 클라이언트 호환성을 우선한다.
- 변경된 partial과 화면 구조를 문서에 반영한다.

**완료 조건**

- Turbo, 인쇄, 메일, PWA 관련 기존 동작이 유지된다.
- 뷰 파일 구조 변경이 `app/views/CLAUDE.md`에 반영된다.

---

## 8. 구현 순서와 변경 단위

전체 뷰를 한 번에 수정하지 않는다. 각 단계는 다음 순서로 진행한다.

1. 디자인 토큰과 공통 레이아웃을 만든다.
2. 대표 화면 한 개에 적용해 데스크톱/모바일 기준을 검증한다.
3. 같은 화면군의 index/show/form/partial에 확장한다.
4. Turbo Stream과 빈 상태를 확인한다.
5. 해당 화면군 테스트를 실행한다.
6. 다음 화면군으로 이동한다.

| 화면군 | 대표 화면 |
|---|---|
| 학생 | 학생 대시보드, 독후감 목록/상세, 몬스터 도감 |
| 교사 | 교사 대시보드, 검토 상세, 학생 목록 |
| 사서 | 사서 대시보드, 행사 목록 |
| 교무 | 전교 통계 |
| 관리자 | 통합 통계, 사용자 목록, CRUD 폼 |

---

## 9. 검증 계획

### 뷰포트

- 360px: 소형 Android
- 390px: 일반 모바일
- 768px: 태블릿 세로
- 1024px: 태블릿 가로/소형 데스크톱
- 1440px: 일반 데스크톱
- 1920px: 와이드 데스크톱

### 주요 기능 흐름

- 학생 로그인 → 대시보드 → 도서 → 독후감 작성/상세
- 학생 게임 → 점수 반영 → 몬스터/포인트 확인
- 상점 구매와 Turbo Stream 갱신
- 교사 로그인 → 검토 큐 → 독후감 승인/재첨삭
- 사서 행사와 대출 화면
- 교무 통계와 NEIS 화면
- 관리자 목록 → 상세 → 수정 흐름

### 자동 검증

- `bin/rails test`
- 관련 통합 테스트 개별 실행
- `bin/rails test:system`이 구성되어 있다면 대표 반응형 흐름 추가
- `bin/rubocop`
- Tailwind 빌드 및 오타 클래스 확인
- ERB 렌더 오류와 Turbo Stream 대상 DOM ID 확인

### 수동 품질 체크

- 의도하지 않은 전체 페이지 가로 스크롤 없음
- 키보드만으로 내비게이션과 폼 조작 가능
- 포커스 위치가 항상 보임
- 200% 확대에서도 핵심 기능 사용 가능
- 긴 한글 텍스트와 빈 데이터 상태 확인
- 로딩, 성공, 오류, 권한 없음 상태 확인
- 애니메이션 감소 설정 확인

---

## 10. 위험과 대응

| 위험 | 대응 |
|---|---|
| 대량 ERB 변경으로 기능 회귀 | 화면군 단위로 적용하고 관련 통합 테스트를 매 단계 실행 |
| 유틸리티와 공통 클래스 혼용 충돌 | 토큰과 핵심 패턴만 공통화하고 적용 순서를 문서화 |
| 모바일 표가 지나치게 복잡함 | 중요도에 따라 스크롤 표와 카드형 행을 선택 |
| 학생 화면의 색이 과도해짐 | 한 화면에 강한 피처 색 1~2개, 나머지는 옅은 표면 사용 |
| 와이드 화면에서 본문 가독성 저하 | 읽기 셸과 업무 셸을 분리하고 긴 문장 폭을 제한 |
| Turbo 갱신 후 스타일 불일치 | 페이지와 Turbo 응답이 같은 partial을 사용하도록 유지 |
| 기존 사용자 변경과 충돌 | 현재 변경을 보존하고 작은 파일 묶음으로 순차 적용 |

---

## 11. 전체 완료 정의

- [ ] `DESIGN.md`와 Tailwind 토큰이 일치한다.
- [ ] 기본 레이아웃의 과도한 상하좌우 여백이 제거되었다.
- [ ] 데스크톱 대시보드·목록·통계가 가용 화면 폭을 활용한다.
- [ ] 긴 글과 폼은 읽기 편한 폭을 유지한다.
- [ ] 360px부터 1920px까지 대표 화면이 정상적으로 재배치된다.
- [ ] 학생 화면이 밝고 친근하면서 정보 우선순위가 분명하다.
- [ ] 교직원·관리자 화면이 차분하고 높은 데이터 밀도를 지원한다.
- [ ] 버튼, 카드, 폼, 표, 배지, 상태 메시지가 공통 규칙을 사용한다.
- [ ] 학생·교사 내비게이션이 모바일에서 완전히 사용 가능하다.
- [ ] Turbo Stream, 인쇄, 메일, PWA 동작에 회귀가 없다.
- [ ] 접근성 체크와 Rails 테스트가 통과한다.
- [ ] 변경된 뷰 구조와 디자인 규칙이 관련 문서에 반영되었다.

---

## 12. 권장 착수점

첫 구현 묶음은 다음 파일로 제한한다.

1. `DESIGN.md` 반응형·역할별 톤 보강
2. `app/assets/tailwind/application.css` 토큰과 공통 기반 추가
3. `app/views/layouts/application.html.erb` 유동형 페이지 셸 적용
4. `app/views/shared/_student_nav.html.erb` 반응형 내비게이션 적용
5. `app/views/dashboard/student.html.erb`를 대표 학생 화면으로 개편

이 묶음에서 360px, 768px, 1440px, 1920px 레이아웃을 먼저 확정한 뒤 나머지 화면으로 확장한다.
