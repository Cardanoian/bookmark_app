# app/services — 도메인 로직 서비스 객체 계층

컨트롤러를 얇게 유지하기 위해 몬스터·랭킹·독서지표·외부 API 연동 같은 도메인 로직을 담는 PORO 서비스 계층. 대부분 `initialize(user)` 로 주체를 받아 `call`/도메인 메서드를 노출한다. 외부 연동(AI·도서검색·인기대출)은 키가 없거나 실패해도 규칙기반/로컬/빈 결과로 폴백해 절대 크래시하지 않는 것이 공통 계약이다.

## 파일
- `monster_acquisition.rb` — 노력 기반 몬스터 획득(스타터 3종 선택 + 마일스톤 발견). 가챠·랜덤 경로 없음, 라인 미보유 시에만 stage 1 폼 생성.
- `monster_seeder.rb` — 도감 시드(`db/seeds/monsters.yml` → `monster_species`). 24라인×3=72폼 전량/Phase별 적재, `evolves_from` 자동 연결, key 기준 멱등. **라인 단위 `unlock_condition`은 stage 1 폼에만 대입**(2·3단계는 nil — 재시드해도 조건이 새지 않게).
- `monster_unlock.rb` — 몬스터 라인 자동 해금 평가(`docs/monster_unlocks.md §4`). 미보유·stage1·`unlock_condition` 존재 라인만 순회해 `ReadingStats#meets?`(AND 판정) 충족 시 `MonsterAcquisition#discover_monster!`로 지급(가챠·확률·유료 경로 없음). **서비스 내부 fixpoint 루프**(최대 24라운드=라인 수 상한)로 `dex_count`처럼 발견이 발견을 부르는 캐스케이드를 한 호출 안에서 완결한다(호출부는 `evaluate!`를 1회만 호출). 개별 라인 지급 실패는 rescue 해 전체 평가를 막지 않는다. 호출부는 쓰기 트리거(승인·게임·토론·스타터) 외에 **도감 조회(monsters#index/#show)에서도 self-heal**로 1회 호출해, 트리거를 거치지 않고 조건이 충족된 라인이 고착되지 않게 한다.
- `ranking_board.rb` — 랭킹 집계(학급/포디움/**학년**/학교/전국/챌린지/명예의 전당). 부작용 없는 순수 조회, 그룹 집계로 N+1 제거. **랭킹 시즌제(account_linking_seasons_plan §Phase 1)**: 읽기 플래그 `AppSetting.feature_enabled?("ranking_seasons", scope: 뷰어 학급, default: false)`(인스턴스당 1회 메모이즈)가 켜지면 정렬 기준을 평생 `users.experience` 에서 현재 학년도 `season_scores.experience_earned` 로 전환한다(class/grade 는 개인 시즌 경험치 순, school/nation 은 **현재 소속 users.classroom_id/school_id 로 group + SUM(season 경험치)** — 스냅샷 컬럼 미사용). 시즌 조인은 `[academic_year,user_id]` 유니크만 타는 현재 학년도 LEFT JOIN(`COALESCE(...,0) AS season_experience`, 신규 0 출발). **플래그 off(기본)면 평생 experience 폴백으로 완전 무변경**(전환 스위치). **`grade_ranking`(신규)**: 뷰어 학교 + 같은 학년(현재 소속) 개인 순위. `podium`=class_ranking.first(3), `challenge_ranking`·`hall_of_fame` 무변경(전당은 평생 도감+진화).
- `student_home_query.rb` — **학생 홈 조회(menu_refactor 심화 §5.1, PR5)**. 활성 XLSX의 공식 추천도서(어린이 분과, 날짜 시작점 + `recommend_cycle`로 6권씩 순환, `more_recommended_books?`=후보>6일 때만 "다른 책 보기" 노출)·우리 반 인기 도서(최근 30일 승인 독후감 상위 — 후보 풀은 `POPULAR_POOL_LIMIT`=30권까지 집계하고 `popular_cycle`로 6권씩 순환, `more_popular_books?`=후보>6일 때만 "다른 책 보기" 노출. **추천·발견과 달리 날짜를 시작점에 더하지 않는다** — 순위 자체가 정보라 cycle 0 은 언제나 실제 1~6위. COUNT 동점은 `book_id ASC`로 타이브레이크해 페이징에서 같은 책이 두 페이지에 나오거나 사라지지 않게 한다)·기존 혼합 카탈로그 책 발견("이 책은 어때요?", 공식 추천·활동책 제외, 학생+날짜 시작점과 `discovery_cycle`로 6권씩 순환)·진행 중 미션. 세 cycle 은 `MAX_CYCLE`로 클램프. 각 섹션 제한 수량·독립 폴백.
- `student_library_query.rb` — **내 서재 조회(menu_refactor 심화 §5.2, PR5)**. 현재 학생의 책별 활동 집계(reports·game_plays GROUP BY 후 Book 일괄 로드, 전체 활동 Ruby group_by 안 함) + kind 필터 + 책 미연결(book_id nil) 독후감을 정규화 book_title 로 레거시 그룹. `Entry`/`LegacyGroup` 읽기 전용 Struct(새 테이블 없음).
- `reading_stats.rb` — 진화·뱃지·**몬스터 해금**(unlock_condition) 조건 판정용 독서 지표 집계(포인트·독후감·스트릭 등). `meets?(condition)` 로 조건 해시를 AND 판정, 지표는 메모이즈. **Phase 3B 게임 지표**: `game_plays`(원장 총 완료 수)·`distinct_games`(플레이한 서로 다른 게임 종류 수)·`game_books`(게임으로 만난 서로 다른 책 수)·`max_daily_reports`(Asia/Seoul 제출일별 전체 기간 첫 승인 독후감의 distinct book 최댓값, 연결 `Book.title`/`book_title` 정규화로 중복 제거) 추가, `classics`는 distinct book 카운트로 변경(같은 책 반복 제출로 인한 인플레이션 방지). **`missions`(menu_refactor 심화 PR2 전환)**: 옛 `reports.mission_id`(참여) 대신 `mission_participations.completed_at`(완료)을 센다 — status 무관(archived 백필 완료 보존, 몬스터 조건 퇴행 0).
- `rate_limiter.rb` — **Solid Cache 원자 increment 기반 rate limit / 예산 유틸(Phase 2b §2b.5)**. `allow?(key, limit:, period:)`·`warming_allowed?(user_id)`(per-user 시간당 한도 AND 글로벌 일일 예산). 온디맨드 게임 워밍 방어에 쓰이며, Phase 6 #7(로그인 스로틀)이 같은 프리미티브를 재사용하도록 설계. 초과 시 호출자는 오프라인 강등/차단으로 우아하게 대응. 저장소 주입 가능(기본 `Rails.cache`; test 는 null_store 라 카운팅 스토어 주입).

## 하위 폴더
- `accounts/` — **계정 연동(MERGE) 도메인 서비스(account_linking_seasons_plan §Phase 2, 별도 CLAUDE.md 없음)**. `merge_service.rb`(`Accounts::MergeService`) — 학년이 바뀌어 새 담임이 만든 현재 학년도 placeholder(new)를 접어 삭제하고 작년 기록을 가진 계정(old)을 생존자로 남기는 **전 과정 단일 `ApplicationRecord.transaction`** 병합. `initialize(old_account:, new_account:, performed_by:)`·`preview`(부작용 없이 생존자 회복 자산 요약)·`call`(Result 구조체 `ok?/surviving_user/account_merge/error_code` 반환). 알고리즘: 가드(양쪽 학생·상이·old 과거 학년도·new 현재 학년도·미정지) → 스냅샷·매니페스트 캡처 → NEW active_monster 해제 → **자식 재부모화**(user_monsters **in-place 승격**[dex 충돌 시 stage 비교, NEW 우월이면 OLD 행 제자리 승격·id·celebrated_at 보존 → active_monster 불변] / **생존자-우선 dedup**[user_badges·game_plays(부분 유니크 2종 EXISTS)·mission_participations·cheers·book_intro_votes·book_sequel_votes·forum_post_likes·forum_post_reports·quiz_reports] / **단순 이관**[reports·forum_posts·book_intros·book_sequels·quiz_attempts·quiz_contributions·stickers·recommendation_imports]) → **counter 재계산**(표준 5종 `reset_counters`, `cheers_count`는 커스텀 `report.update_columns(cheers_count: board_post.cheers.count)` — #forum_post_reports·#quiz_reports 의 hide/reported 플래그는 sticky 미재평가) → 평생 합산 + **시즌 sum-and-delete**(NEW 학년도별 season_scores 를 OLD 로 ON CONFLICT 합산 후 NEW 삭제) → placeholder **raw `delete_all`** + **조건부 claim**(pre-merge tuple 토큰 WHERE, affected 0 → 롤백 = 동시 이중병합 fail-closed) → `AccountMerge.create!` 감사. **커밋 후 사이드이펙트(reload·뱃지·진화·시즌 방송·세션 스왑)는 서비스가 아니라 호출자(컨트롤러) 책임** — `run_post_commit_side_effects!(survivor)` 헬퍼는 call 밖에서 호출한다. **⚠️ 인가 계약(무가드 프리미티브)**: 서비스는 학년도·정지·학생여부만 검증하고 **소유권·인증은 검증하지 않는다** — 호출자가 NEW=인증된 현재 세션 계정, OLD=소유 증명된 계정(셀프서브=비번+스로틀, 교사=`owned_student!`, 총괄=역할 게이트)임을 반드시 강제해야 한다(클래스 헤더 명문화). **동시성 fail-closed**: 같은 OLD 경합은 조건부 claim(`:claim_conflict`), 서로 다른 OLD→같은 NEW 는 `write_audit!` 활성 consumed 부분유니크가 `ActiveRecord::RecordNotUnique`→`:consumed_conflict` 로 우아하게 롤백. `account_merge.rb`(app/models — `reverse!` 되돌리기)·`season_score.rb`(§Phase 0)와 연동.
- [`ai/`](ai/CLAUDE.md) — Google Gemini 연동 AI 서비스(5축 첨삭·OCR·퀴즈생성·진위확인)와 규칙기반 폴백.
- [`games/`](games/CLAUDE.md) — 독서게임 서버 권위 채점(`question_scorer` 4종)·멱등 델타 적립(`point_award` origin 분기)·플레이 기록(`quiz_play`)·**콘텐츠축 캐시-우선 리졸버(`content_provider`, Phase 2b) + 가용성 게이트(`game_content_available?`, Phase 4 — AI-적격[classic·summary] 또는 ai/contributed 콘텐츠 있는 책만 퀴즈·나는 누구게? 표시·플레이) + 줄거리 미확인 책 워밍 시 `BookSummaryJob` 트리거 + 큐레이션 문항 우선 서빙(`curated_content`, Stage 2 — 검수 문항 있는 책은 그 문항이 출제되고 제네릭 오프라인/미검증 AI로 안 덮이며 AI 워밍 억제)**.
- `missions/` — **학급 미션 도메인 서비스(menu_refactor 심화 §2.A, 별도 CLAUDE.md 없음)**. `progress_calculator.rb`(목표 진행도 — 단건 authoritative[game_plays 는 participation 기간 Asia/Seoul clamp] + `.batch` 교사 현황 N+1 제거. **특정 도서 목표(goal.books, '여러 책' any-of 허용목록 — 단일 book_id 에서 전환)면 그 목록 중 어느 책의 독후감/game_plays 든 `where(book_id: goal.books)` 로 집계**하고 비어 있으면 아무 책이나 집계 — goal_type 당 1개라 종류별 단건 필터. 각 goal row 에 표시용 `book_title`(여러 책이면 " · "로 이은 문자열) 포함. EvaluateProgress 는 그대로 후보만 좁히고 book 인정 여부는 여기 completed? 가 authoritative 로 판정한다) · `rewarder.rb`(**정확히-1회 지급** — claim[조건부 UPDATE `WHERE rewarded_at IS NULL`]+credit[`credit_points!`]을 한 `ApplicationRecord.transaction`으로 감싸 이중지급·under-award 동시 차단, m6 목표-0/미발행 가드, 방송·후크는 커밋 후 `run_point_side_effects!`) · `evaluate_progress.rb`(단일 진입점 — 독후감 승인 `on_report_approved`[report.classroom_id 스탬프 타깃=count/trigger parity]·게임 신규 `on_game_play`[현재 소속만]·편입/잡용 `evaluate_pending`) · `assignment_sync.rb`(발행 시 학급 학생 전원 배정 `on_publish` + 편입 시 `for_user`, 각각 배정 직후 즉시 평가로 지연배정 갭 흡수, `create` 비-bang+RecordNotUnique rescue 로 멱등). 완료 판정 단일 진실은 `mission_participations.completed_at`(주기 백스톱은 `app/jobs/missions/reevaluate_job.rb`).
- `challenges/` — **챌린지 목표 도메인 서비스(미션 서브시스템 미러, 별도 CLAUDE.md 없음)**. `progress_calculator.rb`(목표 진행도 — `missions/progress_calculator.rb` 미러이나 **경계가 학급이 아니라 뷰어 학생 본인 활동 전체**[스코프 적격은 EvaluateProgress 가 판단], 기간은 `challenge.window_start`~`window_end`; goal 별 여러 책 any-of 허용목록 필터(`goal.books`)는 동일) · `rewarder.rb`(정확히-1회 지급, `missions/rewarder.rb` 미러 — **published/active 게이트 없음**[완료 인정 기간은 ProgressCalculator 의 window 가 이미 강제], amount>0 일 때만 `credit_points!`/`run_point_side_effects!`) · `evaluate_progress.rb`(`missions/evaluate_progress.rb` 미러 + **지연(lazy) 참여** — `on_report_approved`/`on_game_play` 가 참여대상[전국+소속학교]·활성[기간내]·해당 목표 있는 챌린지에 participation 을 지연 생성한 뒤 Rewarder 호출. `evaluate(challenge)`는 상세 조회 시 뷰어 평가[멱등]).
- `recommendations/` — 공식 추천도서 XLSX 파이프라인(별도 CLAUDE.md 없음). `xlsx_reader.rb`는 전체목록 worksheet XML의 필수 헤더를 검증하고 어린이 분과만 정규화한다(10MB·5,000행 상한). `importer.rb`는 ISBN 누락·오류가 한 권이라도 있으면 업로드 전체를 거부하고, 정규화 ISBN-13으로 Book upsert와 digest 멱등·트랜잭션 활성 교체를 담당한다. 커밋 뒤 `RecommendationCoverEnrichmentJob`(표지 결측)과 **장르 공란 도서에 대한 `BookEnrichmentJob`**(추천 XLSX 유입 도서는 시드 사전계산·네이버 검색 경로를 모두 우회해 무장르로 남으므로, `SearchService` 신규 유입과 같은 무API 장르 추론 트리거를 이 경로에도 건다) + **줄거리 미확인 도서에 대한 `BookSummaryJob`**(BookEnrichmentJob 과 동형 미러, 게임 재구성 Phase 4 code-review 후속 — 추천 XLSX 유입은 온디맨드 워밍(resolve)을 거치지 않아 `content_provider` 의 §1d 트리거가 안 걸리므로, 신규 유입 커버를 위해 임포터가 직접 예약. summary blank·checked_at nil 인 책만, 잡이 무키 no-op·멱등이라 조건 없이 예약해도 안전)을 예약해 비동기 보강한다(동일 파일 재업로드도 재시도 예약, 잡은 멱등).

## 소규모 하위 폴더 (별도 CLAUDE.md 없음)
- `moderation/text_denylist.rb` — **결정적 한국어 금칙어 필터(무API·공용)**. 두 소비자가 **서로 다른 리스트**를 쓴다: `QUIZ`(AI 생성 게임 콘텐츠 게시 전 검증 — 실패=게시제외 비파괴, `QuizModerator` 가 소비하며 기존 동작 보존용으로 넓음)와 `FORUM`(학생 토론 자유입력 저장 검증 — 실패=저장거부 대면실패라 `새끼`(곰 새끼)·`꺼져`(불이 꺼져) 처럼 정상어에 부분일치하는 낱말을 뺀 명백한 욕설만). `hits(text, list:)`/`flagged?`. 매칭은 부분문자열(한국어 단어경계 불명확)이라 FORUM 은 리스트 자체로 오탐을 줄인다. 잔여는 신고·교사 모더레이션으로 사후 회수(정직화).
- `books/` — 도서 검색·보강·추론·중복정리·ISBN 정규화 5파일. `isbn.rb`는 ISBN-10/13 체크디지트를 검증하고 ISBN-10·하이픈 입력을 숫자 ISBN-13으로 통일한다. `search_service.rb`는 네이버 결과와 등록 입력에도 이 정규화를 적용하고 `category: searched`로 ISBN upsert하며, 무키/실패 시 로컬 검색으로 폴백한다. `catalog_enricher.rb`는 ISBN이 있는 큐레이션 도서의 표지·출판사를 네이버 ISBN 정확 일치 결과로 제자리 보강하고, **네이버가 색인하지 못한 판본(ISBN 0건)은 정보나루 `cover_url_for` 표지로 폴백**(표지만 채우고 저자·출판사는 큐레이션 값 보존)하며 별도 searched 행을 만들지 않는다(service·library·throttle DI, 양쪽 무키 시 no-op). `deduplicator.rb`는 제약 도입 전 레거시의 정규화 ISBN 중복과 고신뢰 공란-ISBN 그림자를 대표 도서로 병합해 메타·참조를 보존한다(기본 dry-run, `APPLY=1`만 삭제). `genre_inference.rb`는 외부 API 없이 가중 n-gram kNN과 규칙으로 10개 장르를 추론한다(규칙 2티어: **DECISIVE**=고유명사·도메인 전문어 고정밀 신호로 kNN 신뢰도와 무관하게 우선, **STRONG**=문학 제목과 겹치는 넓은 주제어라 kNN 이 불확실할 때[`RULE_OVERRIDE_CONFIDENCE` 0.72 미만]만 적용).
- `library/data4library_service.rb` — 정보나루(data4library.kr) OpenAPI 래퍼. `popular_loans`(인기대출, `loanItemSrch`)는 무키/실패 시 `[]` 반환(호출자는 CSV 업로드 폴백)·실패 사유는 `last_error` 로 노출. **`cover_url_for(isbn13)`(도서 상세조회 `srchDtlList`)**는 네이버가 색인하지 못한 절판·구판본의 표지 URL(`bookImageURL`)을 공급해 `CatalogEnricher` 폴백이 소비하며, 무키·실패·표지 없음 시 nil(`last_error` 는 건드리지 않아 두 메서드가 독립). **인근 도서관(§5.2): `libraries_holding(isbn13:, region:, page_size: 1000)`(`libSrchByBook` — 시도 소장 도서관 목록, 응답 `response.libs[].lib` 정규화 `[{code,name,address,tel,homepage,latitude,longitude}]`; 무키/미존재 → `[]`, 원격 실패 → **nil**로 :none 과 :error 를 구분, numFound>page_size 는 truncation 경고) + `loan_status(lib_code:, isbn13:)`(`bookExist` — loanAvailable Y→`:available`/N→`:unavailable`/에러·미존재→`:unknown`, `{status:, fetched_at:}` 반환으로 캐시 히트에도 조회시각 보존, `BOOK_EXIST_READ_TIMEOUT`=4s 짧은 read timeout).** 둘 다 `last_error` 불오염(권수는 정보나루 미제공이라 표시 안 함).
- `library/region_codes.rb` — **`Library::RegionCodes`(인근 도서관 §5.1)**. `School#region`(교육청명, 17교육청 폐집합)을 키로 정보나루 자체 시도 2자리 코드(법정동 아님 — 서울만 우연히 11=11)에 매핑하는 `SIDO_BY_OFFICE`(특별자치 개명 별칭 포함) + 주소 첫 토큰 보조맵 `SIDO_BY_ADDRESS`. `for_school(school)`(region 우선→address 폴백→nil)·`for_office`·`for_address`. 접미사 제거(전라북도→전라북) 금지.
- `library/nearby_availability.rb` — **`Library::NearbyAvailability`(인근 도서관 오케스트레이션 §5.3, PORO)**. `initialize(book:, school:, service:, cache: Rails.cache, max_libraries: 5, time_budget: 5.seconds, now:)`(cache·clock 주입 seam — `rate_limiter` 선례). `call` → `Result(state:, libraries:, as_of:)`: state=`:ok`/`:none`/`:no_key`/`:no_location`/`:error`/`:no_isbn`. 절차 = 무키가드→ISBN가드→시도코드(`RegionCodes`) 도출→`libraries_holding`(24h 캐시, nil→:error)→**gu 토큰 완전일치 필터**(주소 공백분할 `== gu`, `include?` 금지로 서구⊄달서구; gu nil=세종은 시도 전체 유지)→`max_libraries` 컷→`loan_status`(확정값만 15min 캐시 — 일시 `:unknown`은 미캐시로 회복 억제 방지, `{status,fetched_at}` 통째) 팬아웃(시간예산 초과분은 `:unknown` degrade)→as_of=표시 도서관 `fetched_at` 최소값(정직 "○분 기준" 라벨 근거). 무키·실패·주소불명 어떤 경우도 크래시 없이 degrade.
- `schools/` — NEIS 학교기본정보 연동 3파일. `gu_parser.rb`(도로명주소→시군구, 도농복합시·세종 단층제·시도 토큰 생략 대응) + `neis_fetcher.rb`(`schoolInfo` 전체 건수 기반 페이지네이션, 재시도, 국내 17개 교육청·코드 보유 초등학교 정규화; 무키는 `[]`, 원격/불완전 수집은 `FetchError`로 기존 CSV 보존) + `snapshot_validator.rb`(필수값·코드 중복·17개 교육청·전국 최소 건수 검증). connection DI로 테스트 네트워크를 차단한다.

## 패턴·규칙
- **`available?` / graceful 폴백**: 외부 연동 서비스(`ai/*`·`books`·`library`)는 클래스 메서드 `self.available?`(내부 `configured?`/`available?`)로 **키 존재 여부만** 판단하며 네트워크를 호출하지 않는다. 키가 없거나 원격이 실패하면 규칙기반 첨삭·로컬 캐시·빈 배열 등으로 폴백한다.
- **Faraday 연결 주입**: 외부 HTTP는 Faraday로 하며, `initialize` 에 `connection:`/`*_connection:` 인자를 두어 테스트에서 스텁 연결을 주입(네트워크 차단)한다. 자격증명은 `initialize(api_key:)` 기본값으로 `ENV["…"].presence || Rails.application.credentials.dig(...)`(ENV 우선, credentials 폴백)에서 읽으며, 테스트는 `api_key:` 인자로 직접 주입해 두 소스를 우회한다.
- **멱등 포인트 델타**: 재제출/재첨삭 파밍을 막기 위해 이미 지급한 최고 적립액 대비 초과분(delta)만 `User#award_points` 로 적립한다(`games/point_award.rb`·`app/jobs/ai_review_job.rb` 공통 패턴). 게임은 상한을 `quiz.origin`으로 분기한다(teacher=per-quiz, system=콘텐츠축; `games/CLAUDE.md`).

---
> ⚠️ **유지보수 규칙**: 이 폴더의 파일이 추가·삭제되거나 역할이 바뀌면 이 CLAUDE.md도 함께 갱신하세요. 하위 폴더 구조가 바뀌면 관련 상·하위 CLAUDE.md 링크도 확인하세요.
