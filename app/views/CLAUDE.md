# app/views/ — ERB 뷰 (리소스별 화면 + 역할별 레이아웃)

컨트롤러가 렌더하는 모든 ERB 템플릿이 리소스 디렉토리 단위로 모여 있습니다. 학생·교사·사서·학교관리자·총괄관리자 5개 역할의 화면이 공존하며, 역할별 레이아웃(application·admin·print·mailer)으로 감쌉니다. 부분 화면은 partial(`_이름.html.erb`), 비동기 부분 갱신은 turbo_stream(`.turbo_stream.erb`)으로 응답합니다.

## 파일 / 하위 리소스
- `account_links/` — **계정 연동(MERGE) 학생 셀프서브(account_linking_seasons_plan §Phase 3)**. `new`(작년 학교·학급·이름·비번 폼 — 학교 선택은 `schools/_picker`[with_classroom, **학년도 기본=지난 학년도 current-1**] + 이름·비번, post → preview)·`preview`(확인화면 — `MergeService#preview`로 집계한 작년 계정 회복 자산[독후감·몬스터·배지·포인트·경험치·레벨] stat-card + **hidden 서명 토큰**[비번 미저장] + "네, 가져올게요" `button_to` confirm + 되돌리기 안내 `state-banner--info`). 디자인 토큰(.card·.stat-card·.form-*·.btn·.state-banner) 사용. 진입점은 마이페이지 계정 섹션.
- `admin/` — 총괄관리자 콘솔. 기존 전역 CRUD 화면(`users/show`의 비밀번호 초기화는 총괄이 6자 이상을 직접 입력하며 `required`·`minlength=6`) + `recommendation_imports/index`(추천도서 XLSX 업로드, 현재 어린이 추천 목록, 이력) + **`audit_logs/index`(P1-3 일반 감사 원장 — 작업/실행자/기간 필터, 대상·범위·최소 메타데이터·IP, 읽기 전용 50건 페이지네이션)** + **`game_contents/index`(게임 콘텐츠 에스컬레이션, Phase 3 §4.5 — 전국 노출 system 풀 퀴즈의 신고 콘텐츠를 숨김/복원/삭제, `admin/shared/_pager`)** + **`account_links/index`(계정 연동 감사, account_linking_seasons_plan §Phase 4 — 병합 요약 테이블[생존자·이동 요약·from/to·수행자·시각·상태]+검색+되돌리기 button_to, **PII 미노출**: snapshot 원본 안 찍음)**. 목록 페이지네이션은 공통 `admin/shared/_pager` + `admin/moderation/_pager`를 사용한다.
- `board_posts/` — 학급 게시판. 목록·상세 + `_board_post`·`_cheer_button`·`_sticker` partial
- `books/` — 도서 목록·상세 + `_book_card`·`_autocomplete_field`(공용 도서 자동완성 필드 — 표시 input + hidden `book_id` + 결과목록 + 표지, strict/fallback. `book_search` 컨트롤러 부착, 퀴즈·게임·독후감 공용. **opt-in 원격검색 확장**(모두 기본 off·미전달 시 마크업 불변): `remote_search_url`이면 루트에 remote-search-url value 출력, `show_search_button`이면 입력 옆 `type: "button"` "🔍 검색" 버튼(`book-search#manualSearch`), `isbn_field`(+`isbn_value`)면 원격 선택 isbn 을 스태시할 hidden("isbn" 타깃, 등록은 제출 시 서버) — 독후감 새 글에서 타이핑=로컬 autocomplete, 검색버튼=네이버 remote_search 로 2-URL 분리) + `_multi_autocomplete_field`(**멀티북 바스켓** — `book-basket` 컨트롤러 + 중첩 `book-search`strict, 미션·챌린지 목표별 '여러 책' any-of 지정 전용 신규 partial. 타이핑=로컬 자동완성, 🔍 검색=원격, 고른 책은 칩으로 누적되며 로컬은 hidden `[ids][]`·원격은 hidden `[isbns][]` 배열로 제출[서버 `GoalBooks`가 등록·중복 제거], `selected_books` 프리필 지원. 단일 선택용 `_autocomplete_field`와 별개) partial
- `challenges/` — 챌린지(도전과제). `index`(**stretched-link 카드**[`reports/_report` 관용구 미러 — 컨테이너 `relative` + 제목 `challenge_path` 링크 `after:absolute after:inset-0` + 참여/수정/삭제 버튼 클러스터 `relative z-10`, 카드 전체 클릭→상세]에 제목·소개(description)·**학생 본인 진행상황**[`@progress_by_challenge` nil 가드]·기간 표시, 개별 책 목록 없음; 교직원=관리 버튼["새 챌린지 만들기"])·`show`(상단=제목·**소개글**·범위·기간·보상, 중단=학생 목표+본인 진행률 progress-bar+완료 배지 / 교직원 목표 정의 표시+수정·삭제 버튼, **하단=목표 지정 도서 목록 `shared/_activity_book_list`**[책→reading_activity, 자유 독서면 안내])·`_form`(미션 `_form` 미러 — 제목·**소개글 text_area**·노출범위 안내·시작/종료일·목표 fieldset[승인 독후감 수·게임 완료 수 + 목표별 '여러 책' 지정 `books/_multi_autocomplete_field`]·완료 보상 포인트)·`new`/`edit`.
- `cheers/` — 응원 반영 `update.turbo_stream.erb`(turbo_stream 전용)
- `dashboard/` — 역할별 대시보드. 학생 홈은 **navbar 바로 아래 활성 몬스터·포인트 요약 → 공식 추천도서 → 우리 반 미션 → 우리 반 인기 도서 → "이 책은 어때요?" 책 발견** 순으로 렌더한다(몬스터·포인트를 최상단, 그 아래 책 추천). **추천도서**·**우리 반 인기 도서**·**책 발견** 세 섹션 모두 **다른 책 보기** 링크로 다음 6권을 즉시 순환한다(추천도서는 날짜 기준 시작 묶음 + 후보가 6권보다 많을 때만 `more_recommended_books?`로 버튼 노출, `root_path(recommend:)`/앵커 `recommended-books`; 인기 도서는 **날짜 시드 없이 실제 1위부터** 시작하고 후보가 6권보다 많을 때만 `more_popular_books?`로 버튼 노출, `root_path(popular:)`/앵커 `popular-books`; 책 발견은 **정보나루 인기도서 풀 유무 분기** — 풀 있으면 복원추출 랜덤 6권[active 제외·삭제 id top-up], 없으면 기존 추천/클래식 결정적 회전 폴백, 풀>6일 때만 `more_discovery_books?`로 "다른 책 보기" 버튼 노출, `root_path(discovery:)`/앵커 `book-discovery`). 우리 반 미션은 담임교사가 발행한 진행 중 미션이 있을 때만 표시되며, **각 미션 카드는 제목·소개(description)·목표별 진행상황·기간만 보이고(지정 도서 목록은 숨김) 클릭 시 미션 상세(`mission_path`)로 이동**한다(challenge-mission-detail). 홈 카드에는 `@joinable_challenge_count`(전국+소속학교 가입 가능 챌린지 수) 배지가 함께 표시된다.
- `libraries/` — **내 서재 `show`(menu_refactor 심화 PR5)**. 책별 활동 카드(승인/검토 중 독후감·게임 완료 배지 + 최근 활동일 + "이 책으로 활동하기" 독서활동 진입) + kind 필터(전체/독후감/게임) + 책 미연결 레거시 독후감 그룹. `StudentLibraryQuery` 소비. **책 제목(+레거시 그룹 제목)은 링크**로, `reports_path(book_id:)`/`reports_path(book_title:)`로 그 책의 내 독후감 목록에 진입한다(요구 2).
- `reading_activities/` — **독서활동 허브 `show`(menu_refactor 심화 PR5)**. 책 미선택=등록 도서 자동완성(`books/_autocomplete_field` strict, GET→book_id) / 선택=선택 도서 카드(+책 바꾸기)+미션 문맥 배너+활동 카드 2종(독후감 쓰기→`new_report_path` book 전달, 독서 게임 칩→`games_<표면>_play_path(book_id:)` + **게임 칩 아래 "✍️ 이 책 문제 내기" 링크**[`new_quiz_contribution_path(book_id:)`, 학생 출제 기여 진입점, Phase 3 §4.1]). **가용성 게이트(게임 재구성 Phase 4 §2b)**: 퀴즈·나는 누구게? 칩은 컨트롤러가 넘긴 `@quiz_available`/`@whoami_available`(`Games::ContentProvider.game_content_available?`)이 true 일 때만 렌더하고(콘텐츠 못 만드는 책은 은닉+안내 문구), **책 소개 대결·뒷이야기 이어쓰기 칩은 항상 표시**(창작 소셜, 콘텐츠 무관). 은닉만으론 URL 직접 진입을 못 막으므로 컨트롤러 플레이 게이트(base_controller `resolve_on_demand`)가 동일 판정으로 리다이렉트한다(§2c). **활동 카드 아래에 인근 도서관 섹션(인근 도서관 §5.5)**: `turbo_frame_tag "nearby_libraries"`(`src: nearby_libraries_reading_activity_path(book_id:)`, `loading: :lazy`)로 첫 렌더를 막지 않고 스피너→프레임 채움. `_nearby_libraries`(동일 id `<turbo-frame>` 래핑 필수)는 `Library::NearbyAvailability::Result` 를 받아 `:ok`=제목 "📍 우리 학교 근처에서 빌리기"+도서관 리스트(이름·주소·홈페이지 링크[http(s)만 `target=_blank rel=noopener`, 그 외 텍스트]+상태 배지 `대출 가능`/`대출 중`/`확인 필요`)+"정보나루 제공·○분 기준·도서관 확인" 주석, `:none`=`shared/empty_state`("없어요"), `:no_location`=주소없음 안내, `:no_key`/`:error`/nil=섹션 숨김(빈 프레임).
- `forum_post_likes/` — 토론 글 좋아요 반영 `update.turbo_stream.erb`(turbo_stream 전용)
- `games/` — 독서게임 4종(Phase 3 온디맨드 + 소셜; 게임 재구성 Phase 1·2 로 정리 — classic[→quiz 통합]·vocab[hard-delete] 뷰 dir 및 `_matching_form` partial 제거). `catalog/index`(도서→게임 진입 관문 — 공용 `shared/student_nav` 렌더[`active: :games`]. **도서 전량 렌더 제거·재구성**: 상단 공용 도서 검색(`book-search`) + 하단 게임 칩[`games_catalog` 컨트롤러가 `book:selected` 구독해 활성화, `@games`=CATALOG.select 라 sequel 자동 반영, 선택된 `book_id`로 진입]) + 실동작 show: mcq 계열 `quiz`(`_quiz_form`, 4지선다), `whoami`(hint_reveal — 서버 상태의 공개 힌트만 렌더, 정답·잔여수 무유출, 힌트 공개는 `button_to`→`whoami#reveal_hint` 서버 렌더 진행), `book/play`(**책 소개 대결** — 도서별 소개 목록[득표순·`button_to` 투표/취소]·작성 폼·정적 작성 가이드, 퀴즈 파이프라인 밖 소셜), `sequel/play`(**뒷이야기 이어쓰기** — 도서별 뒷이야기 목록[득표순·`button_to` 공감/취소]·작성 폼·정적 작성 가이드 + `_feedback`[작성자 본인에게만 보이는 격려 AI 코멘트, `turbo_stream_from sequel` 구독으로 `SequelFeedbackJob` 완료 시 라이브 갱신, 점수·등급 없음], 창작 소셜). 공용 `_regenerate`(다시 뽑기 버튼 + "포인트는 최고 기록만 반영" 안내, 퀴즈 2종 system 판만).
- `layouts/` — 역할별 레이아웃. `application`(기본 — 최상단에 `shared/_app_header`(브랜드 옐로 헤더 밴드) 렌더 + **유동 페이지 셸**: 고정 mt-28·브레이크포인트 컨테이너 제거, `<main>`이 `max-w-[96rem] mx-auto` + 유동 거터[px-4/sm:px-6/lg:px-8]·세로 리듬[py-6] 소유, `shared/flash` 렌더, **`<main>` 뒤에 학생 전용 발견 드레인**[로그인 학생의 `pending_celebration`이 있으면 `shared/_discovery_modal` 렌더])·`admin`(총괄관리자 콘솔 — 최상단 `shared/_app_header` 렌더 추가 + `min-h-screen`을 body 로 올려 헤더 밴드와의 이중 높이 버그 제거, **반응형 오프캔버스 사이드바**: lg 이상 좌측 고정, 미만은 상단 햄버거로 여닫는 패널[`admin-sidebar` Stimulus 컨트롤러], `max-w-[96rem]` 넓은 본문, flash=`.state-banner`)·`print`(인쇄 전용, `@media print`)·`mailer`(html/text). 각 뷰는 `.page-shell*`(page-shell + -wide/-content/-reading/-form)로 `<main>` 안에서 max-width 만 제어한다.
- `learn/` — 학습 홈(index)
- `librarian/` — 사서 화면. `dashboards`·`events` CRUD·`loans`(대출 목록)
- `missions/` — **학생 미션 상세 `show`(challenge-mission-detail)**. 상단 미션 정보 카드(제목·소개·기간·보상·`@progress` 목표별 진행바[**nil 가드** — 교직원 렌더 시 진행상황 섹션 숨김, `challenges/show` 패턴 미러]) + 하단 `shared/_activity_book_list`(목표 지정 도서→reading_activity, 자유 독서면 안내). `MissionsController#show` 렌더, 홈 미션 카드·teacher 미션 관리와 분리된 학생 열람 화면.
- `monsters/` — 몬스터 도감·상세 + `_active_monster`·`_detail`·`_dex_grid`·`_monster_card`·**`_story`**·**`_completion_finale`** partial. 보유·도달한 폼은 `monster_sprite`로 기본 정적 PNG를 렌더하고, `monster_detail` 상단만 애니메이션 WebP를 렌더한다(에셋 누락 시 공용 미발견 SVG 폴백). **상세 상단 소개 카드(`_detail`)는 좌측에 큰 일러스트, 우측에 번호·이름·속성·조건·액션을 두는 전폭 가로 레이아웃**이다(진화 로드맵 partial 은 제거 — 내용이 `_story`와 중복). **잠긴(미보유) 카드·상세**는 헬퍼 `unlock_progress_items`로 해금 조건과 현재 진행도(예: "승인 독후감 4/6편")를 표시한다(`_monster_card`·`_detail`, `monsters_controller#index`/`#show`가 넘기는 `ReadingStats` 스냅샷 기준). **`_story`(P2-3 몬스터 성장 서사)**는 상세(`show`) 하단에 그 몬스터의 이야기를 3단계 장면(기·승·전·결)으로 보여 준다 — 보유 학생의 **도달 단계까지 장면을 공개**하고 이후 장면은 잠금 티저로 두며(진화 동기), **완전 성장 시 성장 메시지**를 공개한다(미보유는 안내 문구 + **잠긴 단계 티저**[번호·잠금·"???"·단계, 구 `_evolution_roadmap`에서 이관]). 24개 라인의 완전형을 모두 모으면 `_completion_finale`가 도감 완성 피날레를 공개한다. 소스는 `MonsterLore`(런타임 `db/seeds/monster_stories.yml`).
- `ocr/` — (뷰 없음) 사진 업로드 후 `OcrController`가 compose(edit) 화면으로 redirect하고, 본문 채움/실패 안내는 `OcrJob`이 `report_editor` 채널로 방송한다(기존 `create.turbo_stream.erb`는 redirect 통일로 제거).
- `passwords/` — 본인 비밀번호 변경 `edit`(현재/새/새 확인 3필드 폼, `PATCH password_path`, 역할무관). 오류는 공통 상단 토스트(`shared/_flash`)가 담당.
- `profiles/` — 학생 마이페이지 `show`(독서가 레벨·활동 통계·바로가기 + 계정 섹션). 성장 바로가기는 `growth_path`로 연결하고 계정 카드에 현재 닉네임·랭킹 참여 여부와 설정 링크를 표시한다(P1-5/8). **계정 섹션은 로그아웃 대신 `edit_password_path` 비밀번호 변경 링크**를 둔다(로그아웃은 전역 밴드 `shared/_app_header`로 이동). **'나의 활동'의 '작성한 독후감'/'선생님 확인' stat-card 는 링크**로 각각 `reports_path`/`reports_path(reviewed: "true")` 목록에 진입한다(요구 2). **계정 연동 진입점(account_linking_seasons_plan §Phase 3)**: 계정 섹션 하단에 `@account_link_available`(컨트롤러 게이트 — 플래그 on + 학급 학생 + 미연동)이 true 일 때만 "계정 연동하기"(`new_account_link_path`) 링크를 노출한다.
- `pwa/` — PWA 자산. `manifest.json.erb`·`service-worker.js`
- `quiz_contributions/` — **학생 출제 기여 폼 `new`(전국 공유 문제은행 UGC, Phase 3 §4.1)**. content_axis 미지정=유형 선택(객관식/나는 누구게? 카드 2종), 지정=축별 간단 폼(mcq: 질문+보기4[라디오 정답]+해설 / hint_reveal: 정답+힌트3+해설). 선택 도서 카드·"선생님이 확인한 뒤 보여 줘요" 안내. 디자인 토큰(.card·.form-*·.state-banner·.btn) 사용.
- `rankings/` — 랭킹 화면 + `_podium`·`_ranking_row` partial. **개인 이름은 전부 `ranking_name`(닉네임)만 표시하며 실명 폴백이 없다**. 성장 화면에서 참여 학생이 선택해 들어오는 보조 경로이고 상단에 "나의 성장으로 돌아가기"를 둔다. 탭 = 우리 반(class)·**학년(grade)**·우리 학교(school)·전국(nation)·챌린지·명예의 전당. 전국(`nation`) 탭은 Top100 + 본인이 Top100 밖이면 "우리 학교" 행을 별도 표기. **학년(grade) 탭(랭킹 시즌제 §Phase 1)**은 포디움·실시간 방송 없이 `_ranking_row` 개인 행만 렌더한다(뷰어 학교 + 같은 학년). **시즌 XP 표시**: `_ranking_row`·`_podium` 이 읽기 플래그 `AppSetting.feature_enabled?("ranking_seasons", scope: 행 학생의 학급, default: false)`가 켜지면 평생 `user.experience` 대신 현재 학년도 시즌 경험치(브로드캐스트 로컬 `season_experience` 우선 → class/grade 조회의 `season_experience` SELECT 별칭)를 배지에 표시하고, off(기본)면 기존 평생 경험치를 유지한다(플래그 스코프는 각 행 학생의 학급이라 HTTP 렌더·Pointable 브로드캐스트 양쪽에서 세션 없이 동작).
- `ranking_preferences/` — 학생 닉네임·랭킹 참여 설정 `edit`. 닉네임 필수 입력(2~12자)과 참여/비참여 라디오를 함께 받고, 첫 설정 전에는 취소 링크를 노출하지 않는다.
- `growths/` — **나의 성장(P1-8) `show`**. 승인 독후감 수·최근 5축 평균·최근 기록일, 최근 두 글의 5축 변화와 가장 많이 오른 축, 최근 12편의 교사 최종점수 시간순 표를 표시한다. 기록이 없으면 승인 후 보인다는 빈 상태를 제공하며 랭킹 참여자는 선택적으로 랭킹으로 이동할 수 있다.
- `registrations/` — 회원가입 `new`. 학교 선택은 `schools/_picker` partial 렌더(전량 select 아님). **학년도 입력(`academic_year`, 기본=`@current_academic_year`)** + 학년·반 필드로 담임 학급을 개설한다(마이그레이션 #38).
- `reports/` — 독후감 CRUD + `_book_chooser`·`_mode_chooser`·`_guided_compose`·`_photo_guide`·`_photo_capture`·`_form`·`_rubric`·`_body_field`·`_report`·`_report_detail`·**`_report_feedback`(신규)** partial. **새 글 진입은 책 고르기 → 모드 선택 스텝**: `new`가 **input_mode 우선 4분기** — `input_mode == "ocr"`→`_photo_capture`, `"keyboard"`(또는 위저드 프리필)→`_form`, 그 외 `report[book_title|book_id]` 프리필 있으면→`_mode_chooser`, 없으면(빈 문자열은 `present?`로 낙하)→`_book_chooser`(진입 1스텝). **`_book_chooser`(신규)**: `form_with method: :get, scope: :report` + 공용 도서 자동완성(타이핑=로컬 `autocomplete_books_path`, **🔍 검색=원격 `remote_search_books_path`**, 미선택 자유텍스트 fallback)로 책을 고른 뒤 "다음"으로 `report[book_id|book_title|remote_isbn]`를 GET 전달. **`_mode_chooser`**는 고른 책을 표시(+"책 다시 고르기")하고 두 모드 링크에 그 책을 실어 넘긴다(`input_mode: :keyboard|:ocr, report: {book_id,book_title,remote_isbn}`; `ocr_available? && Current.user&.ai_consented?`=false면 사진 카드 비활성 — 무키 또는 보호자 AI 미동의 학생은 사진 카드 은닉[P1-1]). `_photo_capture`는 책제목 자동완성(검색버튼 opt-in·`selected_book` 프리필) + 카메라/갤러리 단일 file input[`ocr[photo]`] + canvas/preview 압축·미리보기(`POST /ocr`). 사진 업로드는 `OcrController`가 draft 생성 후 `edit` 화면으로 redirect하고 `OcrJob` 방송이 본문 textarea를 채운다. `edit`은 `#ocr_reading_status`(읽는 중/실패 안내)를 두고, OCR 초안 첫 제출(`first_submit`)엔 `_form`이 dirty-check 미부착·"제출하기" 라벨로 렌더한다. **`_form`의 책 제목은 자유텍스트 대신 공용 도서 자동완성**(`books/_autocomplete_field`, 검색버튼 opt-in·`selected_book` 프리필)으로 받아 선택 시 `book_id`를 연결하고 표지를 자동 표시하되, 미선택 시 기존 `book_title` 자유입력 폴백을 유지한다(+`f.hidden_field :input_mode`로 create 검증 실패 재렌더 시 keyboard 폼 복원). `_report_detail`·`_report`는 연결된 `report.book.cover_url` 표지 이미지를 렌더한다. 목록은 중복 화면 제목을 생략하고 학생 메뉴 바로 아래에 `새 독후감 쓰기` 액션을 둔다. **이 액션은 활성 책 필터를 이월한다** — `@book`이면 `report:{book_id,book_title}`, 레거시 `@book_title_filter`면 `report:{book_title}`를 실어 `new_report_path`로 링크해(무필터는 맨몸) 새 글에서 책 재선택 스텝(`_book_chooser`)을 건너뛰고 바로 `_mode_chooser`로 진입한다(`reading_activities/show` 관례 미러; 빈 상태·필터배너의 "전체 보기"는 필터 해제 링크라 맨몸 유지). 목록 `_report`는 작성 학생에게 확인창이 있는 삭제 버튼을 제공하며, 교사 승인 방송에서도 버튼을 유지한다. **상세(`show`)는 제자리 '수정' 버튼 없이 고쳐쓰기 단일 경로**이고, 상태 의존 영역을 `_report_detail`(local: report, 루트 `dom_id(report, :detail)`)로 분리해 `turbo_stream_from @report` 구독 + AiReviewJob·approval 방송으로 **첨삭/재첨삭 완료·승인을 새로고침 없이 라이브 교체**한다(정책 게이트 버튼은 방송 밖 유지). **피드백 가시성 게이트(report-review-gate)**: `_report_detail`은 `report.feedback_visible?`(`reviewed? && rubric.present?`)로 분기한다 — (1) 미검토(ai_status done, reviewed false) 시 "선생님이 확인하고 있어요" 배너만 렌더 (2) 승인(reviewed true) 시 우측 칭찬/보완/성장 섹션 + 5축 막대그래프 렌더. **민감 피드백 섹션은 `_report_feedback` 파셜로 추출** — 5축·교사 편집본 칭찬/보완/성장(`student_feedback` 해시)·맞춤법을 `feedback_visible?`일 때만 렌더하는 조건부 파셜(은닉 아니라 미렌더 → 미래 필드 추가 시 셸 누출 방지). 향상도(improvement) 줄은 diff 섹션에 `reviewed?` 게이트만 달아 미검토 시 숨김. 레벨 배지도 `reviewed?` 일 때만 렌더. 목록 `_report.html.erb`의 레벨 배지·`teacher_comment`도 `reviewed?` 게이트. `student_status_badge` 헬퍼로 "선생님 확인 중"/"확인 완료" 상태 분기. **독후감 작성 학습단계화(요구 1)**: `_mode_chooser`의 직접쓰기 링크가 `guided: 1`을 실어 `new`의 keyboard 분기가 `params[:guided]`+본문 미프리필+`@guided[:questions]` 존재 시 **`_guided_compose`**(서론→본론→결론 흐름 밴드별 안내 질문 패널[예시 placeholder·비중 안내·"이어서 쓰기"] + 숨김 `_form` 래핑, `report-guide` Stimulus 가 답변을 본문 초안으로 조립, `@guided` 미비 시 `_form` 폴백)를, 그 외는 기존 `_form`을 렌더한다. 사진쓰기는 `_photo_capture`가 업로드 폼 이전에 **`_photo_guide`**(같은 `@guided`를 설명형 자가점검 가이드로 — `guide-modal` Stimulus 가 모달로 승격·자동 오픈, JS 미로딩 시 안내 카드로 폴백)를 렌더한다(가이드 상단에 "이름·학교·반이 보이지 않게 촬영" 개인정보 안내 — 양식의 식별정보 미전송 약속을 OCR 이미지 경로에서도 지키도록, P1-1). **자기 독후감 열람(요구 2)**: 목록 `_report`는 카드 전체 클릭(stretched-link — `<article>` `relative` + 제목 링크 `after:absolute after:inset-0` + 삭제 버튼 클러스터 `relative z-10`)로 상세 진입하고, `index`는 필터 문맥(『책제목』/선생님 확인 헤딩·배지·"전체 보기") + 필터 이월 페이지 링크(`request.query_parameters.merge(page:)`)를 둔다(내 서재 책 제목·마이페이지 타일이 `reports?book_id=`/`?book_title=`/`?reviewed=true` 로 진입).
- `school_admin/` — 학교관리자 화면. `neis`(생기부)·`stats`(통계)
- `schools/` — 학교 선택 하이브리드 피커 `_picker` partial(가입/로그인 공용). 시도/시군구 캐스케이딩은 native `<select>`를 채우고, **이름검색은 목록형 드롭다운(hidden `school_id` + 결과 `<li>` 클릭 선택)**으로 학교를 고른다. `with_classroom: true`(로그인 폼)면 **학년도 select(기본=현재 학년도 `current_academic_year` local, current±1 범위)** + 선택 학교의 학급만 스코프 로드(학년도 변경 시 재조회). 학년도 select 는 로그인 폼 전용(가입 폼은 `with_classroom: false` 라 미렌더 — 가입은 `registrations/new` 가 별도 학년도 입력).
- `sessions/` — 로그인 표면 3화면. `new`(**안내 인덱스** — 처음 접속 시 학생/교직원 로그인 선택 카드, 폼 없음)·`student_new`(학생 로그인 폼, 학교 선택은 `schools/_picker` partial 렌더[`current_academic_year` local 전달] + **학년도 select** + 학급 드롭다운)·`staff_new`(교직원 이메일·비밀번호 로그인 폼). 로그아웃은 별도 뷰 없이 `DELETE /session`.
- `shared/` — 앱 공통 partial. `_app_header`(전역 브랜드 옐로 헤더 밴드 — 좌: 로고[`/icon.png`]+워드마크(`root` 링크), 우: **학생 계정 컨트롤**[이름("○○님", 볼드)·마이페이지·로그아웃, 옐로 위 흰 필+그림자]. `application`·`admin` 공용, 구 `_student_header`·서브헤더 뒤로가기 통합/대체)·`_flash`(notice/alert 를 `.state-banner`[--success/--error] 상단 고정 토스트로 통일 — `role`/`aria-live`, `top-20`으로 헤더 밴드 아래 배치; `layouts/application` 이 렌더)·`_empty_state`(디자인 토큰 빈 상태 카드 — dashed hairline-strong + surface-soft)·**`_activity_book_list`(미션·챌린지 상세 하단 활동 도서 목록 — locals `books:`[Book 배열], 인메모리 `reject(&:searched?)` 후 비면 '자유 독서' 안내+`reading_activity_path`[book_id 없이], 있으면 표지·제목 카드로 `reading_activity_path(book_id:)` 링크. `_home_book_grid` 스타일, 카드·제목 자체 렌더)**·`_seasonal_banner`(옐로 헤더 밴드와의 옐로-온-옐로 적층을 피해 블랙 프로모 스트립[`bg-ink`]으로 렌더)·`_student_nav`(학생 상단 **5메뉴**[홈·내 서재·독서활동·도감·**나의 성장** — 랭킹은 성장 화면 안의 선택 경로] **반응형 네비** — 모바일은 현재 메뉴가 표시된 햄버거 `<details>` 아래 세로 목록, `sm` 이상은 기존 가로 줄바꿈 필 바. 아이콘+라벨, 활성 탭=라벤더 파스텔 필+짙은 브랜드 블루 텍스트+`aria-current="page"`, 44px 터치, `active` 심볼 강조·`current_user.student?` 가드. 대시보드·독후감·게임 카탈로그·도감·미션·성장·랭킹 페이지가 공용 렌더해 메뉴 전반에서 navbar 유지)·`_discovery_modal`(몬스터 발견 연출 큐 모달 — `dex_no`로 species 조회·스프라이트 렌더·에셋 누락 시 이모지 폴백, `discovery` 컨트롤러가 순차 등장·표시 즉시 acknowledge)
- `stickers/` — 스티커 부여 `create.turbo_stream.erb`(turbo_stream 전용)
- `teacher/` — 교사 화면. `dashboards`·**`missions`(menu_refactor 심화 PR4 — **발행 후에도 소유 교사가 수정·삭제 가능(잠금 해제)** — `_form`[제목·설명·기간·고정 2목표(승인 독후감·게임) target·**종류별 '여러 책' any-of 지정**(`books/_multi_autocomplete_field` — 로컬 선택+🔍 검색 원격 추가, `mission[goal_books][<type>][ids/isbns]` 로 보내는 `GoalBooks` 해석, 비우면 아무 책이나 인정)·보상], `index` 상태필터+`_status_badge`[초안/예정/진행 중/종료/취소/보관], `show` 미션 조건(목표별 지정도서 『제목』/아무 책이나)+발행 버튼(draft만)+**소유 교사에겐 발행 후에도 수정·삭제 버튼 노출**(삭제 확인문구 "학생 진행·보상 기록도 함께 사라져요")+`ProgressCalculator.batch` 학생 진행 표(도서 목표 헤더에 책 제목), `edit` **상태 무관 전체 `_form`**[지정도서 프리필, 발행 미션엔 "수정 시 학생 진행 즉시 반영, 지급 보상 미회수" 안내])**·`prints`(상장·학급리포트·가정통신문·포트폴리오 인쇄물)·`quizzes`·**`quiz_contributions/index`(학생 기여 문제 검토 큐, Phase 3 §4.3 — 학생별 pending 카드에 밴드 select·축별 편집 폼[mcq 질문·보기4·라디오 정답 / hint_reveal 정답·힌트] + `submit_tag` 저장[독립 `form_with method: :patch`]. **승인·반려는 그 수정 폼 밖의 독립 `button_to`(형제 폼, method: :post)** — formaction/중첩으로 수정 폼 안에 두면 브라우저가 `_method=patch` 를 실어 POST 가 PATCH 로 승격돼 404 나므로 반드시 분리한다. 승인은 "방금 저장한 현재 페이로드"를 전국 공유 풀에 물질화하므로 수정이 필요하면 저장→승인 2스텝)**·`reviews`(독후감 첨삭)·`rubric_configs`·`students`(학생 추가·비밀번호 초기화 모두 교사가 6자 이상 비밀번호를 직접 입력하고 `required`·`minlength=6`. **AI 동의 UI(P1-1)**: 추가 폼에 보호자 개인정보 필수 체크박스 + AI 선택 체크박스 + `agree.pdf` 양식 링크, 학생별로 "AI 동의/AI 미동의" 상태 배지 + 동의/철회 `button_to`[`set_ai_consent`, consent 목표값]) + **`account_links`(계정 연동 §Phase 4 — `index` 병합 목록[생존자·이동 요약·되돌리기 button_to]/`new` 현재 학급 학생 select + 작년 계정 이름검색 후보 radio, `teacher/_nav`[active: :students])** + `_nav` partial(**🔥 챌린지 탭** 포함). **대시보드(`dashboards/show`)에 "✍️ 학생 기여 문제 검토" 섹션**(pending 건수·검토 큐 링크, 있을 때만).
- `topics/` — 토론 주제 목록·상세 + `_forum_post`(좋아요 + **신고 🚩 버튼**[자기 글·이미 신고한 글 제외, turbo_confirm])·`_like_button` partial. **reading_discussion 진입점 복원**: `index`·`show` 가 `shared/student_nav`(active: :activity)를 렌더해 학생 이탈감을 없애고, 책 앵커드 맥락(📖 도서)·안전 문구·교사 학급 선택(다학급 담임)·글/제목 길이 제한을 노출한다. 책 앵커드 진입은 `reading_activities/show`("💬 이 책으로 토론하기" 섹션 + 인라인 개설), 홈은 `dashboard/student`("💬 우리 반 책 이야기" 카드). 교사 대시보드(`teacher/dashboards/show`)는 "🚩 신고된 토론 글" 섹션 + 숨김/해제 버튼.

## 패턴·규칙
- **이미지 아이콘**: 화면의 장식·상태·액션 아이콘은 이모지 대신 `ui_icon`과 `images/ui-icons.svg` 공용 symbol을 사용한다. `shared/_empty_state`는 의미별 128×128 투명 PNG(`images/empty_states/`)를 자동 선택하며, 책 소개 대결·뒷이야기 이어쓰기는 `sequel-writing.png`를 공유한다. 반응 스티커는 저장값 호환을 위해 이모지 값을 유지하되 화면에는 `sticker_icon` SVG로 렌더한다.
- **partial**: `_이름.html.erb` 접두. `render "리소스/이름"`으로 재사용하며 공통 UI는 `shared/`에 둠.
- **turbo_stream**: `액션.turbo_stream.erb`는 Turbo Stream 응답 전용. 페이지 전체 리로드 없이 특정 DOM 조각만 갱신(응원·구매·스티커·OCR 등 상호작용).
- **레이아웃**: 컨트롤러 네임스페이스에 맞춰 `admin`/`print`/`mailer` 레이아웃을 명시 지정, 그 외 화면은 `application` 사용.
- **역할 뷰**: 같은 개념(대시보드·미션·퀴즈)도 역할별 폴더(`teacher/`·`librarian/`·`school_admin/`·`admin/`)에 분리해 권한·화면을 격리.
- **디자인 시스템**: 전 역할 화면(학생·교사·사서·교무·총괄관리자) + 인쇄·메일·PWA·turbo_stream 이 `app/assets/tailwind/application.css`의 공통 토큰·컴포넌트(`.btn*`·`.card*`·`stat-card`·`.form-*`·`.badge*`·`.page-shell*`·`.state-banner*`·`.progress-bar`)를 사용한다(디자인 시스템 개편 완료, 2026-07-17. 토큰 기준은 [`DESIGN.md`](../../DESIGN.md)). 일반 뷰는 `.page-shell*`로 max-width 만 제어하되 **총괄관리자 콘솔 뷰는 `layouts/admin`이 `max-w-[96rem]` 폭·거터를 소유하므로 `page-shell`을 덧대지 않는다.** 파괴적 액션(삭제·정지·숨기기)은 `.btn-danger`, 복원적 액션(해제)은 `.btn-secondary`로 시각 분리한다. 인쇄물(`teacher/prints/*`, `layout "print"`)은 흑백 가독 우선, 메일러(`layouts/mailer`)는 이메일 클라이언트 호환을 위해 인라인 스타일만 사용한다.

---
> ⚠️ **유지보수 규칙**: 이 폴더의 파일이 추가·삭제되거나 역할이 바뀌면 이 CLAUDE.md도 함께 갱신하세요. 하위 폴더 구조가 바뀌면 관련 상·하위 CLAUDE.md 링크도 확인하세요.
