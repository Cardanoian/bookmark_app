<%# 도서 카드(P5.2). local: book %>
<article class="flex gap-3 rounded-xl border border-gray-200 p-3">
  <div class="h-28 w-20 shrink-0 overflow-hidden rounded-md bg-gray-100">
    <% if book.cover_url.present? %>
      <%= image_tag book.cover_url, alt: book.title, class: "h-full w-full object-cover", loading: "lazy" %>
    <% else %>
      <div class="flex h-full w-full items-center justify-center"><%= ui_icon :book_cover, class_name: "h-12 w-12" %></div>
    <% end %>
  </div>
  <div class="min-w-0">
    <%= link_to book.title, book_path(book),
          class: "line-clamp-2 font-semibold text-gray-900 hover:underline" %>
    <p class="mt-1 text-sm text-gray-500"><%= [ book.author, book.publisher ].compact_blank.join(" · ") %></p>
    <% if book.grade_band.present? %>
      <span class="mt-2 inline-block rounded-full bg-amber-100 px-2 py-0.5 text-sm text-amber-700"><%= book.grade_band %></span>
    <% end %>
    <%= book_meta_badges(book, wrapper_class: "mt-2") %>
  </div>
</article>
