<%# locals: (message:, icon: "📭") %>
<div class="flex flex-col items-center justify-center rounded-lg border border-dashed border-gray-200 py-12 text-center">
  <div class="mb-3 text-4xl" aria-hidden="true"><%= icon %></div>
  <p class="text-gray-500"><%= message %></p>
</div>
