<%# 대회 출품 식별 배너 — 로그인 첫 화면(sessions#new) 상단.
    바로 위가 브랜드 옐로 헤더 밴드(shared/_app_header)라 옐로-온-옐로 적층을 피해
    잉크(블랙) 스트립으로 둔다 — `shared/_seasonal_banner` 와 같은 관용구다.

    ⚠️ 요강은 화면에 **시·도명 · 학교명 · 출품자명**이 보이는 것을 금지한다. 이 배너에는
    대회명·연구 제목·대상 학년만 담고 소속·성명을 넣지 않는다(추가할 때도 마찬가지).

    문구는 **체험판 판본(`frontend/src/ui.jsx` 의 `ContestBanner`)과 1:1로 유지**한다 —
    같은 출품작을 두 경로(실서비스 / USB 체험판)로 보여 주므로 표기가 어긋나면 안 된다. %>
<div class="mx-auto mb-8 w-full max-w-2xl rounded-card bg-ink px-5 py-4 text-center text-on-dark"
     data-role="contest-banner">
  <p class="text-sm font-semibold">제20회 디지털교육연구대회</p>
  <p class="mt-1 text-base font-bold leading-relaxed">
    AI 첨삭과 게이미피케이션을 활용한 학교 독서교육 모델 「책갈피」 개발과 적용
  </p>
  <p class="mt-1 text-sm text-on-dark-muted">대상: 초등학교 1~6학년</p>
</div>
