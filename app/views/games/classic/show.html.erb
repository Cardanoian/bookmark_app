<% content_for :title, "고전 읽기 여행 · 책갈피" %>

<div class="w-full max-w-2xl mx-auto">
  <header class="mb-6 flex items-center justify-between">
    <div>
      <h1 class="text-2xl font-bold text-gray-900">🏛️ 고전 읽기 여행</h1>
      <p class="mt-1 text-sm text-gray-500"><%= @quiz.book&.title %> · 정답 한 문제당 <%= Games::QuizPlay::POINTS_PER_CORRECT %>포인트</p>
    </div>
    <%= link_to "← 게임 고르기", games_catalog_path, class: "text-sm text-gray-500 hover:text-gray-700" %>
  </header>

  <% if flash[:notice] %>
    <div class="mb-4 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700"><%= flash[:notice] %></div>
  <% end %>

  <%= render "games/quiz_form", quiz: @quiz, game: "classic", submit_label: "채점하기" %>
  <%= render "games/regenerate", quiz: @quiz, game: "classic" %>
</div>
